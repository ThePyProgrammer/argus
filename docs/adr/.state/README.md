# Blueprint State

Per-project Blueprint state lives here next to the ADRs.

- `state.toml` — project paths and last ADR metadata.
- `relationships.toml` — ADR graph nodes and relationships.
- `contexts.toml` — bounded-context mapping.
- `evidence.toml` — evidence freshness configuration and source mapping.
- `governance.toml` — governance mode and review expectations.
