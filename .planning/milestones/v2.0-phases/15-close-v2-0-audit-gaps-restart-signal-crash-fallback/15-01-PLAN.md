---
phase: 15-close-v2-0-audit-gaps-restart-signal-crash-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.py
  - frontend/src/hooks/useWebSocket.ts
  - frontend/src/components/pipeline/ApplyBar.tsx
  - frontend/src/stores/pipelineStore.ts
autonomous: true
requirements: [ABST-05, CTRL-02]
gap_closure: true

must_haves:
  truths:
    - "After any restart (algorithm switch, merge strategy, pipeline apply), frontend spinners clear via WebSocket signal"
    - "ApplyBar isApplying clears when backend confirms restart, not on a blind 1-second timeout"
    - "AlgorithmSection isRestarting clears when slam_restart_complete arrives via WebSocket"
    - "No dead code exports remain in pipelineStore.ts"
  artifacts:
    - path: "src/main.py"
      provides: "slam_restart_complete WebSocket emission after restart"
      contains: "slam_restart_complete"
    - path: "frontend/src/hooks/useWebSocket.ts"
      provides: "Dispatch slam_restart_complete to both slamStore and pipelineStore"
      contains: "setIsApplying"
    - path: "frontend/src/components/pipeline/ApplyBar.tsx"
      provides: "ApplyBar without blind setTimeout for restart confirmation"
    - path: "frontend/src/stores/pipelineStore.ts"
      provides: "Clean store without dead fetchNodeCatalog/fetchPresets exports"
  key_links:
    - from: "src/main.py"
      to: "streaming_viz._message_queue"
      via: "append slam_restart_complete message after restart completes"
      pattern: "slam_restart_complete"
    - from: "frontend/src/hooks/useWebSocket.ts"
      to: "pipelineStore.setIsApplying(false)"
      via: "slam_restart_complete case handler"
      pattern: "setIsApplying.*false"
---

<objective>
Emit `slam_restart_complete` WebSocket message from backend after every restart, wire frontend handlers to clear both `slamStore.isRestarting` and `pipelineStore.isApplying` via this signal, remove blind setTimeout from ApplyBar, and delete dead code from pipelineStore.

Purpose: Close MISSING-01 (slam_restart_complete never emitted), FLOW-BREAK-01 (restart confirmation never reaches frontend), FLOW-BREAK-02 (isApplying clears on fixed timeout), and orphaned exports.
Output: Reliable restart-complete signal path from backend to all frontend consumers.
</objective>

<execution_context>
@/home/prannayag/.claude/get-shit-done/workflows/execute-plan.md
@/home/prannayag/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit slam_restart_complete WS message and wire frontend handlers</name>
  <files>src/main.py, frontend/src/hooks/useWebSocket.ts</files>
  <read_first>
    - src/main.py (lines 400-490, the restart loop)
    - frontend/src/hooks/useWebSocket.ts (full file, especially slam_restart_complete handler at line 163)
    - frontend/src/stores/pipelineStore.ts (to confirm setIsApplying exists)
  </read_first>
  <action>
1. In `src/main.py`, after line 485 (`logger.info("Simulation restarted.")`), but still INSIDE the `with _restart_lock:` block (before line 485, after `streaming_viz.reset_cloud_tracking()`), add the slam_restart_complete emission:

```python
# Notify frontend that restart is complete
if streaming_viz is not None:
    streaming_viz._message_queue.append({
        "type": "slam_restart_complete",
        "payload": {},
    })
```

Place this AFTER `streaming_viz.reset_cloud_tracking()` (line 483) and BEFORE the `logger.info("Simulation restarted.")` (line 485). The emission must be inside the `with _restart_lock:` block so it fires only after all restart state is committed.

2. In `frontend/src/hooks/useWebSocket.ts`, update the `slam_restart_complete` case (lines 163-166) to also clear `pipelineStore.isApplying`:

```typescript
case 'slam_restart_complete': {
  useSlamStore.getState().setRestarting(false);
  usePipelineStore.getState().setIsApplying(false);
  fetchSlamState();
  break;
}
```

The import for `usePipelineStore` is already present at line 6.
  </action>
  <verify>
    <automated>grep -n "slam_restart_complete" src/main.py && grep -n "setIsApplying" frontend/src/hooks/useWebSocket.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "slam_restart_complete" src/main.py` returns a line containing `streaming_viz._message_queue.append`
    - `grep "setIsApplying" frontend/src/hooks/useWebSocket.ts` returns a line inside the slam_restart_complete case
    - The emission line appears between `reset_cloud_tracking` and `logger.info("Simulation restarted")`
  </acceptance_criteria>
  <done>Backend emits slam_restart_complete after every restart; WS handler clears both slamStore.isRestarting and pipelineStore.isApplying</done>
</task>

<task type="auto">
  <name>Task 2: Remove blind setTimeout from ApplyBar and delete dead pipelineStore exports</name>
  <files>frontend/src/components/pipeline/ApplyBar.tsx, frontend/src/stores/pipelineStore.ts</files>
  <read_first>
    - frontend/src/components/pipeline/ApplyBar.tsx (full file, especially handleConfirmApply lines 28-56)
    - frontend/src/stores/pipelineStore.ts (lines 218-253, the dead exports)
  </read_first>
  <action>
1. In `frontend/src/components/pipeline/ApplyBar.tsx`, replace the `.then(() => { ... })` success handler (lines 44-50) that uses `setTimeout`. The new handler should call `markApplied()` but NOT call `setIsApplying(false)` or `fetchSlamState()` -- those are now handled by the `slam_restart_complete` WS message in `useWebSocket.ts`. Replace lines 44-50 with:

```typescript
.then(() => {
  usePipelineStore.getState().markApplied();
  // isApplying cleared by slam_restart_complete WS handler in useWebSocket.ts
})
```

Also remove the `import { fetchSlamState } from '../../stores/slamStore';` on line 3 since it is no longer used in this file.

2. In `frontend/src/stores/pipelineStore.ts`, delete the dead `fetchNodeCatalog` function (lines 219-239) and the dead `fetchPresets` function (lines 241-253). These are exported but never imported anywhere. Remove from line 218 (after the store closing `));`) to line 253 (end of fetchPresets).
  </action>
  <verify>
    <automated>! grep -n "setTimeout" frontend/src/components/pipeline/ApplyBar.tsx && ! grep -n "fetchNodeCatalog\|fetchPresets" frontend/src/stores/pipelineStore.ts && cd /home/prannayag/pragnition/robotics/dimensional-applications/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "setTimeout" frontend/src/components/pipeline/ApplyBar.tsx` returns NO matches
    - `grep "fetchSlamState" frontend/src/components/pipeline/ApplyBar.tsx` returns NO matches
    - `grep "fetchNodeCatalog" frontend/src/stores/pipelineStore.ts` returns NO matches
    - `grep "fetchPresets" frontend/src/stores/pipelineStore.ts` returns NO matches
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>ApplyBar relies on WS signal for restart confirmation; dead fetchNodeCatalog and fetchPresets exports removed from pipelineStore.ts</done>
</task>

</tasks>

<verification>
1. `grep -c "slam_restart_complete" src/main.py` returns >= 1
2. `grep "setIsApplying" frontend/src/hooks/useWebSocket.ts` shows the call inside slam_restart_complete handler
3. `grep -c "setTimeout" frontend/src/components/pipeline/ApplyBar.tsx` returns 0
4. `grep -c "fetchNodeCatalog\|fetchPresets" frontend/src/stores/pipelineStore.ts` returns 0
5. `cd frontend && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Backend emits `slam_restart_complete` WebSocket message after every simulation restart
- `useWebSocket.ts` handler clears both `slamStore.isRestarting` and `pipelineStore.isApplying` on this signal
- `ApplyBar.tsx` no longer uses a blind 1-second setTimeout for restart confirmation
- Dead exports `fetchNodeCatalog` and `fetchPresets` removed from `pipelineStore.ts`
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-close-v2-0-audit-gaps-restart-signal-crash-fallback/15-01-SUMMARY.md`
</output>
