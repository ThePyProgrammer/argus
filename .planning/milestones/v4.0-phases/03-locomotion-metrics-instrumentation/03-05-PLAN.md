---
phase: 03-locomotion-metrics-instrumentation
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - src/locomotion/metrics.py
  - src/locomotion/env.py
  - tests/locomotion/test_locomotion_metrics_collector.py
  - tests/locomotion/test_argus_go2_env_metrics.py
autonomous: true
gap_closure: true
requirements:
  - LOC-METRICS-02
must_haves:
  truths:
    - "Zero-command standing episodes do not fail with `progress_stalled` after the progress window."
    - "Progress-stall failure is evaluated only when desired translational command magnitude is non-trivial; yaw-only commands do not require XY progress."
    - "ArgusGo2Env does not return `terminated=True` from false `progress_stalled` during repeated zero-command standing steps."
  artifacts:
    - path: "src/locomotion/metrics.py"
      provides: "Desired-translational-command-gated progress-stall stability failure semantics"
      contains: "min_progress_command_speed_m_per_s"
    - path: "src/locomotion/env.py"
      provides: "Env propagation of desired command into collector stability evaluation without false termination"
      contains: "record_step"
    - path: "tests/locomotion/test_locomotion_metrics_collector.py"
      provides: "Collector regression tests for zero-command and yaw-only no-progress standing windows"
      contains: "test_zero_command_stationary_steps_do_not_progress_stall"
    - path: "tests/locomotion/test_argus_go2_env_metrics.py"
      provides: "Env regression test proving repeated zero commands do not terminate as progress_stalled"
      contains: "test_zero_command_standing_does_not_terminate_progress_stalled"
  key_links:
    - from: "src/locomotion/env.py"
      to: "src/locomotion/metrics.py"
      via: "desired_command passed to LocomotionMetricsCollector.record_step"
      pattern: "desired_command=self._command"
    - from: "src/locomotion/metrics.py"
      to: "LocomotionMetricsConfig"
      via: "configurable translational-command gate per D-08"
      pattern: "min_progress_command_speed_m_per_s"
---

<objective>
Close the Phase 3 verification gap by making progress-stall termination conditional on a non-trivial desired translational command.

Purpose: LOC-METRICS-02, D-05, D-06, D-07, and D-08 require stability termination thresholds to be explicit and valid across scenarios/controllers. Standing still under a zero XY command is valid locomotion behavior, so it must not become a `progress_stalled` failure solely because distance does not increase.
Output: Targeted metrics/env fix plus collector-level and env-level regressions for zero-command standing and yaw-only commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-CONTEXT.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-RESEARCH.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-PATTERNS.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-VERIFICATION.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-01-SUMMARY.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-02-SUMMARY.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-03-SUMMARY.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-04-SUMMARY.md
@src/locomotion/metrics.py
@src/locomotion/env.py
@tests/locomotion/test_locomotion_metrics_collector.py
@tests/locomotion/test_argus_go2_env_metrics.py

<interfaces>
Existing collector contract to preserve:

```python
class LocomotionMetricsCollector:
    def record_step(
        self,
        *,
        desired_command: Sequence[float] | np.ndarray,
        measured_base_velocity: Sequence[float] | np.ndarray,
        roll_rad: float,
        pitch_rad: float,
        base_height_m: float,
        base_xy_position: Sequence[float] | np.ndarray,
        action_target: Sequence[float] | np.ndarray,
        previous_action_target: Sequence[float] | np.ndarray,
        previous_previous_action_target: Sequence[float] | np.ndarray,
        joint_qpos: Sequence[float] | np.ndarray,
        joint_qvel: Sequence[float] | np.ndarray,
        dt: float,
        ...
    ) -> LocomotionMetricStep: ...
```

Concrete new config field:

```python
@dataclass(frozen=True)
class LocomotionMetricsConfig:
    min_progress_command_speed_m_per_s: float = 0.05
```

Concrete semantics:
- `desired_translational_speed_m_per_s = hypot(desired_command[0], desired_command[1])`.
- `_progress_stalled(...)` returns `False` when `desired_translational_speed_m_per_s < config.min_progress_command_speed_m_per_s`.
- Yaw-only commands such as `[0.0, 0.0, 0.8]` have translational speed `0.0`, so they do not require XY progress.
- Roll, pitch, and base-height failures remain active for all commands, including zero-command standing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add zero-command and yaw-only progress-stall regressions</name>
  <files>tests/locomotion/test_locomotion_metrics_collector.py, tests/locomotion/test_argus_go2_env_metrics.py</files>
  <read_first>
    - .planning/phases/03-locomotion-metrics-instrumentation/03-VERIFICATION.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-CONTEXT.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-04-SUMMARY.md
    - src/locomotion/metrics.py
    - src/locomotion/env.py
    - tests/locomotion/test_locomotion_metrics_collector.py
    - tests/locomotion/test_argus_go2_env_metrics.py
  </read_first>
  <behavior>
    - `test_zero_command_stationary_steps_do_not_progress_stall`: record `progress_window_steps + 3` stationary steps with desired command `[0.0, 0.0, 0.0]`, base XY `[0.0, 0.0]`, stable roll/pitch/height, `min_progress_m_per_s=0.001`, and `progress_window_steps=3`; every returned `step.failure_reason` is `None`, `collector.episode_summary().success is True`, and `collector.episode_summary().failure_reason is None`.
    - `test_yaw_only_stationary_steps_do_not_progress_stall`: record `progress_window_steps + 3` stationary steps with desired command `[0.0, 0.0, 0.8]`; every returned `step.failure_reason` is `None` and summary success remains `True`.
    - `test_nonzero_translational_command_still_progress_stalls_when_stationary`: record `progress_window_steps + 3` stationary steps with desired command `[0.2, 0.0, 0.0]`; at least one step after the progress window has `failure_reason == "progress_stalled"` and summary success is `False`.
    - `test_zero_command_standing_does_not_terminate_progress_stalled`: run `ArgusGo2Env.step(np.zeros(3, dtype=np.float32))` for `progress_window_steps + 3` fake steps with no base XY movement, stable base height, stable roll/pitch, and advancing fake simulation time; no step returns `terminated is True`, no returned `info["locomotion_metrics"]["stability"]["failure_reason"]` equals `"progress_stalled"`, and `env.last_locomotion_metrics_summary` remains `None` unless truncation is intentionally reached.
  </behavior>
  <action>Add the exact collector test names `test_zero_command_stationary_steps_do_not_progress_stall`, `test_yaw_only_stationary_steps_do_not_progress_stall`, and `test_nonzero_translational_command_still_progress_stalls_when_stationary` to `tests/locomotion/test_locomotion_metrics_collector.py`. Use `LocomotionMetricsConfig(progress_window_steps=3, min_progress_m_per_s=0.001, min_progress_command_speed_m_per_s=0.05)` in the tests so the window is short and the command gate is explicit. Add the exact env test name `test_zero_command_standing_does_not_terminate_progress_stalled` to `tests/locomotion/test_argus_go2_env_metrics.py`; use the existing `_make_env`, `_step_once`, fake MuJoCo patching, and zero velocity action patterns. Set `ArgusGo2EnvConfig(sim_steps_per_frame=1, max_episode_steps=20, metrics_config=LocomotionMetricsConfig(progress_window_steps=3, min_progress_m_per_s=0.001, min_progress_command_speed_m_per_s=0.05))` so repeated zero commands pass the progress window without truncating. In the env test, patch `src.locomotion.env.dispatch_controller` to return `ControllerResult(action=np.zeros(12, dtype=np.float64), metadata={"source": "test"})`; the fake `mj_step` must increment `data.time` by `0.002` without changing `data.qpos[0:2]` or `data.qpos[2]`. Do not modify production code in this task beyond whatever import adjustments are required for tests. Do not add the non-blocking contact horizon warning to this task.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py::test_zero_command_stationary_steps_do_not_progress_stall tests/locomotion/test_locomotion_metrics_collector.py::test_yaw_only_stationary_steps_do_not_progress_stall tests/locomotion/test_locomotion_metrics_collector.py::test_nonzero_translational_command_still_progress_stalls_when_stationary tests/locomotion/test_argus_go2_env_metrics.py::test_zero_command_standing_does_not_terminate_progress_stalled -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_zero_command_stationary_steps_do_not_progress_stall" tests/locomotion/test_locomotion_metrics_collector.py` returns one test definition.
    - `grep -n "def test_yaw_only_stationary_steps_do_not_progress_stall" tests/locomotion/test_locomotion_metrics_collector.py` returns one test definition.
    - `grep -n "def test_nonzero_translational_command_still_progress_stalls_when_stationary" tests/locomotion/test_locomotion_metrics_collector.py` returns one test definition.
    - `grep -n "def test_zero_command_standing_does_not_terminate_progress_stalled" tests/locomotion/test_argus_go2_env_metrics.py` returns one test definition.
    - `grep -n "min_progress_command_speed_m_per_s" tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_argus_go2_env_metrics.py` returns test config usage.
    - The focused pytest command exits 0 after Task 2 implementation.
  </acceptance_criteria>
  <done>Regression tests prove the verification gap: zero-command and yaw-only standing windows are valid, while nonzero translational commands still enforce progress-stall failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate progress-stalled stability failure on desired translational command</name>
  <files>src/locomotion/metrics.py, src/locomotion/env.py, tests/locomotion/test_locomotion_metrics_collector.py, tests/locomotion/test_argus_go2_env_metrics.py</files>
  <read_first>
    - .planning/phases/03-locomotion-metrics-instrumentation/03-VERIFICATION.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-CONTEXT.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-RESEARCH.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-PATTERNS.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-03-SUMMARY.md
    - src/locomotion/metrics.py
    - src/locomotion/env.py
    - tests/locomotion/test_locomotion_metrics_collector.py
    - tests/locomotion/test_argus_go2_env_metrics.py
  </read_first>
  <action>In `src/locomotion/metrics.py`, add `min_progress_command_speed_m_per_s: float = 0.05` to `LocomotionMetricsConfig` and validate it in `__post_init__` with exact semantics `np.isfinite(value) and value >= 0.0`; raise `ValueError("min_progress_command_speed_m_per_s must be a non-negative finite value")` otherwise. Change `record_step` so it passes the already validated `desired` command vector into `_stability_payload`. Change `_stability_payload` signature to accept `desired_command: np.ndarray` in addition to `roll`, `pitch`, `height`, `xy`, and `dt`. Change `_progress_stalled` signature to accept `desired_command: np.ndarray`; compute `desired_translational_speed = float(np.linalg.norm(desired_command[:2]))`; if `desired_translational_speed < self.config.min_progress_command_speed_m_per_s`, return `False` before checking history length or distance delta. Preserve the existing roll, pitch, and base-height failure order before progress-stall evaluation. Preserve nonzero translational-command behavior: for desired `[0.2, 0.0, 0.0]`, zero distance over the configured progress window must still produce `"progress_stalled"`. Do not use yaw-rate magnitude to trigger XY progress checks. In `src/locomotion/env.py`, preserve the existing `desired_command=self._command` data flow into `record_step`; only adjust env code if test setup exposes that the desired command is not reaching the collector. Do not implement the non-blocking contact accumulator horizon warning in this plan.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py tests/locomotion/test_argus_go2_env_metrics.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "min_progress_command_speed_m_per_s" src/locomotion/metrics.py` returns the config field, validation, and progress-stall gate usage.
    - `grep -n "desired_translational_speed" src/locomotion/metrics.py` returns the local variable used in `_progress_stalled`.
    - `grep -n "np.linalg.norm(desired_command\[:2\])" src/locomotion/metrics.py` returns the translational-command magnitude calculation, or an equivalent explicit `math.hypot(float(desired_command[0]), float(desired_command[1]))` calculation is present.
    - `grep -n "desired_command" src/locomotion/metrics.py | grep "_stability_payload"` shows `desired_command` is passed into stability evaluation.
    - `grep -n "progress_stalled" tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_argus_go2_env_metrics.py` returns assertions for both no-failure zero/yaw-only cases and the nonzero translational failure case.
    - `.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py tests/locomotion/test_argus_go2_env_metrics.py -q` exits 0.
  </acceptance_criteria>
  <done>Progress-stall failure is disabled for zero-command and yaw-only standing windows, remains active for nonzero translational commands, and the focused Phase 3 metrics quick suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metrics config -> stability termination | Configured thresholds decide whether a local simulation episode fails. |
| env desired command -> collector stability gate | Desired velocity command controls whether XY progress is required. |
| collector failure_reason -> Gymnasium termination | Collector failures become `terminated=True` in `ArgusGo2Env.step`. |
| local tests -> benchmark confidence | Regression tests define whether standing-window stability metrics can be trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | `LocomotionMetricsConfig.min_progress_command_speed_m_per_s` | mitigate | Validate non-negative finite threshold before any metric recording. |
| T-03-05-02 | Integrity | `_progress_stalled` | mitigate | Gate progress-stall on `hypot(vx, vy)` from desired command; tests cover zero, yaw-only, and nonzero translational commands. |
| T-03-05-03 | Denial of Service | repeated standing steps | accept | Work is local in-memory telemetry using existing bounded histories; no external input or unbounded allocation is introduced. |
| T-03-05-04 | Information Disclosure | env/metrics info payload | accept | No secrets, files, network, auth, or PII are introduced; payload shape remains existing compact metrics info. |
| T-03-05-05 | Elevation of Privilege | local test/metrics code | accept | This plan changes local Python metric/test logic only and introduces no privilege, subprocess, network, or filesystem write surface. |
</threat_model>

<verification>
Run the focused Phase 3 quick suite after implementation:

```bash
.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py tests/locomotion/test_argus_go2_env_metrics.py -q
```

If `.venv/bin/python` is unavailable in the execution worktree, use the project venv path documented by completed Phase 3 summaries:

```bash
/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py tests/locomotion/test_argus_go2_env_metrics.py -q
```
</verification>

<success_criteria>
- `LocomotionMetricsConfig` exposes configurable `min_progress_command_speed_m_per_s` per D-08.
- Zero desired XY command does not produce `failure_reason == "progress_stalled"` after `progress_window_steps` stationary records.
- Yaw-only command does not produce `failure_reason == "progress_stalled"` unless a future explicit yaw-progress criterion is added; this plan does not add one.
- Nonzero desired XY command still produces `failure_reason == "progress_stalled"` when stationary beyond the progress window.
- `ArgusGo2Env.step` no longer returns false `terminated=True` for repeated zero-command standing windows.
- The Phase 3 quick metrics suite exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/03-locomotion-metrics-instrumentation/03-05-SUMMARY.md`.
</output>
