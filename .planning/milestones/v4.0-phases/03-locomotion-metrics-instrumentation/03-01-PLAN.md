---
phase: 03-locomotion-metrics-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locomotion/actions.py
  - src/locomotion/metrics.py
  - tests/locomotion/test_locomotion_metrics_collector.py
autonomous: true
requirements:
  - LOC-METRICS-01
  - LOC-METRICS-02
  - LOC-METRICS-03
must_haves:
  truths:
    - "Per-step metrics report desired, measured, signed error, absolute error, and aggregate tracking error for vx, vy, and yaw_rate."
    - "Stability records expose roll, pitch, base_height, base_height_deviation, distance_xy, failure_reason, fall_rate, and distance_before_failure."
    - "Action quality records expose action_delta_norm, action_jerk_proxy_norm, joint-limit counts, position_servo_effort_proxy, and position_target_saturation_proxy."
  artifacts:
    - path: "src/locomotion/metrics.py"
      provides: "LocomotionMetricsConfig, LocomotionMetricsCollector, LocomotionMetricStep, LocomotionEpisodeSummary"
      contains: "position_servo_effort_proxy"
    - path: "src/locomotion/actions.py"
      provides: "public copied joint bounds helper for metrics"
      contains: "def joint_position_bounds"
    - path: "tests/locomotion/test_locomotion_metrics_collector.py"
      provides: "Nyquist tests for LOC-METRICS-01/02/03 and D-03/D-08/D-13/D-16"
  key_links:
    - from: "src/locomotion/metrics.py"
      to: "src/locomotion/actions.py"
      via: "joint_position_bounds() import"
      pattern: "joint_position_bounds"
    - from: "tests/locomotion/test_locomotion_metrics_collector.py"
      to: "src/locomotion/metrics.py"
      via: "collector record_step tests"
      pattern: "LocomotionMetricsCollector"
---

<objective>
Build the core locomotion metrics collector and validation contract for command tracking, stability, and action-quality metrics.

Purpose: Phase 3 needs controller-agnostic metric definitions before environment wiring. This plan implements D-03, D-04, D-08, and D-13 through D-16 with bounded histories and explicit position-servo proxy labels.
Output: `src/locomotion/metrics.py`, a public action-bound helper in `src/locomotion/actions.py`, and focused pytest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-CONTEXT.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-RESEARCH.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-VALIDATION.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-PATTERNS.md
@.planning/phases/02-controller-plugin-baseline/02-CONTEXT.md
@.planning/phases/01-locomotion-env-contract/01-PATTERNS.md
@.planning/phases/02-controller-plugin-baseline/02-PATTERNS.md
@src/locomotion/actions.py
@src/locomotion/controller_dispatch.py
@src/metrics/metrics_tracker.py
@src/metrics/detection_metrics_tracker.py

<interfaces>
Create and use these concrete contracts:

```python
@dataclass(frozen=True)
class LocomotionMetricsConfig:
    history_size: int = 1000
    max_abs_roll_rad: float = 0.8
    max_abs_pitch_rad: float = 0.8
    nominal_base_height_m: float = 0.30
    min_base_height_m: float = 0.18
    base_height_tolerance_m: float = 0.12
    min_progress_m_per_s: float = 0.03
    progress_window_steps: int = 25
    joint_limit_tolerance_rad: float = 1e-3
    saturation_margin_fraction: float = 0.05
```

Required family keys for every payload: `command_tracking`, `stability`, `action_quality`, `contact_terrain`.
Required proxy field names per D-14 and D-16: `position_servo_effort_proxy`, `position_target_saturation_proxy`, `near_joint_limit_count`, `clipped_target_count`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create core collector tests for command, stability, and action quality</name>
  <files>tests/locomotion/test_locomotion_metrics_collector.py</files>
  <read_first>
    - .planning/phases/03-locomotion-metrics-instrumentation/03-CONTEXT.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-VALIDATION.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-PATTERNS.md
    - tests/metrics/test_metrics_tracker.py
    - tests/metrics/test_detection_metrics_tracker.py
    - tests/locomotion/test_argus_go2_env_action_modes.py
    - tests/locomotion/test_locomotion_metrics_collector.py
  </read_first>
  <behavior>
    - `test_records_command_tracking_errors`: desired `[0.5, -0.2, 0.3]` and measured `[0.2, -0.1, -0.1]` records signed errors `[-0.3, 0.1, -0.4]`, absolute errors `[0.3, 0.1, 0.4]`, and summary keys `vx_rmse`, `vy_rmse`, `yaw_rate_rmse`.
    - `test_records_stability_failure_and_distance_before_failure`: roll above `max_abs_roll_rad` sets `failure_reason == "roll_limit"`, summary `success is False`, `fall_count == 1`, `stability["fall_rate"] == 1.0`, and `distance_before_failure_m` equals recorded XY distance; a no-failure episode has `stability["fall_rate"] == 0.0`.
    - `test_records_action_quality_metrics`: two previous targets and current target produce nonzero `action_delta_norm`, nonzero `action_jerk_proxy_norm`, exact proxy keys `position_servo_effort_proxy` and `position_target_saturation_proxy`, commanded/observed joint violation counts, and per-joint count dictionaries.
    - `test_reset_episode_preserves_baseline_snapshot`: `capture_baseline()` followed by `reset_episode()` leaves `collector.baseline` non-None while current step history length becomes 0 per D-03.
    - `test_history_is_bounded_and_payload_is_finite`: `history_size=3` retains exactly 3 records and latest info contains only finite scalar/list values under the four metric families.
  </behavior>
  <action>Create `tests/locomotion/test_locomotion_metrics_collector.py` with local dataclass fakes or NumPy arrays only; do not instantiate `ArgusGo2Env` or MuJoCo in this file. Import `LocomotionMetricsConfig` and `LocomotionMetricsCollector` from `src.locomotion.metrics`. Encode the exact test names from `03-VALIDATION.md`: `test_records_command_tracking_errors`, `test_records_action_quality_metrics`, and add tests named `test_records_stability_failure_and_distance_before_failure`, `test_reset_episode_preserves_baseline_snapshot`, and `test_history_is_bounded_and_payload_is_finite`. Use `.venv/bin/python` in all command comments or docs if any are added. Add assertions proving `fall_rate` is exposed under the nested episode-summary stability family, e.g. `locomotion_metrics_summary["stability"]["fall_rate"]`, and proving no top-level `info["fall_rate"]` style key is part of the collector payload.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_records_command_tracking_errors" tests/locomotion/test_locomotion_metrics_collector.py` returns one test definition.
    - `grep -n "position_servo_effort_proxy" tests/locomotion/test_locomotion_metrics_collector.py` returns at least one assertion.
    - `grep -n "reset_episode" tests/locomotion/test_locomotion_metrics_collector.py` returns at least one assertion.
    - `grep -n "fall_rate" tests/locomotion/test_locomotion_metrics_collector.py` returns assertions for nested summary stability semantics and absence from top-level info payloads.
    - `.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py -q` exits 0 after Task 2 implementation.
  </acceptance_criteria>
  <done>Core metric tests assert LOC-METRICS-01, LOC-METRICS-02, LOC-METRICS-03 and locked decisions D-03, D-04, D-08, D-13, D-14, D-15, and D-16 with concrete field names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement metrics config, collector records, summaries, and joint-bound helper</name>
  <files>src/locomotion/actions.py, src/locomotion/metrics.py, tests/locomotion/test_locomotion_metrics_collector.py</files>
  <read_first>
    - src/locomotion/actions.py
    - src/locomotion/controller_dispatch.py
    - src/metrics/metrics_tracker.py
    - src/metrics/detection_metrics_tracker.py
    - src/bridge/sensor_types.py
    - tests/locomotion/test_locomotion_metrics_collector.py
    - .planning/phases/03-locomotion-metrics-instrumentation/03-RESEARCH.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-PATTERNS.md
  </read_first>
  <action>In `src/locomotion/actions.py`, add `joint_position_bounds() -> tuple[np.ndarray, np.ndarray]` returning copies of the 12-element joint-position low/high bounds currently used for `ACTION_MODE_JOINT_POSITION`; do not expose mutable module arrays. In new `src/locomotion/metrics.py`, implement `LocomotionMetricsConfig`, `LocomotionMetricStep`, `LocomotionEpisodeSummary`, and `LocomotionMetricsCollector`. Validate config values in `__post_init__`: `history_size >= 1`, thresholds positive, `0.0 < saturation_margin_fraction < 0.5`, and `progress_window_steps >= 1`. Collector methods must include `reset_episode()`, `capture_baseline()`, `baseline` property, `record_step(...)`, `latest_info_payload()`, `episode_summary()`, and `history_length`. `record_step` must accept copied arrays/values for `desired_command`, `measured_base_velocity`, `roll_rad`, `pitch_rad`, `base_height_m`, `base_xy_position`, `action_target`, `previous_action_target`, `previous_previous_action_target`, `joint_qpos`, `joint_qvel`, `dt`, and optional `contact_terrain_payload`. Compute command-tracking signed/absolute errors for `vx`, `vy`, `yaw_rate`; stability failure reasons `roll_limit`, `pitch_limit`, `base_height_low`, and `progress_stalled`; Phase 3 single-episode summary stability field `fall_rate = 1.0` if the episode summary has a non-None `failure_reason` or terminated failure and `0.0` otherwise, leaving cross-run aggregate fall rates to Phase 4; action delta and jerk norms over 12 joints; `position_servo_effort_proxy = sum(abs(action_delta) * abs(joint_qvel) * dt)`; commanded and observed joint-limit violation counts using `joint_position_bounds()` plus tolerance; and saturation proxy counts when a target is within `saturation_margin_fraction` of either bound. Payloads must always contain the four families `command_tracking`, `stability`, `action_quality`, and `contact_terrain`, with contact family empty/defaulted until Plan 02 extends it; `fall_rate` must appear only under the nested stability summary family, not as a top-level info/payload key. Do not add JSONL, CSV, CLI runner, frontend visualization, RL, MPC, WBC, ROS, or hardware work.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def joint_position_bounds" src/locomotion/actions.py` returns one public helper.
    - `grep -n "class LocomotionMetricsConfig" src/locomotion/metrics.py` returns one dataclass definition.
    - `grep -n "class LocomotionMetricsCollector" src/locomotion/metrics.py` returns one collector definition.
    - `grep -n "position_servo_effort_proxy" src/locomotion/metrics.py` returns code references and no field named `energy_joules` exists: `! grep -n "energy_joules\|torque_energy\|power_watts" src/locomotion/metrics.py`.
    - `grep -n "fall_rate" src/locomotion/metrics.py` returns implementation under episode-summary stability construction, and tests prove no top-level `info["fall_rate"]` key is emitted.
    - `.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py -q` exits 0.
  </acceptance_criteria>
  <done>Collector computes command tracking, stability, and action-quality metrics with bounded history, configurable thresholds, baseline-preserving episode reset, and explicit position-servo proxy labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env/controller input -> metrics collector | Action targets, commands, qpos/qvel, and config values enter metric calculations. |
| metrics collector -> Gymnasium info | Compact simulation telemetry is exposed to downstream callers. |
| MuJoCo model names -> foot/contact metrics | Named foot geom assumptions affect contact metric integrity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `LocomotionMetricsConfig`, `record_step` | mitigate | Validate finite arrays, exact 12-joint shapes, positive thresholds, and bounded saturation fraction before recording metrics. |
| T-03-02 | Repudiation / Integrity | action-quality proxy labels | mitigate | Use field names `position_servo_effort_proxy` and `position_target_saturation_proxy`; test forbids `energy_joules`, `torque_energy`, and `power_watts`. |
| T-03-03 | Denial of Service | collector history | mitigate | Use `deque(maxlen=config.history_size)` and test bounded eviction. |
| T-03-04 | Integrity | Go2 foot mapping | transfer to Plan 02 | Plan 02 implements fail-fast `FL`, `FR`, `RL`, `RR` mapping with missing/duplicate tests. |
| T-03-05 | Information Disclosure / DoS | per-step info payload | mitigate | `latest_info_payload()` returns scalar/list family summaries only; no raw unbounded arrays in payload. |
</threat_model>

<verification>
Run after this plan:

```bash
.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py -q
```

Then run the phase quick subset after dependent plans add their test files:

```bash
.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py tests/locomotion/test_argus_go2_env_metrics.py -q
```
</verification>

<success_criteria>
- LOC-METRICS-01 core per-step and aggregate command tracking errors exist for forward velocity, lateral velocity, and yaw rate.
- LOC-METRICS-02 core stability records and summaries include roll/pitch bounds, base-height deviation, fall/failure count, single-episode `fall_rate`, and distance before failure.
- LOC-METRICS-03 core action-quality records and summaries include smoothness, jerk proxy, joint-limit violations, effort proxy, and saturation proxy.
- D-03, D-04, D-08, D-13, D-14, D-15, and D-16 are implemented with grep-verifiable field names and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-locomotion-metrics-instrumentation/03-01-SUMMARY.md`.
</output>
