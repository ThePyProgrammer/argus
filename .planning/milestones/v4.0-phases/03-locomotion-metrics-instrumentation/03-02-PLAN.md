---
phase: 03-locomotion-metrics-instrumentation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/locomotion/metrics.py
  - src/locomotion/scenarios.py
  - tests/locomotion/test_locomotion_metrics_foot_mapping.py
autonomous: true
requirements:
  - LOC-METRICS-04
must_haves:
  truths:
    - "Go2 foot mapping resolves exactly FL, FR, RL, and RR against the loaded MuJoCo model and fails on missing or duplicate mappings."
    - "Per-step contact terrain metrics expose per-foot contact booleans, world-frame XY slip velocity while in contact, terrain-relative foot clearance for flat/slope/rough scenarios, and contact transition flags."
    - "Episode summaries expose duty factor per foot, slip summaries, clearance min/max per foot, gait timing summary, and scenario_success."
  artifacts:
    - path: "src/locomotion/metrics.py"
      provides: "Go2FootMapping, terrain-height helper, and contact/terrain metric calculations"
      contains: "FOOT_GEOM_NAMES"
    - path: "tests/locomotion/test_locomotion_metrics_foot_mapping.py"
      provides: "Nyquist tests for strict mapping, contact classification, slip, clearance, duty factor, and scenario success"
      contains: "test_resolves_go2_foot_geom_mapping"
  key_links:
    - from: "src/locomotion/metrics.py"
      to: "models/unitree_go2/go2.xml"
      via: "MuJoCo mj_name2id foot geom names"
      pattern: "FL|FR|RL|RR"
    - from: "tests/locomotion/test_locomotion_metrics_foot_mapping.py"
      to: "src/locomotion/metrics.py"
      via: "Go2FootMapping tests"
      pattern: "Go2FootMapping"
---

<objective>
Implement strict Go2 foot mapping plus contact/terrain proxy metrics.

Purpose: LOC-METRICS-04 and D-09 through D-12 require reliable contact identity and terrain proxy records; silent heuristic fallback would poison benchmark results.
Output: Contact/terrain extensions in `src/locomotion/metrics.py` and foot-mapping/contact tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-CONTEXT.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-RESEARCH.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-VALIDATION.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-PATTERNS.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-01-SUMMARY.md
@src/locomotion/metrics.py
@src/locomotion/scenarios.py
@models/unitree_go2/go2.xml
@src/metrics/mujoco_gt.py
@tests/metrics/test_mujoco_gt.py

<interfaces>
Existing contract from Plan 01 must remain intact: `LocomotionMetricsCollector.latest_info_payload()` returns family keys `command_tracking`, `stability`, `action_quality`, and `contact_terrain`.

Add these concrete names:

```python
FOOT_GEOM_NAMES = ("FL", "FR", "RL", "RR")

@dataclass(frozen=True)
class Go2FootMapping:
    foot_geom_ids: dict[str, int]

    @classmethod
    def from_mujoco_model(cls, model: Any) -> "Go2FootMapping": ...
```

Add a terrain helper contract consumed by contact/clearance metrics:

```python
def terrain_height_at(sample: ScenarioSample | None, x: float, y: float) -> float: ...
```

It must return `0.0` for plane terrain (`flat_ground`, `low_friction`, `push_disturbance`), derive slope height from `ScenarioSample.terrain_parameters["slope_radians"]` and the generated slope plane convention, and derive rough-heightfield height from `heightfield_data`/`heightfield_size` using nearest or bilinear lookup over the generated hfield extents. If current metadata is insufficient, add explicit metadata plumbing in `src/locomotion/scenarios.py`; do not hardcode non-flat terrain height to zero.

`contact_terrain` payload must include keys: `per_foot_contact`, `foot_xy_velocity_when_contact`, `foot_clearance_m`, `contact_transition`, and summary keys `duty_factor`, `slip_mean_m_per_s`, `slip_max_m_per_s`, `clearance_min_m`, `clearance_max_m`, `gait_symmetry_contact_balance`, `scenario_success`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create strict foot mapping and contact proxy tests</name>
  <files>tests/locomotion/test_locomotion_metrics_foot_mapping.py</files>
  <read_first>
    - .planning/phases/03-locomotion-metrics-instrumentation/03-CONTEXT.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-VALIDATION.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-PATTERNS.md
    - models/unitree_go2/go2.xml
    - tests/metrics/test_mujoco_gt.py
    - tests/locomotion/test_locomotion_metrics_foot_mapping.py
  </read_first>
  <behavior>
    - `test_resolves_go2_foot_geom_mapping`: loading `models/unitree_go2/go2.xml` through MuJoCo or a compatible patched XML resolves `FL`, `FR`, `RL`, and `RR` to four distinct geom ids.
    - `test_missing_required_foot_name_raises`: a fake resolver/model missing `RR` raises `ValueError` containing `Missing required Go2 foot geom 'RR'`.
    - `test_duplicate_foot_geom_id_raises`: duplicate ids raise `ValueError` containing `Duplicate Go2 foot geom ids`.
    - `test_no_substring_heuristic_fallback`: available names such as `front_left_foot` do not satisfy required name `FL`; error mentions `FL`.
    - `test_records_contact_slip_clearance_and_duty_factor`: controlled per-step foot positions and contact booleans compute XY slip only while in contact, clearance relative to `terrain_height_m`, per-foot duty factor, and `scenario_success is True` when no failure occurred.
    - `test_terrain_height_helper_returns_zero_for_plane_scenarios`: flat/low-friction/push-disturbance plane metadata returns exactly `0.0`.
    - `test_terrain_height_helper_derives_slope_height_from_parameters`: nonzero `slope_radians` and nonzero x/y sample produce nonzero expected plane height, proving D-11 is not hardcoded to zero for slope.
    - `test_terrain_height_helper_samples_rough_heightfield_data`: controlled `heightfield_data` grid returns the expected nearest or bilinear sampled height, proving D-11 is not hardcoded to zero for rough heightfields.
  </behavior>
  <action>Create `tests/locomotion/test_locomotion_metrics_foot_mapping.py` using the MuJoCo skip helper pattern from `tests/locomotion/test_argus_go2_env_contract.py` and fake model/data classes for missing/duplicate/no-heuristic tests. Test strict names `FL`, `FR`, `RL`, and `RR` exactly per D-09; do not accept prefixed names or substring fallback in Phase 3. Include tests for D-10, D-11, and D-12 using direct collector inputs so the file can run quickly without full environment stepping. Add terrain-height helper tests for plane, slope, and rough heightfield conventions per D-11; slope and rough tests must assert nonzero/non-flat expected values from scenario metadata, not `0.0` defaults.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_foot_mapping.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_resolves_go2_foot_geom_mapping" tests/locomotion/test_locomotion_metrics_foot_mapping.py` returns one test definition.
    - `grep -n "Missing required Go2 foot geom 'RR'" tests/locomotion/test_locomotion_metrics_foot_mapping.py` returns one assertion or regex.
    - `grep -n "front_left_foot" tests/locomotion/test_locomotion_metrics_foot_mapping.py` returns one no-heuristic test fixture.
    - `grep -n "test_terrain_height_helper_derives_slope_height_from_parameters" tests/locomotion/test_locomotion_metrics_foot_mapping.py` returns one test definition.
    - `grep -n "test_terrain_height_helper_samples_rough_heightfield_data" tests/locomotion/test_locomotion_metrics_foot_mapping.py` returns one test definition.
    - `.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_foot_mapping.py -q` exits 0 after Task 2 implementation.
  </acceptance_criteria>
  <done>Tests fail until strict mapping and contact/terrain proxy implementation exists, and then prove D-09 through D-12 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Go2 foot mapping and contact terrain aggregation</name>
  <files>src/locomotion/metrics.py, src/locomotion/scenarios.py, tests/locomotion/test_locomotion_metrics_foot_mapping.py</files>
  <read_first>
    - src/locomotion/metrics.py
    - src/locomotion/scenarios.py
    - tests/locomotion/test_locomotion_metrics_foot_mapping.py
    - src/metrics/mujoco_gt.py
    - models/unitree_go2/go2.xml
    - .planning/phases/03-locomotion-metrics-instrumentation/03-01-SUMMARY.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-RESEARCH.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-PATTERNS.md
  </read_first>
  <action>Extend `src/locomotion/metrics.py` with `FOOT_GEOM_NAMES = ("FL", "FR", "RL", "RR")`, `Go2FootMapping.from_mujoco_model(model)`, and contact/terrain methods that use explicit foot ids only. Implement `Go2FootMapping.from_mujoco_model` with `mujoco.mj_name2id(model, mujoco.mjtObj.mjOBJ_GEOM, name)` for each required foot; raise `ValueError(f"Missing required Go2 foot geom '{name}'")` when id < 0 and `ValueError(f"Duplicate Go2 foot geom ids resolved: {ids}")` when ids are not unique. Add `terrain_height_at(sample, x, y)` in `src/locomotion/metrics.py` or a clearly imported helper, plus any missing metadata plumbing in `src/locomotion/scenarios.py` needed to compute slope and rough-heightfield heights. The helper must return `0.0` only for plane terrain, compute slope height from existing slope plane parameters, and sample rough heightfields from `heightfield_data`/`heightfield_size` using nearest or bilinear lookup over the generated hfield extents. Add collector support for contact inputs: `foot_positions_world: dict[str, np.ndarray]`, `foot_contacts: dict[str, bool]`, `terrain_height_m: float`, and `scenario_failed: bool | None = None`. Compute per-foot XY velocity as `(current_xy - previous_xy) / dt` only when current contact is true; otherwise report `0.0`. In env integration, obtain `terrain_height_m` per foot from the helper at the foot world x/y. Compute foot clearance as `z - terrain_height_m` for every foot, track swing-phase min/max when contact is false, expose per-foot contact booleans and transition labels `touchdown`, `liftoff`, or `none`, and summarize duty factor as contact count divided by total recorded steps per foot. Compute `gait_symmetry_contact_balance` as max duty factor minus min duty factor. Compute `scenario_success` per D-07 as `True` only when an episode summary is requested with no failure threshold and terminal cause is truncation/survival; keep command tracking separate from this boolean. Preserve Plan 01 payload families and action-quality field names. Do not implement Phase 4 exports or multi-robot prefixed mapping support.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FOOT_GEOM_NAMES" src/locomotion/metrics.py` returns the required tuple.
    - `grep -n "mjOBJ_GEOM" src/locomotion/metrics.py` returns MuJoCo geom lookup code.
    - `grep -n "Duplicate Go2 foot geom ids" src/locomotion/metrics.py` returns fail-fast duplicate validation.
    - `grep -n "duty_factor" src/locomotion/metrics.py` returns summary generation code.
    - `grep -n "def terrain_height_at" src/locomotion/metrics.py` returns one terrain-height helper.
    - `grep -n "heightfield_data" src/locomotion/metrics.py src/locomotion/scenarios.py` returns rough-heightfield plumbing/lookup code.
    - `.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py -q` exits 0.
  </acceptance_criteria>
  <done>Contact/terrain proxy metrics satisfy LOC-METRICS-04 and D-09 through D-12 with strict Go2 foot identity and episode summaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MuJoCo model -> foot mapping | Model name table determines which geoms are feet. |
| MuJoCo contact/position state -> metrics collector | Contact booleans and geom positions become benchmark metrics. |
| collector history -> episode summary | Per-step contact records become aggregate success/duty/slip claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | contact metric inputs | mitigate | Validate finite foot positions and positive `dt` before calculating velocity/clearance. |
| T-03-02 | Repudiation / Integrity | effort labels | transfer to Plan 01 | Plan 01 tests proxy labels and forbids physical energy labels. |
| T-03-03 | Denial of Service | contact history | mitigate | Reuse collector bounded histories from Plan 01 for contact records. |
| T-03-04 | Integrity | `Go2FootMapping.from_mujoco_model` | mitigate | Resolve exactly `FL`, `FR`, `RL`, `RR` via `mjOBJ_GEOM`; raise on missing/duplicate ids; no substring fallback. |
| T-03-05 | Information Disclosure / DoS | `contact_terrain` payload | mitigate | Expose compact per-foot scalar dictionaries, not raw `data.contact` objects or unbounded position arrays. |
</threat_model>

<verification>
Run after this plan:

```bash
.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py -q
```
</verification>

<success_criteria>
- LOC-METRICS-04 is covered for foot slip, foot clearance, contact timing/duty factor, and scenario success rate.
- D-09 strict mapping is enforced with tests for missing, duplicate, and no-heuristic cases.
- D-10, D-11, and D-12 are implemented with per-foot per-step records and episode summaries.
</success_criteria>

<output>
After completion, create `.planning/phases/03-locomotion-metrics-instrumentation/03-02-SUMMARY.md`.
</output>
