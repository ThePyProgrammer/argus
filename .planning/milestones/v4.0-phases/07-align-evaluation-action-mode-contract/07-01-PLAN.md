---
phase: 07-align-evaluation-action-mode-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locomotion/evaluation.py
  - tests/locomotion/test_locomotion_evaluation_runner.py
  - tests/locomotion/test_locomotion_evaluation_exports.py
autonomous: true
requirements:
  - LOC-ENV-04
  - LOC-EVAL-03
requirements_addressed:
  - LOC-ENV-04
  - LOC-EVAL-03
must_haves:
  truths:
    - "Evaluation accepts velocity_command as the only evaluator-runnable action mode and preserves Phase 6 same-index command/export semantics."
    - "Evaluation rejects unknown, joint_position, and residual_baseline action modes during matrix validation before env construction, run directory creation, or artifact writing."
    - "Completed velocity_command artifacts record the action mode actually stepped at manifest, validated-cell, run, step, and episode levels."
  artifacts:
    - path: "src/locomotion/evaluation.py"
      provides: "fail-fast evaluator action-mode validation and mode-aware velocity-command action construction"
      contains: "_EVALUATOR_RUNNABLE_ACTION_MODES"
    - path: "tests/locomotion/test_locomotion_evaluation_runner.py"
      provides: "pre-side-effect rejection regressions and preserved velocity_command same-index action/export regression"
      contains: "test_unsupported_action_modes_fail_before_env_construction"
    - path: "tests/locomotion/test_locomotion_evaluation_exports.py"
      provides: "completed-run action-mode metadata and no-artifact rejection regressions"
      contains: "test_completed_velocity_command_run_records_action_mode_metadata"
  key_links:
    - from: "src/locomotion/evaluation.py"
      to: "src/locomotion/actions.py"
      via: "available_action_modes and ACTION_MODE_VELOCITY imported as the authoritative env action-mode source"
      pattern: "available_action_modes"
    - from: "src/locomotion/evaluation.py"
      to: "tests/locomotion/test_locomotion_evaluation_runner.py"
      via: "validate_evaluation_matrix rejects before env_factory is called"
      pattern: "calls == []"
    - from: "src/locomotion/evaluation.py"
      to: "tests/locomotion/test_locomotion_evaluation_exports.py"
      via: "run_evaluation_matrix writes artifacts only after completed velocity_command cells"
      pattern: "manifest.*action_mode"
---

<objective>
Close the evaluation-runner action-mode contract gap for LOC-ENV-04 and LOC-EVAL-03.

Purpose: The environment supports velocity-command, joint-position, and residual-over-baseline seams, but the evaluator currently only has a concrete velocity-command action source. This plan makes that boundary explicit in code: `velocity_command` remains runnable, `joint_position` and `residual_baseline` fail fast with actionable errors, unknown modes fail against the authoritative env action-mode list, and completed artifacts record only modes actually stepped.
Output: Test-first runner/export regressions plus evaluation-layer validation and metadata fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-align-evaluation-action-mode-contract/07-RESEARCH.md
@.planning/phases/07-align-evaluation-action-mode-contract/07-VALIDATION.md
@.planning/phases/07-align-evaluation-action-mode-contract/07-PATTERNS.md
@.planning/phases/06-repair-evaluation-runner-semantics/06-VERIFICATION.md
@src/locomotion/actions.py
@src/locomotion/evaluation.py
@tests/locomotion/test_locomotion_evaluation_runner.py
@tests/locomotion/test_locomotion_evaluation_exports.py

<interfaces>
Authoritative env action-mode names from `src/locomotion/actions.py`:
```python
ACTION_MODE_VELOCITY = "velocity_command"
ACTION_MODE_JOINT_POSITION = "joint_position"
ACTION_MODE_RESIDUAL_BASELINE = "residual_baseline"
def available_action_modes() -> list[str]: ...
def build_action_space(mode: str) -> spaces.Box: ...
```

Current runner side-effect ordering from `src/locomotion/evaluation.py` must remain:
```python
cells = validate_evaluation_matrix(matrix, max_matrix_runs=run_config.max_matrix_runs)
created_at = datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")
run_dir = _prepare_run_dir(run_config.output_root)
```

Phase 6 invariant to preserve:
```python
executed_commanded_velocity = list(action)
executed_command_source = source
executed_command_context = dict(initial_context)
# step rows use executed_* values from before env.step(action)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 runner and export contract tests for action modes</name>
  <files>tests/locomotion/test_locomotion_evaluation_runner.py, tests/locomotion/test_locomotion_evaluation_exports.py</files>
  <read_first>
    - .planning/phases/07-align-evaluation-action-mode-contract/07-VALIDATION.md
    - .planning/phases/07-align-evaluation-action-mode-contract/07-PATTERNS.md
    - .planning/phases/06-repair-evaluation-runner-semantics/06-VERIFICATION.md
    - tests/locomotion/test_locomotion_evaluation_runner.py
    - tests/locomotion/test_locomotion_evaluation_exports.py
    - src/locomotion/evaluation.py
  </read_first>
  <behavior>
    - In `tests/locomotion/test_locomotion_evaluation_runner.py`, add `test_unknown_action_mode_fails_before_env_construction` using `EvaluationMatrix(action_mode="not_a_mode")`; assert `pytest.raises(ValueError, match="Unknown locomotion action_mode 'not_a_mode'")`, `calls == []`, and `not any(tmp_path.iterdir())`.
    - In `tests/locomotion/test_locomotion_evaluation_runner.py`, add parametrized `test_unsupported_action_modes_fail_before_env_construction` over `("joint_position", "residual_baseline")`; assert `pytest.raises(ValueError, match="argus eval-locomotion currently runs action_mode='velocity_command'")`, `calls == []`, and `not any(tmp_path.iterdir())`.
    - In `tests/locomotion/test_locomotion_evaluation_runner.py`, keep or strengthen `test_runner_records_each_step_commanded_velocity_from_executed_action` so captured fake-env step actions equal same-index `result.step_rows[*]["commanded_velocity"]` for `[0.0, 0.0, 0.0]` then `[0.4, 0.0, 0.0]`.
    - In `tests/locomotion/test_locomotion_evaluation_exports.py`, add `test_completed_velocity_command_run_records_action_mode_metadata`; run the existing single-cell fake export env, read `manifest.json`, `steps.jsonl`, and `episodes.csv`, then assert `manifest["action_mode"] == "velocity_command"`, every `manifest["validated_cells"][*]["action_mode"] == "velocity_command"`, every `manifest["runs"][*]["action_mode"] == "velocity_command"`, every JSONL step row has `"action_mode": "velocity_command"`, and the first CSV row has `action_mode == "velocity_command"`.
    - In `tests/locomotion/test_locomotion_evaluation_exports.py`, add `test_rejected_action_mode_writes_no_artifacts`; call `run_evaluation_matrix(EvaluationMatrix(action_mode="joint_position"), EvaluationRunConfig(output_root=tmp_path), env_factory=_factory())`, assert the same unsupported-mode ValueError, and assert `not any(tmp_path.iterdir())`.
  </behavior>
  <action>Create the Wave 0 tests from `07-VALIDATION.md` rows 41-43. Use the selected contract direction exactly: `velocity_command` is evaluator-runnable; `joint_position` and `residual_baseline` are env-supported seams but not runnable by `argus eval-locomotion`; unknown strings fail against the env action-mode list. The actionable unsupported-mode error text must include `argus eval-locomotion currently runs action_mode='velocity_command'` and the rejected mode name. Do not create dummy 12-value actions. Do not weaken the Phase 6 same-index action/export assertions.</action>
  <acceptance_criteria>
    - `grep -R "test_unknown_action_mode_fails_before_env_construction" tests/locomotion/test_locomotion_evaluation_runner.py` finds exactly one test definition.
    - `grep -R "test_unsupported_action_modes_fail_before_env_construction" tests/locomotion/test_locomotion_evaluation_runner.py` finds exactly one test definition.
    - `grep -R "argus eval-locomotion currently runs action_mode='velocity_command'" tests/locomotion/test_locomotion_evaluation_runner.py tests/locomotion/test_locomotion_evaluation_exports.py` finds assertions in both files.
    - `grep -R "test_completed_velocity_command_run_records_action_mode_metadata" tests/locomotion/test_locomotion_evaluation_exports.py` finds exactly one test definition.
    - `grep -R "test_rejected_action_mode_writes_no_artifacts" tests/locomotion/test_locomotion_evaluation_exports.py` finds exactly one test definition.
    - `grep -R "not any(tmp_path.iterdir())" tests/locomotion/test_locomotion_evaluation_runner.py tests/locomotion/test_locomotion_evaluation_exports.py` finds pre-side-effect assertions for rejected modes.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; r=Path('tests/locomotion/test_locomotion_evaluation_runner.py').read_text(); e=Path('tests/locomotion/test_locomotion_evaluation_exports.py').read_text(); assert 'test_unknown_action_mode_fails_before_env_construction' in r; assert 'test_unsupported_action_modes_fail_before_env_construction' in r; assert 'test_completed_velocity_command_run_records_action_mode_metadata' in e; assert 'test_rejected_action_mode_writes_no_artifacts' in e; assert \"argus eval-locomotion currently runs action_mode='velocity_command'\" in r and \"argus eval-locomotion currently runs action_mode='velocity_command'\" in e; assert 'not any(tmp_path.iterdir())' in r and 'not any(tmp_path.iterdir())' in e"</automated>
  </verify>
  <done>Wave 0 tests encode fail-fast unsupported-mode behavior, no-artifact rejection behavior, velocity-command metadata, and preserved same-index command/export semantics before production implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fail-fast evaluator action-mode validation</name>
  <files>src/locomotion/evaluation.py, tests/locomotion/test_locomotion_evaluation_runner.py, tests/locomotion/test_locomotion_evaluation_exports.py</files>
  <read_first>
    - .planning/phases/07-align-evaluation-action-mode-contract/07-RESEARCH.md
    - .planning/phases/07-align-evaluation-action-mode-contract/07-PATTERNS.md
    - .planning/phases/06-repair-evaluation-runner-semantics/06-VERIFICATION.md
    - src/locomotion/actions.py
    - src/locomotion/evaluation.py
    - tests/locomotion/test_locomotion_evaluation_runner.py
    - tests/locomotion/test_locomotion_evaluation_exports.py
  </read_first>
  <action>In `src/locomotion/evaluation.py`, import `ACTION_MODE_VELOCITY` and `available_action_modes` from `src.locomotion.actions`. Add a module-level constant `_EVALUATOR_RUNNABLE_ACTION_MODES = (ACTION_MODE_VELOCITY,)`. In `validate_evaluation_matrix()`, after verifying `action_mode` is a non-empty string and before controller/scenario expansion returns cells, validate in this order: (1) if `action_mode not in available_action_modes()`, raise `ValueError(f"Unknown locomotion action_mode '{action_mode}'. Available env action modes: {available_action_modes()}")`; (2) if `action_mode not in _EVALUATOR_RUNNABLE_ACTION_MODES`, raise `ValueError(f"argus eval-locomotion currently runs action_mode='velocity_command'; requested action_mode='{action_mode}' is an env-supported seam but has no evaluator action source")`. Keep `velocity_command` cells valid and unchanged. Do not add evaluator support for `joint_position` or `residual_baseline` unless a real 12-value action source already exists in the source files read for this task; the selected contract direction says reject them. Preserve `_prepare_run_dir()` after validation, `_csv_safe()` CSV formula safety, `Run directory escapes output root` containment, `runner_step_count`, and Phase 6 `executed_commanded_velocity` same-index row labeling.</action>
  <acceptance_criteria>
    - `grep -n "from src.locomotion.actions import" src/locomotion/evaluation.py` shows imports for `ACTION_MODE_VELOCITY` and `available_action_modes`.
    - `grep -n "_EVALUATOR_RUNNABLE_ACTION_MODES = (ACTION_MODE_VELOCITY,)" src/locomotion/evaluation.py` finds the runnable-mode constant.
    - `grep -n "Unknown locomotion action_mode" src/locomotion/evaluation.py` finds the unknown-mode ValueError.
    - `grep -n "env-supported seam but has no evaluator action source" src/locomotion/evaluation.py` finds the unsupported-mode ValueError.
    - `grep -n "run_dir = _prepare_run_dir" src/locomotion/evaluation.py` still appears after `validate_evaluation_matrix(matrix` in `run_evaluation_matrix`.
    - `grep -n "executed_commanded_velocity" src/locomotion/evaluation.py` still finds Phase 6 same-index command/export logic.
    - `grep -n "writerow({field: _csv_safe" src/locomotion/evaluation.py` still finds CSV formula safety preservation.
    - `grep -n "Run directory escapes output root" src/locomotion/evaluation.py` still finds output path containment preservation.
    - `uv run python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py tests/locomotion/test_locomotion_evaluation_exports.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py tests/locomotion/test_locomotion_evaluation_exports.py -q</automated>
  </verify>
  <done>`validate_evaluation_matrix()` rejects unknown and env-supported-but-unevaluable action modes before side effects, while velocity-command evaluation and Phase 6 row semantics remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize action-mode artifact metadata and quick gate</name>
  <files>src/locomotion/evaluation.py, tests/locomotion/test_locomotion_evaluation_exports.py</files>
  <read_first>
    - .planning/phases/07-align-evaluation-action-mode-contract/07-VALIDATION.md
    - .planning/phases/07-align-evaluation-action-mode-contract/07-PATTERNS.md
    - .planning/phases/06-repair-evaluation-runner-semantics/06-VERIFICATION.md
    - src/locomotion/evaluation.py
    - tests/locomotion/test_locomotion_evaluation_exports.py
  </read_first>
  <action>Ensure all completed-run metadata surfaces record the action mode actually used for completed `velocity_command` runs. `_base_row(cell)` must continue to include `"action_mode": cell["action_mode"]` so step rows, episode rows, manifest run rows, and validated cells all carry `velocity_command`. `_build_manifest()` may keep top-level `"action_mode": matrix.action_mode` because validation now prevents rejected modes from reaching manifest creation. Do not write a manifest, steps, episodes, summary, comparison, or diagnostic artifact for rejected `joint_position`, `residual_baseline`, or unknown modes. Preserve `_ARTIFACT_FILES = ("manifest.json", "steps.jsonl", "episodes.csv", "summary.json", "comparison.md")` and `_csv_safe()` behavior.</action>
  <acceptance_criteria>
    - `grep -n '"action_mode": matrix.action_mode' src/locomotion/evaluation.py` finds the top-level manifest action-mode field.
    - `grep -n '"action_mode": cell\["action_mode"\]' src/locomotion/evaluation.py` finds the base-row action-mode field used by completed rows.
    - `grep -n '_ARTIFACT_FILES = ("manifest.json", "steps.jsonl", "episodes.csv", "summary.json", "comparison.md")' src/locomotion/evaluation.py` confirms artifact inventory is unchanged.
    - `grep -n "_csv_safe" src/locomotion/evaluation.py` confirms CSV safety remains in the writer path.
    - `uv run python -m pytest tests/locomotion/test_locomotion_evaluation_exports.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/locomotion/test_locomotion_evaluation_exports.py -q</automated>
  </verify>
  <done>The planned quick verification command passes for runner, export, env action-mode, and CLI parser surfaces after the implementation task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/matrix config -> evaluation validation | User-provided `--action-mode` or JSON `action_mode` crosses into matrix execution. |
| Evaluation action builder -> ArgusGo2Env.step | Numeric arrays produced by the runner cross into env action validation and MuJoCo control paths. |
| Evaluation runner -> artifact filesystem | Completed run metadata and CSV fields are written to disk under user-selected output roots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering / Repudiation | `validate_evaluation_matrix` in `src/locomotion/evaluation.py` | mitigate | Reject unknown, `joint_position`, and `residual_baseline` action modes before env construction, run directory creation, or artifact writing; tests assert `calls == []` and empty output root. |
| T-07-02 | Tampering / Denial of Service | `env.step(np.asarray(action, dtype=np.float32))` action path | mitigate | Keep evaluator runnable mode to `velocity_command` 3-vectors and rely on existing env/action-space tests for finite shape/bounds checks; do not synthesize unsupported 12-value actions. |
| T-07-03 | Repudiation | `manifest.json`, `steps.jsonl`, `episodes.csv` | mitigate | Write action-mode metadata only for completed velocity-command cells; rejected modes produce no artifacts. |
| T-07-04 | Tampering | CSV and output path writers in `src/locomotion/evaluation.py` | mitigate | Preserve `_csv_safe()` for episode CSV fields and `_prepare_run_dir()` resolved-root containment guard when touching export paths. |
</threat_model>

<verification>
Quick gate: `uv run python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py tests/locomotion/test_locomotion_evaluation_exports.py tests/locomotion/test_argus_go2_env_action_modes.py tests/test_main_args.py -q`

Full gate after Wave 1: `uv run python -m pytest tests/locomotion tests/bridge/test_sim_bridge.py tests/bridge/test_multi_bridge.py -q`
</verification>

<success_criteria>
- `velocity_command` evaluation still constructs envs, steps fake envs, and writes completed artifacts.
- `joint_position`, `residual_baseline`, and unknown action modes fail during validation before env construction and before any artifact directory appears.
- Completed artifacts contain `action_mode == "velocity_command"` wherever rows/cells/runs record action mode.
- Phase 6 same-index command/export behavior remains covered and green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-align-evaluation-action-mode-contract/07-01-SUMMARY.md`
</output>
