---
phase: 06-repair-evaluation-runner-semantics
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - tests/locomotion/test_locomotion_baseline_regression.py
  - .planning/phases/06-repair-evaluation-runner-semantics/06-VALIDATION.md
autonomous: true
requirements:
  - LOC-METRICS-05
  - LOC-EVAL-01
  - LOC-EVAL-02
  - LOC-EVAL-04
must_haves:
  truths:
    - "A stationary or command-ignoring controller cannot pass commanded flat-ground baseline acceptance."
    - "The Phase 6 quick gate proves active commands, stability distance exports, and baseline threshold semantics together."
    - "Validation metadata records deterministic pytest evidence without claiming runtime AI dependency."
  artifacts:
    - path: "tests/locomotion/test_locomotion_baseline_regression.py"
      provides: "stationary-controller negative regression for commanded locomotion baseline"
      contains: "test_stationary_controller_fails_commanded_locomotion_baseline_gate"
    - path: ".planning/phases/06-repair-evaluation-runner-semantics/06-VALIDATION.md"
      provides: "Phase 6 Nyquist status rows updated after green automated evidence"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "src/locomotion/evaluation.py"
      to: "tests/locomotion/test_locomotion_baseline_regression.py"
      via: "run_evaluation_matrix episode_rows feed assert_analytical_flat_ground_thresholds"
      pattern: "assert_analytical_flat_ground_thresholds\(result\.episode_rows\)"
    - from: "tests/locomotion/test_locomotion_baseline_regression.py"
      to: "06-VALIDATION.md"
      via: "quick gate command status evidence"
      pattern: "test_locomotion_baseline_regression.py -q"
---

<objective>
Lock Phase 6 repairs with a baseline regression gate and Nyquist validation update.

Purpose: Close LOC-EVAL-04 by proving commanded locomotion cannot be certified by a stable standing/no-op rollout, then run the full Phase 6 deterministic quick gate across command scheduling, distance exports, and baseline semantics.
Output: Stationary-controller negative test plus updated `06-VALIDATION.md` statuses after automated evidence is green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v4.0-MILESTONE-AUDIT.md
@.planning/phases/06-repair-evaluation-runner-semantics/06-AI-SPEC.md
@.planning/phases/06-repair-evaluation-runner-semantics/06-RESEARCH.md
@.planning/phases/06-repair-evaluation-runner-semantics/06-VALIDATION.md
@.planning/phases/06-repair-evaluation-runner-semantics/06-PATTERNS.md
@tests/locomotion/test_locomotion_baseline_regression.py
@tests/locomotion/test_locomotion_evaluation_runner.py
@tests/locomotion/test_locomotion_evaluation_exports.py

<interfaces>
Baseline helper contract from `tests/locomotion/test_locomotion_baseline_regression.py`:
```python
def assert_analytical_flat_ground_thresholds(rows): ...
```
Required row shape for the stationary negative fixture:
```python
{
    "success": True,
    "failure_reason": "",
    "commanded_velocity": [0.4, 0.0, 0.0],
    "command_source": "scenario_schedule",
    "tracking_rmse": 0.0,
    "distance_xy_m": 0.0,
    "base_height_min_m": 0.30,
    "roll_abs_max_rad": 0.0,
    "pitch_abs_max_rad": 0.0,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 stationary-controller negative baseline fixture</name>
  <files>tests/locomotion/test_locomotion_baseline_regression.py</files>
  <read_first>
    - .planning/phases/06-repair-evaluation-runner-semantics/06-VALIDATION.md
    - .planning/phases/06-repair-evaluation-runner-semantics/06-PATTERNS.md
    - .planning/v4.0-MILESTONE-AUDIT.md
    - tests/locomotion/test_locomotion_baseline_regression.py
    - tests/locomotion/test_locomotion_evaluation_runner.py
    - tests/locomotion/test_locomotion_evaluation_exports.py
    - src/locomotion/evaluation.py
  </read_first>
  <behavior>
    - Add `test_stationary_controller_fails_commanded_locomotion_baseline_gate` to `tests/locomotion/test_locomotion_baseline_regression.py`.
    - The test must call `run_evaluation_matrix` with a fake stationary env/controller path; direct row-only/helper assertions are not sufficient for this regression.
    - The fake stationary env must emit `current_command={"vx": 0.4, "vy": 0.0, "omega": 0.0, "source": "scenario_schedule"}`, maintain stable posture fields, and return a locomotion summary with `stability.distance_xy_m = 0.0`.
    - Wrap `assert_analytical_flat_ground_thresholds(result.episode_rows)` in `pytest.raises(AssertionError)` and assert the rejected rows contain `success=True`, empty `failure_reason`, `commanded_velocity=[0.4, 0.0, 0.0]`, `command_source="scenario_schedule"`, and `distance_xy_m=0.0`.
    - Keep the existing real MuJoCo smoke test optional via `_skip_if_mujoco_python_unsupported()`; do not remove the integration marker or weaken fixed-seed coverage.
  </behavior>
  <action>Create the Wave 0 baseline negative fixture from `06-VALIDATION.md` row 06-03-01. The test must run through `run_evaluation_matrix` with a fake stationary env/controller path, not a direct hand-built row. The fake env must emit a nonzero `current_command`, preserve stable posture metrics, produce `stability.distance_xy_m = 0.0`, and then prove `assert_analytical_flat_ground_thresholds(result.episode_rows)` raises `AssertionError`. This encodes the exact false-pass failure mode from the audit: nonzero commanded translation plus stable posture plus zero distance is not locomotion success. Use deterministic pytest only; do not introduce LLM judges, network calls, sleeps, or random seeds outside the existing matrix seed pattern.</action>
  <acceptance_criteria>
    - `grep -R "test_stationary_controller_fails_commanded_locomotion_baseline_gate" tests/locomotion/test_locomotion_baseline_regression.py` returns exactly one test definition.
    - `grep -R "\[0.4, 0.0, 0.0\]" tests/locomotion/test_locomotion_baseline_regression.py` finds the nonzero commanded velocity fixture.
    - `grep -R "run_evaluation_matrix" tests/locomotion/test_locomotion_baseline_regression.py` includes the stationary-controller test path.
    - `grep -R "stability.*distance_xy_m" tests/locomotion/test_locomotion_baseline_regression.py` finds the zero-distance fake-env summary fixture.
    - `grep -R "pytest.raises(AssertionError)" tests/locomotion/test_locomotion_baseline_regression.py` includes the stationary-controller test around `assert_analytical_flat_ground_thresholds(result.episode_rows)`.
    - `python -m pytest tests/locomotion/test_locomotion_baseline_regression.py -q` passes, with real MuJoCo smoke passing or skipping only under existing documented skip conditions.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/locomotion/test_locomotion_baseline_regression.py -q</automated>
  </verify>
  <done>Baseline threshold coverage rejects standing-still commanded locomotion and preserves optional real smoke behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Run Phase 6 quick/full gates and update Nyquist validation evidence</name>
  <files>.planning/phases/06-repair-evaluation-runner-semantics/06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/06-repair-evaluation-runner-semantics/06-VALIDATION.md
    - .planning/phases/06-repair-evaluation-runner-semantics/06-AI-SPEC.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - tests/locomotion/test_locomotion_evaluation_runner.py
    - tests/locomotion/test_locomotion_evaluation_exports.py
    - tests/locomotion/test_locomotion_baseline_regression.py
  </read_first>
  <action>Run the exact Phase 6 quick gate: `python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py tests/locomotion/test_locomotion_evaluation_exports.py tests/locomotion/test_locomotion_baseline_regression.py -q`. Then run the full wave gate from `06-VALIDATION.md`: `python -m pytest tests/locomotion tests/bridge/test_sim_bridge.py tests/bridge/test_multi_bridge.py -q`. If the full wave gate cannot run because the current interpreter lacks project-supported dependencies, record the exact pytest skip/error output in the summary and keep `06-VALIDATION.md` pending for the blocked rows; do not mark Nyquist complete on a failed command. If both gates pass or skip only under existing real-MuJoCo dependency skip conditions, update `06-VALIDATION.md` frontmatter to `nyquist_compliant: true` and `wave_0_complete: true`, change rows 06-01-01, 06-01-02, 06-02-01, 06-03-01, and 06-03-02 from pending to green, and check the validation sign-off items that are satisfied by the automated commands. Do not edit requirement status in `REQUIREMENTS.md` in this plan; verification/complete-phase owns requirement checkoff.</action>
  <acceptance_criteria>
    - `grep -n "nyquist_compliant: true" .planning/phases/06-repair-evaluation-runner-semantics/06-VALIDATION.md` succeeds only after the quick/full gates are green or dependency skips are documented by existing skip conditions.
    - `grep -n "wave_0_complete: true" .planning/phases/06-repair-evaluation-runner-semantics/06-VALIDATION.md` succeeds only after Wave 0 tests exist and pass.
    - `grep -n "06-03-02" .planning/phases/06-repair-evaluation-runner-semantics/06-VALIDATION.md` shows the final regression row is no longer pending when the quick gate passes.
    - The command `python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py tests/locomotion/test_locomotion_evaluation_exports.py tests/locomotion/test_locomotion_baseline_regression.py -q` exits 0.
    - `grep -R "claude_agent_sdk\|anthropic\|ClaudeSDKClient\|query(" src pyproject.toml tests 2>/dev/null` finds no runtime AI SDK dependency introduced by Phase 6.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py tests/locomotion/test_locomotion_evaluation_exports.py tests/locomotion/test_locomotion_baseline_regression.py -q</automated>
    <automated>python -m pytest tests/locomotion tests/bridge/test_sim_bridge.py tests/bridge/test_multi_bridge.py -q</automated>
  </verify>
  <done>Phase 6 deterministic gates prove command scheduling, distance export, and baseline threshold repairs together; validation metadata reflects only green automated evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluation artifacts -> baseline threshold helper | CSV/result-row dictionaries become pass/fail baseline evidence. |
| optional simulator dependency -> CI gate | Real MuJoCo smoke may skip under documented unsupported Python/dependency conditions; fake deterministic tests must still prove semantics. |
| validation metadata -> milestone audit | `06-VALIDATION.md` frontmatter and rows may be consumed as proof; it must not overclaim failed or unrun gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Repudiation | `assert_analytical_flat_ground_thresholds` evidence | mitigate | Add a negative test proving nonzero commanded velocity with zero distance raises `AssertionError`; keep row fields explicit. |
| T-06-12 | Tampering | validation metadata | mitigate | Update `nyquist_compliant` and row statuses only after automated commands exit 0 or existing dependency skips are documented; do not mark green on failures. |
| T-06-13 | Denial of Service | baseline/regression tests | mitigate | Use the targeted quick gate for per-task verification; keep real MuJoCo smoke optional through existing skip conditions. |
| T-06-14 | Information Disclosure / Supply Chain | developer automation | mitigate | Use deterministic pytest commands only; no runtime Claude SDK, network, Phoenix, Promptfoo, or LLM judges. |
| T-06-15 | Tampering | action-mode scope | transfer | Do not repair `joint_position`/`residual_baseline` runner actions here; Phase 7 owns support/rejection semantics. |
</threat_model>

<verification>
Run the baseline gate:

```bash
python -m pytest tests/locomotion/test_locomotion_baseline_regression.py -q
```

Run the Phase 6 quick gate:

```bash
python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py tests/locomotion/test_locomotion_evaluation_exports.py tests/locomotion/test_locomotion_baseline_regression.py -q
```

Run the full wave gate from `06-VALIDATION.md`:

```bash
python -m pytest tests/locomotion tests/bridge/test_sim_bridge.py tests/bridge/test_multi_bridge.py -q
```
</verification>

<success_criteria>
- Stationary/no-op commanded-locomotion fixture fails the analytical baseline threshold helper.
- Quick gate covers active command transition, stability distance export, and baseline threshold rejection.
- `06-VALIDATION.md` is updated only after deterministic automated evidence is green.
- Phase 6 leaves Phase 7 action-mode contract scope untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repair-evaluation-runner-semantics/06-03-SUMMARY.md`.
</output>
