---
phase: 06-repair-evaluation-runner-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/locomotion/evaluation.py
  - tests/locomotion/test_locomotion_evaluation_exports.py
autonomous: true
requirements:
  - LOC-METRICS-05
  - LOC-EVAL-02
must_haves:
  truths:
    - "Real evaluation artifacts preserve stability.distance_xy_m instead of flattening distance from command_tracking."
    - "episodes.csv, summary.json, regenerated comparison output, and result.episode_rows report the same nonzero distance value."
    - "CSV formula safety and output path containment remain intact while export semantics change."
  artifacts:
    - path: "src/locomotion/evaluation.py"
      provides: "episode CSV flattening from stability distance source and existing aggregate comparison inputs"
      contains: "distance_xy_m"
    - path: "tests/locomotion/test_locomotion_evaluation_exports.py"
      provides: "artifact regression proving stability-sourced distance survives all export layers"
      contains: "test_episode_export_reads_distance_xy_m_from_stability_summary"
  key_links:
    - from: "src/locomotion/metrics.py"
      to: "src/locomotion/evaluation.py"
      via: "summary['stability']['distance_xy_m']"
      pattern: "stability.*distance_xy_m"
    - from: "src/locomotion/evaluation.py"
      to: "summary.json and comparison.md"
      via: "episode_rows aggregation"
      pattern: "distance_xy_m_mean"
---

<objective>
Repair distance export flattening so aggregate artifacts measure actual locomotion distance.

Purpose: Close the audit gap where real `distance_xy_m` lived under `stability` but evaluation exported zero from `command_tracking`, corrupting LOC-METRICS-05 and LOC-EVAL-02 outputs.
Output: Export tests and evaluator mapping that preserve nonzero `stability.distance_xy_m` through CSV, result rows, summary JSON, and comparison Markdown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v4.0-MILESTONE-AUDIT.md
@.planning/phases/06-repair-evaluation-runner-semantics/06-AI-SPEC.md
@.planning/phases/06-repair-evaluation-runner-semantics/06-RESEARCH.md
@.planning/phases/06-repair-evaluation-runner-semantics/06-VALIDATION.md
@.planning/phases/06-repair-evaluation-runner-semantics/06-PATTERNS.md
@src/locomotion/evaluation.py
@src/locomotion/metrics.py
@tests/locomotion/test_locomotion_evaluation_exports.py

<interfaces>
Production summary source of truth from `src/locomotion/metrics.py`:
```python
LocomotionEpisodeSummary(
    command_tracking={...},
    stability={"distance_xy_m": float(...), ...},
    action_quality={...},
    contact_terrain={...},
    success=bool,
    failure_reason=str | None,
    step_count=int,
)
```
Evaluator export target in `_episode_csv_row()`:
```python
"tracking_rmse": _metric_value(command_tracking, "tracking_error_rmse", default=0.0),
"distance_xy_m": _metric_alias(stability, "distance_xy_m", default=0.0),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 stability-distance artifact regression test</name>
  <files>tests/locomotion/test_locomotion_evaluation_exports.py</files>
  <read_first>
    - .planning/phases/06-repair-evaluation-runner-semantics/06-VALIDATION.md
    - .planning/phases/06-repair-evaluation-runner-semantics/06-PATTERNS.md
    - .planning/v4.0-MILESTONE-AUDIT.md
    - tests/locomotion/test_locomotion_evaluation_exports.py
    - src/locomotion/evaluation.py
    - src/locomotion/metrics.py
  </read_first>
  <behavior>
    - Add or tighten `test_episode_export_reads_distance_xy_m_from_stability_summary` in `tests/locomotion/test_locomotion_evaluation_exports.py`.
    - The fake env summary must set `command_tracking` to either omit `distance_xy_m` or set `"distance_xy_m": 0.0`, and set `stability["distance_xy_m"] = 0.42`.
    - Assert `result.episode_rows[0]["distance_xy_m"] == 0.42`.
    - Read `episodes.csv` and assert `float(row["distance_xy_m"]) == 0.42`.
    - Read `summary.json` and assert an aggregate field such as `overall_by_controller[0]["distance_xy_m_mean"] == 0.42`.
    - Assert `comparison.md` contains `0.420` or the repository's exact formatted value for the 0.42 distance mean.
  </behavior>
  <action>Create the Wave 0 export test from `06-VALIDATION.md` row 06-02-01. Preserve existing artifact tests for `_ARTIFACT_NAMES`, formula-safe CSV strings, saved-run regeneration, and metadata; do not delete or weaken LOC-EVAL-03 metadata assertions even though Phase 7 owns action-mode metadata gap closure.</action>
  <acceptance_criteria>
    - `grep -R "test_episode_export_reads_distance_xy_m_from_stability_summary" tests/locomotion/test_locomotion_evaluation_exports.py` returns exactly one test definition.
    - `grep -R "\"distance_xy_m\": 0.42" tests/locomotion/test_locomotion_evaluation_exports.py` finds the stability distance fixture.
    - `grep -R "command_tracking" tests/locomotion/test_locomotion_evaluation_exports.py` shows the test sets command-tracking distance absent or zero.
    - `grep -R "comparison.md" tests/locomotion/test_locomotion_evaluation_exports.py` shows the test validates Markdown comparison output, not only CSV.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/locomotion/test_locomotion_evaluation_exports.py -q</automated>
  </verify>
  <done>Artifact regression exists and fails until evaluator flattening reads `stability.distance_xy_m`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flatten distance from stability and preserve export hardening</name>
  <files>src/locomotion/evaluation.py, tests/locomotion/test_locomotion_evaluation_exports.py</files>
  <read_first>
    - .planning/phases/06-repair-evaluation-runner-semantics/06-AI-SPEC.md
    - .planning/phases/06-repair-evaluation-runner-semantics/06-RESEARCH.md
    - .planning/phases/06-repair-evaluation-runner-semantics/06-PATTERNS.md
    - src/locomotion/evaluation.py
    - src/locomotion/metrics.py
    - tests/locomotion/test_locomotion_evaluation_exports.py
  </read_first>
  <action>In `src/locomotion/evaluation.py`, update `_episode_csv_row()` so `"distance_xy_m"` is sourced from the `stability` dict, not `command_tracking`. Use the existing metric helper style: `"distance_xy_m": _metric_alias(stability, "distance_xy_m", default=0.0)` unless the current helper signature requires an explicit alias list. Keep `"tracking_rmse"` sourced from `command_tracking["tracking_error_rmse"]`. Do not move distance into `src/locomotion/metrics.py` command-tracking output; `metrics.py` remains the source of truth with `stability.distance_xy_m`. Preserve `_prepare_run_dir()` containment logic and `writer.writerow({field: _csv_safe(row.get(field, "")) for field in _EPISODE_CSV_FIELDS})` unchanged.</action>
  <acceptance_criteria>
    - `grep -n "\"distance_xy_m\"" src/locomotion/evaluation.py` shows the `_episode_csv_row()` mapping references `stability`, not `command_tracking`.
    - `grep -n "writerow({field: _csv_safe" src/locomotion/evaluation.py` still finds the CSV formula-safety write path.
    - `grep -n "Run directory escapes output root" src/locomotion/evaluation.py` still finds the path-containment guard.
    - `python -m pytest tests/locomotion/test_locomotion_evaluation_exports.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/locomotion/test_locomotion_evaluation_exports.py -q</automated>
  </verify>
  <done>`distance_xy_m` survives result rows, CSV, summary, comparison, and regeneration from the stability metric family while path and CSV hardening remain present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env metrics summary -> artifact flattener | Nested metrics dictionaries cross from simulation/collector code into CSV/JSON/Markdown writer code. |
| local filesystem output_root -> generated artifacts | CLI-provided output path controls where manifest, JSONL, CSV, summary, and comparison files are written. |
| CSV artifact -> spreadsheet consumer | Exported string cells may be opened by spreadsheet software outside the project. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | `src/locomotion/evaluation.py::_episode_csv_row` | mitigate | Flatten `distance_xy_m` only from `stability`, the production metrics family; add regression where command_tracking distance is zero but stability distance is 0.42. |
| T-06-07 | Tampering | `_prepare_run_dir()` | mitigate | Preserve resolved-root containment check and test coverage; do not introduce string-concatenated artifact paths. |
| T-06-08 | Information Disclosure / Tampering | `episodes.csv` writer | mitigate | Preserve `_csv_safe()` writer path for all episode CSV fields; do not bypass `writer.writerow({field: _csv_safe(...)})`. |
| T-06-09 | Denial of Service | comparison regeneration from artifacts | mitigate | Keep existing artifact schema and row parsing; do not add pandas or unbounded report generation. |
| T-06-10 | Supply Chain | runtime dependencies | mitigate | Do not add pandas, Claude SDK, Phoenix, Promptfoo, or network eval tooling for this deterministic export repair. |
</threat_model>

<verification>
Run the Phase 6 export gate:

```bash
python -m pytest tests/locomotion/test_locomotion_evaluation_exports.py -q
```

Then run the shared quick gate from `06-VALIDATION.md`:

```bash
python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py tests/locomotion/test_locomotion_evaluation_exports.py tests/locomotion/test_locomotion_baseline_regression.py -q
```
</verification>

<success_criteria>
- `distance_xy_m` flattening uses `summary["stability"]`.
- `command_tracking.distance_xy_m` absent or zero does not zero out artifact distance when `stability.distance_xy_m` is nonzero.
- Aggregate summary and comparison Markdown report nonzero distance means from corrected episode rows.
- CSV formula safety and output path containment remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repair-evaluation-runner-semantics/06-02-SUMMARY.md`.
</output>
