---
phase: 06-repair-evaluation-runner-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locomotion/env.py
  - src/locomotion/evaluation.py
  - tests/locomotion/test_argus_go2_env_contract.py
  - tests/locomotion/test_locomotion_evaluation_runner.py
autonomous: true
requirements:
  - LOC-EVAL-01
  - LOC-EVAL-04
must_haves:
  truths:
    - "Evaluation actions use the command active at the current simulation time, not command_schedule[0]."
    - "ArgusGo2Env exposes current_command at reset and every step for evaluator consumption."
    - "Exported step command fields match the same active command vector used for env.step(action)."
  artifacts:
    - path: "src/locomotion/env.py"
      provides: "current_command info payload from env-owned schedule lookup"
      contains: "current_command"
    - path: "src/locomotion/evaluation.py"
      provides: "active-command-first evaluator action generation and command export fields"
      contains: "def _action_from_command_context"
    - path: "tests/locomotion/test_argus_go2_env_contract.py"
      provides: "env contract regression for reset/post-transition current_command"
      contains: "current_command"
    - path: "tests/locomotion/test_locomotion_evaluation_runner.py"
      provides: "fake-env regression for zero-to-nonzero schedule transition driving actions"
      contains: "test_runner_uses_active_current_command_after_schedule_transition"
  key_links:
    - from: "src/locomotion/env.py"
      to: "src/locomotion/evaluation.py"
      via: "info['current_command']"
      pattern: "current_command"
    - from: "tests/locomotion/test_locomotion_evaluation_runner.py"
      to: "src/locomotion/evaluation.py"
      via: "run_evaluation_matrix(... env_factory=...) captures env.step(action)"
      pattern: "\(\"step\", \[0\.4, 0\.0, 0\.0\]\)"
---

<objective>
Repair active command semantics for the evaluation runner.

Purpose: Close the audit gap where real evaluation used the first scenario command forever, turning scheduled locomotion scenarios into standing-still episodes.
Output: Env info exposes `current_command`; evaluator action generation and command fields prefer that payload; Wave 0 tests prove zero-at-reset then nonzero-after-transition behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v4.0-MILESTONE-AUDIT.md
@.planning/phases/06-repair-evaluation-runner-semantics/06-AI-SPEC.md
@.planning/phases/06-repair-evaluation-runner-semantics/06-RESEARCH.md
@.planning/phases/06-repair-evaluation-runner-semantics/06-VALIDATION.md
@.planning/phases/06-repair-evaluation-runner-semantics/06-PATTERNS.md
@src/locomotion/env.py
@src/locomotion/evaluation.py
@tests/locomotion/test_argus_go2_env_contract.py
@tests/locomotion/test_locomotion_evaluation_runner.py

<interfaces>
From `06-PATTERNS.md` and source analogs:
```python
def _command_at_time(self, sim_time: float) -> np.ndarray: ...
def _current_sim_time(self) -> float: ...
def _action_from_command_context(info: dict[str, Any]) -> tuple[list[float], str, dict[str, Any]]: ...
def _command_fields(info: dict[str, Any], reset_info: dict[str, Any], *, default_action: list[float], default_source: str, default_context: dict[str, Any]) -> tuple[list[float], str, dict[str, Any]]: ...
```
`current_command` target schema for this plan:
```python
{
    "time": float(sim_time),
    "vx": float(active_command[0]),
    "vy": float(active_command[1]),
    "omega": float(active_command[2]),
    "source": "scenario_schedule",
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 active-command regression tests</name>
  <files>tests/locomotion/test_argus_go2_env_contract.py, tests/locomotion/test_locomotion_evaluation_runner.py</files>
  <read_first>
    - .planning/phases/06-repair-evaluation-runner-semantics/06-VALIDATION.md
    - .planning/phases/06-repair-evaluation-runner-semantics/06-PATTERNS.md
    - .planning/v4.0-MILESTONE-AUDIT.md
    - tests/locomotion/test_argus_go2_env_contract.py
    - tests/locomotion/test_locomotion_evaluation_runner.py
    - src/locomotion/env.py
    - src/locomotion/evaluation.py
  </read_first>
  <behavior>
    - Add `test_argus_go2_env_info_exposes_current_command_from_schedule_transition` or an equivalently named test in `tests/locomotion/test_argus_go2_env_contract.py` that constructs `ArgusGo2Env`, injects a `ScenarioSample` with command schedule `({"time": 0.0, "vx": 0.0, "vy": 0.0, "omega": 0.0}, {"time": 0.25, "vx": 0.4, "vy": -0.1, "omega": 0.2})`, sets step/sim time past the transition without requiring real MuJoCo, calls `_info()`, and asserts `info["current_command"]` has `vx == 0.4`, `vy == -0.1`, `omega == 0.2`, `source == "scenario_schedule"`.
    - Add `test_runner_uses_active_current_command_after_schedule_transition` in `tests/locomotion/test_locomotion_evaluation_runner.py` using a fake env whose reset info has `command_schedule` first entry zero and whose first step info has `current_command` equal to `{"vx": 0.4, "vy": 0.0, "omega": 0.0, "source": "scenario_schedule"}`. The fake env must force at least two `env.step()` calls so the second action proves the latest active command drives evaluation.
    - Assert captured fake-env calls include `("step", [0.0, 0.0, 0.0])` for the reset-time command and `("step", [0.4, 0.0, 0.0])` after the current-command transition. Assert the emitted step row for the post-transition sample has `commanded_velocity == [0.4, 0.0, 0.0]`, `command_source == "scenario_schedule"`, and `command_context["current_command"]["vx"] == 0.4`.
  </behavior>
  <action>Create the Wave 0 tests from `06-VALIDATION.md` rows 06-01-01 and 06-01-02 before changing production code. Keep tests deterministic and fake-data based; do not require MuJoCo/Gymnasium for the runner test. If env contract imports require Gymnasium, follow the existing skip/fake-data pattern in `test_argus_go2_env_contract.py` rather than broadening dependency assumptions.</action>
  <acceptance_criteria>
    - `grep -R "test_runner_uses_active_current_command_after_schedule_transition" tests/locomotion/test_locomotion_evaluation_runner.py` returns exactly one test definition.
    - `grep -R "current_command" tests/locomotion/test_argus_go2_env_contract.py` shows assertions for `vx`, `vy`, `omega`, and `source`.
    - `grep -R "\[0.4, 0.0, 0.0\]" tests/locomotion/test_locomotion_evaluation_runner.py` finds the nonzero active command assertion.
    - Running the tests before Task 2 should fail because production code has not yet emitted/preferred `current_command` correctly.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py -q</automated>
    <automated>python -m pytest tests/locomotion/test_argus_go2_env_contract.py -k current_command -q</automated>
  </verify>
  <done>Wave 0 tests exist and fail against stale command semantics, with no runtime Claude/LLM/network dependency added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit and consume current_command as the active runtime command</name>
  <files>src/locomotion/env.py, src/locomotion/evaluation.py, tests/locomotion/test_argus_go2_env_contract.py, tests/locomotion/test_locomotion_evaluation_runner.py</files>
  <read_first>
    - .planning/phases/06-repair-evaluation-runner-semantics/06-AI-SPEC.md
    - .planning/phases/06-repair-evaluation-runner-semantics/06-RESEARCH.md
    - .planning/phases/06-repair-evaluation-runner-semantics/06-PATTERNS.md
    - src/locomotion/env.py
    - src/locomotion/evaluation.py
    - tests/locomotion/test_argus_go2_env_contract.py
    - tests/locomotion/test_locomotion_evaluation_runner.py
  </read_first>
  <action>In `src/locomotion/env.py`, update `_info()` to compute `sim_time = self._current_sim_time()`, `active_command = self._command_at_time(sim_time)`, and include `"current_command": {"time": float(sim_time), "vx": float(active_command[0]), "vy": float(active_command[1]), "omega": float(active_command[2]), "source": "scenario_schedule"}` in the returned info payload. Preserve existing `command_schedule` metadata and do not duplicate schedule iteration outside `_command_at_time()`. In `src/locomotion/evaluation.py`, change `_action_from_command_context(info)` to check `current_command = info.get("current_command")` first; when it is a dict, return `_command_vector(current_command)`, source `str(current_command.get("source", "current_command"))`, and context containing both `"current_command": dict(current_command)` and `"command_schedule": _serializable_sequence(info.get("command_schedule", ()))`. Keep schedule fallback only for envs that do not expose `current_command`. Ensure `_command_fields()` continues to prefer `current_command` and preserves schedule metadata from `info` or `reset_info`. Do not change non-default action-mode behavior in this phase; Phase 7 owns that contract.</action>
  <acceptance_criteria>
    - `grep -n "\"current_command\"" src/locomotion/env.py` shows `_info()` emits the payload.
    - `grep -n "current_command = info.get(\"current_command\")" src/locomotion/evaluation.py` appears before any `command_schedule` fallback inside `_action_from_command_context`.
    - `grep -R "claude_agent_sdk\|anthropic\|ClaudeSDKClient\|query(" src pyproject.toml tests 2>/dev/null` finds no new runtime AI SDK usage introduced by this plan.
    - `python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py -q` passes.
    - `python -m pytest tests/locomotion/test_argus_go2_env_contract.py -k current_command -q` passes or skips only under the file's existing MuJoCo/Python dependency skip conditions.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py -q</automated>
    <automated>python -m pytest tests/locomotion/test_argus_go2_env_contract.py -k current_command -q</automated>
  </verify>
  <done>Real and fake evaluation semantics use env-provided active commands; post-transition commanded actions and exported command fields match the same nonzero vector.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scenario/env info -> evaluator action | Info dictionaries may be produced by real or fake envs; evaluator must validate/coerce command vectors before stepping. |
| CLI/test matrix -> env construction | Matrix values must remain validated before side effects or resource allocation. |
| developer planning AI -> runtime package | AI-SPEC permits Claude SDK only for developer assistance; runtime robotics package must stay deterministic and offline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `src/locomotion/evaluation.py::_action_from_command_context` | mitigate | Use existing `_command_vector()` finite 3-vector coercion for `current_command`; preserve schedule fallback only after current-command lookup. |
| T-06-02 | Denial of Service | evaluation matrix execution | mitigate | Do not alter `validate_evaluation_matrix(... max_matrix_runs=...)`; runner tests must continue through existing validation path. |
| T-06-03 | Repudiation | step command artifacts | mitigate | Preserve `command_context` with both `current_command` and `command_schedule` so artifacts prove which command source drove each step. |
| T-06-04 | Information Disclosure / Supply Chain | runtime dependency creep | mitigate | Do not import `claude_agent_sdk`, `anthropic`, or network/model clients in `src/` or tests; acceptance is pytest output only. |
| T-06-05 | Tampering | unsupported action-mode semantics | transfer | Phase 7 owns non-default action-mode support/rejection; this plan must not broaden joint/residual action generation. |
</threat_model>

<verification>
Run the Phase 6 active-command gate:

```bash
python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py -q
python -m pytest tests/locomotion/test_argus_go2_env_contract.py -k current_command -q
```

Then run the shared quick gate from `06-VALIDATION.md`:

```bash
python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py tests/locomotion/test_locomotion_evaluation_exports.py tests/locomotion/test_locomotion_baseline_regression.py -q
```
</verification>

<success_criteria>
- Reset and step info include `current_command` with `vx`, `vy`, `omega`, `time`, and `source`.
- Evaluator action generation prefers `current_command` before static `command_schedule` fallback.
- Fake-env transition test proves a post-transition nonzero command drives `env.step(action)`.
- No Claude/LLM/network runtime dependency is added.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repair-evaluation-runner-semantics/06-01-SUMMARY.md`.
</output>
