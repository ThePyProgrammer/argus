---
phase: 05-harness-docs-and-comparison-matrix
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/locomotion/controllers.py
  - tests/locomotion/test_locomotion_controller_registry.py
  - tests/test_locomotion_benchmark_docs.py
autonomous: true
gap_closure: true
requirements:
  - LOC-REPORT-02
must_haves:
  truths:
    - "The residual RL controller-family boundary uses one public action-mode vocabulary across implementation metadata, documentation, and documentation guards."
    - "`residual_policy` remains a registered unavailable placeholder, but its advertised seam is `residual_baseline`, which is one of the environment action modes."
    - "Documentation tests fail if the residual-policy guide row drifts away from `ControllerRegistry` metadata again."
  artifacts:
    - path: "src/locomotion/controllers.py"
      provides: "Residual placeholder capability metadata aligned to the public residual-over-baseline action-mode seam"
      contains: "_placeholder_capabilities(\"residual_policy\", \"residual_baseline\")"
    - path: "tests/locomotion/test_locomotion_controller_registry.py"
      provides: "Registry-level regression check for residual placeholder action-mode vocabulary"
      contains: "residual_baseline"
    - path: "tests/test_locomotion_benchmark_docs.py"
      provides: "Doc/metadata cross-check that catches residual-policy seam drift"
      contains: "ControllerRegistry"
  key_links:
    - from: "src/locomotion/controllers.py"
      to: "src/locomotion/actions.py"
      via: "residual_policy capability action_mode uses available action-mode vocabulary"
      pattern: "residual_baseline"
    - from: "tests/locomotion/test_locomotion_controller_registry.py"
      to: "src/locomotion/controllers.py"
      via: "ControllerRegistry.list_controllers residual_policy metadata assertion"
      pattern: "residual_policy.*residual_baseline|residual_baseline.*residual_policy"
    - from: "tests/test_locomotion_benchmark_docs.py"
      to: "docs/locomotion-benchmark.md"
      via: "guide text assertion for residual_policy row"
      pattern: "residual_policy"
---

<objective>
Close the Phase 5 verification gap by aligning the residual RL placeholder seam across controller metadata, public action-mode vocabulary, and documentation guards.

Purpose: The Phase 5 matrix is supposed to make supported/deferred controller-family boundaries explicit. A placeholder whose metadata says `residual_joint_position` while the public environment vocabulary says `residual_baseline` is not an explicit boundary; it is contradictory documentation bait.
Output: A targeted implementation metadata correction plus regression tests that pin the residual-policy seam to `residual_baseline` without making the unavailable placeholder runnable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-harness-docs-and-comparison-matrix/05-CONTEXT.md
@.planning/phases/05-harness-docs-and-comparison-matrix/05-VERIFICATION.md
@.planning/phases/05-harness-docs-and-comparison-matrix/05-01-SUMMARY.md
@docs/locomotion-benchmark.md
@src/locomotion/controllers.py
@src/locomotion/actions.py
@tests/locomotion/test_locomotion_controller_registry.py
@tests/test_locomotion_benchmark_docs.py

<interfaces>
Concrete gap facts from `05-VERIFICATION.md`:

- Failed truth: "The controller-family matrix includes concrete controller ids/placeholders and seams that match the actual codebase."
- Mismatch: `docs/locomotion-benchmark.md` documents `residual_policy` as using `residual_baseline`, but `ResidualPolicyController.CAPABILITIES` currently advertises `residual_joint_position`.
- Public environment action modes from `src/locomotion/actions.py`: `velocity_command`, `joint_position`, `residual_baseline`.
- Required target state for this gap plan: `ResidualPolicyController.CAPABILITIES = _placeholder_capabilities("residual_policy", "residual_baseline")`.
- The residual placeholder must remain unavailable: `ControllerRegistry.create("residual_policy")` still raises `UnavailableControllerError` and `ControllerRegistry.list_controllers()` still reports `available is False` for `residual_policy`.
</interfaces>

<decision_traceability>
- D-06: Preserve hard-boundary language; residual RL remains `registered placeholder unavailable`, not runnable.
- D-08: Make the concrete controller id/placeholders and current action modes/seams accurate by using `residual_baseline` consistently for `residual_policy`.
- D-10/D-11/D-12: Extend lightweight pytest guards to catch the exact documentation/metadata mismatch without executing the smoke benchmark or MuJoCo.
</decision_traceability>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align residual placeholder metadata to residual_baseline</name>
  <files>src/locomotion/controllers.py, tests/locomotion/test_locomotion_controller_registry.py</files>
  <read_first>
    - src/locomotion/controllers.py
    - src/locomotion/actions.py
    - tests/locomotion/test_locomotion_controller_registry.py
    - .planning/phases/05-harness-docs-and-comparison-matrix/05-VERIFICATION.md
    - .planning/phases/05-harness-docs-and-comparison-matrix/05-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `ControllerRegistry.list_controllers()` returns the `residual_policy` entry with `entry["capabilities"]["action_mode"] == "residual_baseline"`.
    - Test 2: `"residual_baseline" in available_action_modes()` proves the residual placeholder advertises a public environment action mode, not the stale `residual_joint_position` string.
    - Test 3: `residual_policy` remains unavailable and still raises `UnavailableControllerError` through existing placeholder tests.
  </behavior>
  <action>Update `tests/locomotion/test_locomotion_controller_registry.py` first. Add a focused test named `test_residual_placeholder_advertises_public_residual_baseline_action_mode`. In that test, import `available_action_modes` from `src.locomotion.actions` and `ControllerRegistry` from `src.locomotion.controllers`; build `entries = {entry["name"]: entry for entry in ControllerRegistry.list_controllers()}`; set `action_mode = entries["residual_policy"]["capabilities"]["action_mode"]`; assert `action_mode == "residual_baseline"`; assert `action_mode in available_action_modes()`; assert `entries["residual_policy"]["available"] is False`. Then update `src/locomotion/controllers.py` so `ResidualPolicyController` uses exactly `_placeholder_capabilities("residual_policy", "residual_baseline")` instead of `_placeholder_capabilities("residual_policy", "residual_joint_position")`. Do not change unavailable reasons, controller ids, registry names, analytical controller behavior, direct policy metadata, MPC metadata, WBC metadata, or action-mode definitions in `src/locomotion/actions.py`.</action>
  <verify>
    <automated>/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/locomotion/test_locomotion_controller_registry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/locomotion/controllers.py` contains `_placeholder_capabilities("residual_policy", "residual_baseline")`.
    - `src/locomotion/controllers.py` does not contain `_placeholder_capabilities("residual_policy", "residual_joint_position")`.
    - `tests/locomotion/test_locomotion_controller_registry.py` contains `test_residual_placeholder_advertises_public_residual_baseline_action_mode`.
    - `tests/locomotion/test_locomotion_controller_registry.py` contains `available_action_modes`.
    - `tests/locomotion/test_locomotion_controller_registry.py` contains `action_mode == "residual_baseline"`.
    - `/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/locomotion/test_locomotion_controller_registry.py -q` exits 0.
  </acceptance_criteria>
  <done>The residual placeholder capability metadata advertises the same public `residual_baseline` seam that the environment and Phase 5 guide document, while remaining unavailable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add doc guard cross-check for residual_policy seam</name>
  <files>tests/test_locomotion_benchmark_docs.py</files>
  <read_first>
    - tests/test_locomotion_benchmark_docs.py
    - docs/locomotion-benchmark.md
    - src/locomotion/controllers.py
    - src/locomotion/actions.py
    - .planning/phases/05-harness-docs-and-comparison-matrix/05-VERIFICATION.md
    - .planning/phases/05-harness-docs-and-comparison-matrix/05-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: The doc guard imports `ControllerRegistry` and `available_action_modes` and reads real residual placeholder metadata.
    - Test 2: The guard asserts the residual metadata action mode is exactly `residual_baseline` and is listed by `available_action_modes()`.
    - Test 3: The guard asserts `docs/locomotion-benchmark.md` contains the exact residual row seam phrase `Controller id `residual_policy`; residual-over-baseline seam via `residual_baseline` action mode`.
    - Test 4: The existing content-only source guard still passes and no subprocess, `uv`, benchmark command, MuJoCo, or `ArgusGo2Env` execution is added.
  </behavior>
  <action>Extend `tests/test_locomotion_benchmark_docs.py` with one new focused test named `test_residual_policy_documentation_matches_registry_action_mode`. In the new test, import `available_action_modes` from `src.locomotion.actions` and `ControllerRegistry` from `src.locomotion.controllers` inside the test function. Read the guide with `GUIDE.read_text(encoding="utf-8")`. Build `entries = {entry["name"]: entry for entry in ControllerRegistry.list_controllers()}`. Set `residual = entries["residual_policy"]` and `action_mode = residual["capabilities"]["action_mode"]`. Assert `residual["available"] is False`, assert `action_mode == "residual_baseline"`, assert `action_mode in available_action_modes()`, and assert `f"Controller id `residual_policy`; residual-over-baseline seam via `{action_mode}` action mode" in guide`. Keep this as a metadata/string consistency test only. Do not import `subprocess`, do not call `uv`, do not construct `ArgusGo2Env`, do not run `eval-locomotion`, and do not edit `docs/locomotion-benchmark.md` unless Task 1 reveals the existing guide phrase is missing. If the guide phrase is missing, add exactly `Controller id `residual_policy`; residual-over-baseline seam via `residual_baseline` action mode` to the residual RL matrix row and include `docs/locomotion-benchmark.md` in the summary.</action>
  <verify>
    <automated>/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/test_locomotion_benchmark_docs.py tests/locomotion/test_locomotion_controller_registry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_locomotion_benchmark_docs.py` contains `test_residual_policy_documentation_matches_registry_action_mode`.
    - `tests/test_locomotion_benchmark_docs.py` contains `ControllerRegistry`.
    - `tests/test_locomotion_benchmark_docs.py` contains `available_action_modes`.
    - `tests/test_locomotion_benchmark_docs.py` contains `action_mode == "residual_baseline"`.
    - `tests/test_locomotion_benchmark_docs.py` contains `residual-over-baseline seam via `{action_mode}` action mode`.
    - `tests/test_locomotion_benchmark_docs.py` does not contain `import subprocess`.
    - `/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/test_locomotion_benchmark_docs.py tests/locomotion/test_locomotion_controller_registry.py -q` exits 0.
  </acceptance_criteria>
  <done>Phase 5 documentation guards now fail on the exact residual-policy metadata/documentation mismatch reported by verification, without executing the benchmark runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation-reader | A developer may rely on the controller-family matrix to choose a controller seam or future implementation path. |
| registry-metadata-to-docs | Controller placeholder metadata crosses from implementation into public documentation claims. |
| pytest-to-runtime | Documentation guards may import lightweight metadata but must not execute MuJoCo, CLI benchmark runs, or environment construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering / Repudiation | `ResidualPolicyController.CAPABILITIES` and `docs/locomotion-benchmark.md` residual RL row | mitigate | Set residual placeholder `action_mode` to the public `residual_baseline` string and add registry/doc tests asserting the same string. |
| T-05-02-02 | Denial of Service | `tests/test_locomotion_benchmark_docs.py` | mitigate | Keep the new guard to metadata imports and Markdown reads only; explicitly prohibit `subprocess`, `uv`, `ArgusGo2Env` construction, and `eval-locomotion` execution. |
| T-05-02-03 | Information Disclosure / Elevation of Privilege | Phase 5 gap closure scope | accept | No secrets, credentials, auth paths, network endpoints, or privileged filesystem writes are introduced; this plan changes a static metadata string and tests. No high-severity ASVS L1 threat applies. |
</threat_model>

<verification>
Run these checks after all tasks:

```bash
/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/test_locomotion_benchmark_docs.py tests/locomotion/test_locomotion_controller_registry.py -q
/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/locomotion tests/bridge/test_sim_bridge.py tests/bridge/test_multi_bridge.py tests/test_main_args.py tests/test_locomotion_benchmark_docs.py -q
```

Static gap closure spot-check:

```bash
grep -F '_placeholder_capabilities("residual_policy", "residual_baseline")' src/locomotion/controllers.py
grep -F 'test_residual_policy_documentation_matches_registry_action_mode' tests/test_locomotion_benchmark_docs.py
grep -F 'test_residual_placeholder_advertises_public_residual_baseline_action_mode' tests/locomotion/test_locomotion_controller_registry.py
```
</verification>

<success_criteria>
- `ControllerRegistry.list_controllers()` reports `residual_policy` capability `action_mode` as `residual_baseline`.
- `residual_policy` remains `available is False` and remains a fail-fast unavailable placeholder.
- `docs/locomotion-benchmark.md` residual RL row documents `residual_baseline`, matching registry metadata and public action-mode vocabulary.
- `tests/test_locomotion_benchmark_docs.py` and `tests/locomotion/test_locomotion_controller_registry.py` contain regression assertions for the residual seam mismatch found in `05-VERIFICATION.md`.
- No documentation guard executes MuJoCo or the benchmark command.
</success_criteria>

<output>
After completion, create `.planning/phases/05-harness-docs-and-comparison-matrix/05-02-SUMMARY.md`.
</output>
