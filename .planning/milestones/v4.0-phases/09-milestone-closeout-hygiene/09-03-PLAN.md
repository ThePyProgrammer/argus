---
phase: 09-milestone-closeout-hygiene
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - .planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - LOC-ENV-01
  - LOC-CTRL-01
  - LOC-METRICS-01
  - LOC-EVAL-01
user_setup: []
must_haves:
  truths:
    - "Phase 09 validation sign-off reflects the actual executed closeout gate and Nyquist compliance."
    - "Project state points at Phase 09 execution/verification readiness instead of stale Phase 8/UI-SPEC positioning."
    - "Roadmap Phase 9 lists the created plans and `gsd-sdk query audit-open` reports no open closeout blockers."
  artifacts:
    - path: ".planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md"
      provides: "Final Phase 09 validation gate and sign-off"
      contains: "nyquist_compliant: true"
    - path: ".planning/STATE.md"
      provides: "Updated project closeout position"
      contains: "Phase 09"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 plan inventory and execution next step"
      contains: "09-03-PLAN.md"
  key_links:
    - from: ".planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md"
      to: "gsd-sdk query audit-open"
      via: "quick run command and final sign-off require clean audit"
      pattern: "gsd-sdk query audit-open"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/09-milestone-closeout-hygiene/09-*-PLAN.md"
      via: "Phase 9 Plans list names all created plan files"
      pattern: "09-01-PLAN.md.*09-02-PLAN.md.*09-03-PLAN.md"
---

<objective>
Finalize Phase 09 governance bookkeeping and run the milestone closeout audit gate.

Purpose: Ensure the phase's own validation file, project state, and roadmap reflect the metadata/artifact cleanup that Plans 01 and 02 performed, and prove `/gsd-complete-milestone` is no longer blocked by open artifacts.
Output: Updated Phase 09 validation sign-off, STATE/ROADMAP bookkeeping, and a clean `gsd-sdk query audit-open` gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-milestone-closeout-hygiene/09-RESEARCH.md
@.planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md
@.planning/phases/09-milestone-closeout-hygiene/09-PATTERNS.md
@.planning/phases/09-milestone-closeout-hygiene/09-01-PLAN.md
@.planning/phases/09-milestone-closeout-hygiene/09-02-PLAN.md

<interfaces>
Final audit acceptance command:
```bash
gsd-sdk query audit-open
```
Expected clean human-readable output includes `All artifact types clear. Safe to proceed.` when no blockers remain. If JSON is returned in this environment, expected clean structured fields are `has_open_items: false` and `counts.total: 0`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare Phase 09 validation sign-off evidence</name>
  <files>.planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md</files>
  <read_first>
    - .planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md
    - .planning/phases/09-milestone-closeout-hygiene/09-01-PLAN.md
    - .planning/phases/09-milestone-closeout-hygiene/09-02-PLAN.md
    - .planning/phases/09-milestone-closeout-hygiene/09-PATTERNS.md
    - /home/prannayag/.claude/get-shit-done/bin/lib/audit.cjs
  </read_first>
  <action>
    Update `.planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md` after Plans 01 and 02 are complete to prepare pending sign-off evidence only. In the Per-Task Verification Map, keep `09-01-01`, `09-02-01`, and `09-03-01` present and keep `gsd-sdk query audit-open` as the automated command for all three, but do not set final green/passed status for `09-03-01` before the final audit gate runs. Preserve the Manual-Only Verifications section, but add one sentence after its table: `Resolved during Plan 02 by manual browser verification or explicit out-of-v4 closeout disposition recorded in .planning/debug/voxel-becomes-pointcloud-closeup.md.` Do not set frontmatter `status: passed`, `nyquist_compliant: true`, or `wave_0_complete: true`; do not check all sign-off boxes; do not set `**Approval:** passed`; those finalization updates happen only in Task 3 after `gsd-sdk query audit-open` returns clean. Do not add frontend work.
  </action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
path = Path('.planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md')
text = path.read_text()
for needle in ['| 09-01-01 |', '| 09-02-01 |', '| 09-03-01 |', 'gsd-sdk query audit-open', 'Resolved during Plan 02']:
    if needle not in text:
        raise SystemExit(f'missing {needle}')
signoff = text.split('## Validation Sign-Off', 1)[1]
for forbidden in ['**Approval:** passed']:
    if forbidden in signoff:
        raise SystemExit(f'premature final sign-off: {forbidden}')
print('phase 09 validation pending sign-off evidence prepared')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md` does not claim final `**Approval:** passed` before Task 3 runs the final audit gate.
    - The Per-Task Verification Map still contains task IDs `09-01-01`, `09-02-01`, and `09-03-01` and each row contains `gsd-sdk query audit-open`.
    - `.planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md` contains exact string `Resolved during Plan 02 by manual browser verification or explicit out-of-v4 closeout disposition recorded in .planning/debug/voxel-becomes-pointcloud-closeup.md.`
    - Final frontmatter lines `status: passed`, `nyquist_compliant: true`, and `wave_0_complete: true`, checked sign-off boxes, and `**Approval:** passed` are deferred to Task 3 after `gsd-sdk query audit-open` returns clean.
  </acceptance_criteria>
  <done>Phase 09 validation contains pending sign-off evidence without prematurely claiming the final audit has passed.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP and STATE closeout bookkeeping</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/09-milestone-closeout-hygiene/09-01-PLAN.md
    - .planning/phases/09-milestone-closeout-hygiene/09-02-PLAN.md
    - .planning/phases/09-milestone-closeout-hygiene/09-03-PLAN.md
  </read_first>
  <action>
    Update `.planning/ROADMAP.md` Phase 9 details to list `**Plans:** 3 plans` and replace the placeholder `Plan with /gsd-plan-phase 9` with this plan list: `09-01-PLAN.md — Reconcile Phase 1/2/3/6 validation and Nyquist metadata with passed verification evidence.`, `09-02-PLAN.md — Close debug-session and quick-task artifacts using scanner-readable statuses and evidence-backed disposition.`, `09-03-PLAN.md — Finalize Phase 09 validation, STATE/ROADMAP bookkeeping, and clean audit-open gate.` Keep Phase 9 unchecked unless execution/verification workflow marks it complete later. Update the Progress table row for Phase 9 from `0/0 | Planned` to `0/3 | Planned` during planning or to `3/3 | Complete` only if executing this plan as part of phase execution after summaries exist; do not claim completion during planning-only runs. Update `.planning/STATE.md` current focus/current position away from stale `Phase 9 UI-SPEC approved`: set `stopped_at: Phase 09 planned` if planning only, or `stopped_at: Phase 09 audit-open gate passed` if executing after the final audit passes. Set `Current focus` to `Phase 09 — milestone-closeout-hygiene` and `Plan:` to `Ready to execute` during planning or `Complete` after execution. Keep pending todo wording that says rerun audit and complete milestone, but update it to say `Run /gsd-complete-milestone v4.0 after Phase 09 verification passes.`
  </action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
roadmap = Path('.planning/ROADMAP.md').read_text()
state = Path('.planning/STATE.md').read_text()
for needle in ['**Plans**: 3 plans', '09-01-PLAN.md', '09-02-PLAN.md', '09-03-PLAN.md']:
    if needle not in roadmap:
        raise SystemExit(f'ROADMAP missing {needle}')
for needle in ['Phase 09', 'milestone-closeout-hygiene']:
    if needle not in state:
        raise SystemExit(f'STATE missing {needle}')
if 'Plan with `/gsd-plan-phase 9`' in roadmap:
    raise SystemExit('ROADMAP still contains Phase 9 planning placeholder')
print('roadmap/state bookkeeping updated')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 9 section contains exact string `**Plans**: 3 plans`.
    - `.planning/ROADMAP.md` contains exact strings `09-01-PLAN.md`, `09-02-PLAN.md`, and `09-03-PLAN.md` under Phase 9.
    - `.planning/ROADMAP.md` no longer contains exact string `Plan with `/gsd-plan-phase 9``.
    - `.planning/STATE.md` contains exact string `Phase 09` and exact string `milestone-closeout-hygiene`.
    - `.planning/STATE.md` no longer contains exact string `Stopped at: Phase 9 UI-SPEC approved`.
  </acceptance_criteria>
  <done>Roadmap and state point to the three Phase 09 plans and the next milestone-closeout action without claiming unverified completion.</done>
</task>

<task type="auto">
  <name>Task 3: Run final open-artifact audit gate</name>
  <files>.planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md</files>
  <read_first>
    - .planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md
    - .planning/debug/point-cloud-below-ground.md
    - .planning/debug/point-cloud-rotation.md
    - .planning/debug/voxel-becomes-pointcloud-closeup.md
    - .planning/quick/260317-hat-update-readme-md-with-proper-project-pla/260317-hat-SUMMARY.md
    - .planning/quick/260324-euj-allow-me-to-hide-the-output-rendering-on/260324-euj-SUMMARY.md
    - .planning/quick/260324-ffy-persist-output-mode-across-page-reloads/260324-ffy-SUMMARY.md
    - .planning/quick/260324-gov-extract-slider-number-field-as-reusable-/260324-gov-SUMMARY.md
    - .planning/quick/260324-hb0-add-colored-scene-glb-with-material-colo/260324-hb0-SUMMARY.md
    - /home/prannayag/.claude/get-shit-done/bin/lib/audit.cjs
  </read_first>
  <action>
    Run `gsd-sdk query audit-open` from the repository root. If output is clean, finalize `.planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md`: set frontmatter `status: passed`, `nyquist_compliant: true`, and `wave_0_complete: true`; set applicable Per-Task Verification Map rows for `09-01-01`, `09-02-01`, and `09-03-01` to `green`; check the six `## Validation Sign-Off` boxes; set `**Approval:** passed`; and append or update a short final evidence line under `## Validation Sign-Off`: `Final audit gate: gsd-sdk query audit-open — All artifact types clear. Safe to proceed.` If output is JSON, use `Final audit gate: gsd-sdk query audit-open — has_open_items=false, counts.total=0.` If any item remains open, do not mark approval passed; list the exact returned slug(s) in the task result and fix only the corresponding Phase 09 artifact if it is one of the files already owned by Plan 01 or Plan 02. Do not edit `audit.cjs` to hide blockers.
  </action>
  <verify>
    <automated>gsd-sdk query audit-open</automated>
  </verify>
  <acceptance_criteria>
    - `gsd-sdk query audit-open` output contains either exact string `All artifact types clear. Safe to proceed.` or structured values `has_open_items` false and `total` 0.
    - `gsd-sdk query audit-open` output does not contain exact strings `point-cloud-below-ground`, `point-cloud-rotation`, `voxel-becomes-pointcloud-closeup`, `260317-hat`, `260324-euj`, `260324-ffy`, `260324-gov`, or `260324-hb0`.
    - `.planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md` frontmatter contains exact lines `status: passed`, `nyquist_compliant: true`, and `wave_0_complete: true`.
    - `.planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md` contains exact string `**Approval:** passed`.
    - The `## Validation Sign-Off` section contains no unchecked `- [ ]` checkbox lines.
    - `.planning/phases/09-milestone-closeout-hygiene/09-VALIDATION.md` contains exact string `Final audit gate: gsd-sdk query audit-open`.
    - No changes are made to `/home/prannayag/.claude/get-shit-done/bin/lib/audit.cjs`.
  </acceptance_criteria>
  <done>The open artifact audit gate is clean and Phase 09 records the final command evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artifact edits -> audit-open result | Final command output determines milestone close readiness. |
| Planner/executor -> STATE/ROADMAP | Governance bookkeeping can misrepresent completion if updated prematurely. |
| Scanner implementation -> closeout gate | Editing scanner logic would bypass actual artifact cleanup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | R | `.planning/phases/09-*/09-VALIDATION.md` | mitigate | Record exact final `gsd-sdk query audit-open` result string in validation sign-off. |
| T-09-12 | T | `.planning/STATE.md`, `.planning/ROADMAP.md` | mitigate | Use non-completion wording during planning; only claim audit passed after the command returns clean. |
| T-09-13 | E | `$HOME/.claude/get-shit-done/bin/lib/audit.cjs` | mitigate | Do not edit scanner code; final acceptance requires scanner output, not changed scanner predicates. |
| T-09-14 | I | Final closeout docs | mitigate | Summarize only clean status/counts, not raw artifact body content. |
| T-09-15 | T | Product code | mitigate | This plan modifies only governance docs; any product-code change is out of scope and must fail review. |

High-severity threats remaining: none if the final audit gate is clean without scanner modification.
</threat_model>

<verification>
Overall phase check:

```bash
gsd-sdk query audit-open
```

Expected clean result: `All artifact types clear. Safe to proceed.` or JSON equivalent with `has_open_items=false` and `counts.total=0`.
</verification>

<success_criteria>
- Phase 09 validation frontmatter and sign-off are passed/true after the audit gate.
- ROADMAP Phase 9 lists all three plans and no longer contains the planning placeholder.
- STATE points to Phase 09 closeout readiness rather than stale UI-SPEC status.
- `gsd-sdk query audit-open` returns no open items blocking milestone closure.
</success_criteria>

<output>
After completion, create `.planning/phases/09-milestone-closeout-hygiene/09-03-SUMMARY.md`.
</output>
