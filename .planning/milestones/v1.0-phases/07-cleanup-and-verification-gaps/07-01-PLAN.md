---
phase: 07-cleanup-and-verification-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridge/test_multi_bridge.py
  - tests/locomotion/test_locomotion.py
  - tests/web/test_streaming_viz.py
  - pyproject.toml
autonomous: true
requirements: [VIZ-01]
gap_closure: true

must_haves:
  truths:
    - "pytest tests/ --collect-only succeeds with zero errors"
    - "pytest tests/ -k 'not integration' passes with zero failures"
    - "All async web tests run correctly with pytest-asyncio"
  artifacts:
    - path: "tests/bridge/test_multi_bridge.py"
      provides: "Correct default assertions matching MultiRobotConfig"
      contains: "sim_steps_per_frame"
    - path: "tests/locomotion/test_locomotion.py"
      provides: "Correct path to Go2 XML model"
      contains: "models/unitree_go2/go2.xml"
    - path: "tests/web/test_streaming_viz.py"
      provides: "Fixed async tests and true_rgb color mode test"
      contains: "pytest.mark.asyncio"
  key_links:
    - from: "tests/locomotion/test_locomotion.py"
      to: "models/unitree_go2/go2.xml"
      via: "GO2_XML path constant"
      pattern: "models.*unitree_go2.*go2\\.xml"
    - from: "tests/bridge/test_multi_bridge.py"
      to: "src/bridge/multi_robot_config.py"
      via: "default config assertions"
      pattern: "sim_steps_per_frame"
---

<objective>
Fix all stale and broken tests so `pytest tests/` runs clean.

Purpose: The v1.0 audit identified multiple test failures from stale assertions, missing model paths, missing pytest-asyncio dependency, and a test-vs-implementation mismatch. This plan fixes all test issues so the full suite passes.

Output: All non-integration tests pass with zero failures.
</objective>

<execution_context>
@/home/prannayag/.claude/get-shit-done/workflows/execute-plan.md
@/home/prannayag/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale test assertions and missing model paths</name>
  <files>tests/bridge/test_multi_bridge.py, tests/locomotion/test_locomotion.py, tests/web/test_streaming_viz.py</files>
  <read_first>
    - tests/bridge/test_multi_bridge.py
    - tests/locomotion/test_locomotion.py
    - tests/web/test_streaming_viz.py
    - src/bridge/multi_robot_config.py
    - backend/web/streaming_viz.py
  </read_first>
  <action>
Fix three categories of test failures:

**1. tests/bridge/test_multi_bridge.py -- stale default assertion (1 failure)**
- Line asserting `config.sim_steps_per_frame == 10` is wrong -- the actual default in MultiRobotConfig is `sim_steps_per_frame: int = 5`
- Change the assertion to `assert config.sim_steps_per_frame == 5`

**2. tests/locomotion/test_locomotion.py -- wrong model path (6+ failures)**
- Line 14 defines `GO2_XML = str(Path(__file__).parent.parent / "models" / "unitree_go2" / "go2.xml")` which resolves to `tests/models/unitree_go2/go2.xml` -- does not exist
- The real model is at the project root: `models/unitree_go2/go2.xml`
- Fix to: `GO2_XML = str(Path(__file__).parent.parent.parent / "models" / "unitree_go2" / "go2.xml")`
- This resolves to `<project_root>/models/unitree_go2/go2.xml` which exists
- Note: The MuJoCo integration tests (TestMuJoCoIntegration) also reference this path and will be fixed by the same change

**3. tests/web/test_streaming_viz.py -- true_rgb color mode test (1 failure)**
- `test_true_rgb_color_mode` asserts colors are `[255, 255, 255]` (white placeholder) but the actual WebStreamingViz implementation returns robot-tinted colors `[230, 159, 0]` even in true_rgb mode when no per-point RGB data is available in the voxel array
- Read the `_build_cloud_delta` or equivalent method in `backend/web/streaming_viz.py` to understand actual true_rgb behavior
- Update the test assertion to match the actual implementation behavior -- the test expectation was written before the implementation and is wrong
- If true_rgb mode falls back to robot_tint when no per-point colors exist, update the test to assert robot_tint colors for robot_a (which is `[230, 159, 0]` per `color_for_robot`)
  </action>
  <verify>
    <automated>cd /home/prannayag/pragnition/robotics/dimensional-applications && uv run python -m pytest tests/bridge/test_multi_bridge.py tests/locomotion/test_locomotion.py tests/web/test_streaming_viz.py -k "not integration and not asyncio" -q --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - grep "sim_steps_per_frame == 5" tests/bridge/test_multi_bridge.py returns a match
    - grep "parent.parent.parent" tests/locomotion/test_locomotion.py returns a match (3 levels up from test file to project root)
    - uv run python -m pytest tests/bridge/test_multi_bridge.py -q passes with 0 failures
    - uv run python -m pytest tests/locomotion/test_locomotion.py -k "not integration" -q passes (TestPatchActuators tests pass; MuJoCo integration tests may still need the actual MuJoCo model)
    - uv run python -m pytest tests/web/test_streaming_viz.py::TestWebStreamingViz::test_true_rgb_color_mode -q passes
  </acceptance_criteria>
  <done>All three test files' non-integration tests pass. Stale assertions corrected to match actual implementation defaults and behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Install pytest-asyncio and fix async test configuration</name>
  <files>pyproject.toml, tests/web/test_streaming_viz.py</files>
  <read_first>
    - pyproject.toml
    - tests/web/test_streaming_viz.py
  </read_first>
  <action>
Fix the 2 async web test failures caused by missing pytest-asyncio:

**1. Add pytest-asyncio to dev dependencies in pyproject.toml**
- Add `pytest-asyncio` to the dev dependencies group (or test dependencies, whichever pattern the project uses)
- Run `uv sync` after updating pyproject.toml

**2. Configure pytest-asyncio mode**
- The pytest.ini (or pyproject.toml [tool.pytest.ini_options]) already has `asyncio_mode = "auto"` and `asyncio_default_fixture_loop_scope = "function"` but pytest warns these are "unknown config options" because pytest-asyncio is not installed
- After installing, these warnings should disappear and the 2 async tests should run

**3. Verify the async tests**
- `TestConnectionManager::test_broadcast_json_sends_to_all` -- uses `@pytest.mark.asyncio`
- `TestConnectionManager::test_disconnect_removes_websocket` -- uses `@pytest.mark.asyncio`
- Both should pass once pytest-asyncio is available

If pytest-asyncio auto mode causes issues with the existing sync tests, use `mode = "strict"` instead and ensure only the 2 async tests have the `@pytest.mark.asyncio` decorator.
  </action>
  <verify>
    <automated>cd /home/prannayag/pragnition/robotics/dimensional-applications && uv run python -m pytest tests/web/test_streaming_viz.py -q --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - grep "pytest-asyncio" pyproject.toml returns a match
    - uv run python -m pytest tests/web/test_streaming_viz.py -q shows 0 failures (all 34 tests pass)
    - uv run python -m pytest tests/web/test_streaming_viz.py -q output contains no "PytestUnknownMarkWarning" for asyncio
  </acceptance_criteria>
  <done>pytest-asyncio installed, async web tests pass, no asyncio-related warnings in test output.</done>
</task>

</tasks>

<verification>
Full test suite verification:
```bash
uv run python -m pytest tests/ -k "not integration" -q --timeout=30
```
Expected: All non-integration tests pass with 0 failures.

Collection verification:
```bash
uv run python -m pytest tests/ --collect-only 2>&1 | tail -5
```
Expected: 163+ tests collected, no errors.
</verification>

<success_criteria>
1. `uv run python -m pytest tests/ -k "not integration" -q --timeout=30` passes with 0 failures
2. No "unknown config option" warnings for asyncio_mode
3. All test assertions match actual implementation defaults
</success_criteria>

<output>
After completion, create `.planning/phases/07-cleanup-and-verification-gaps/07-01-SUMMARY.md`
</output>
