---
phase: 01-simulation-bridge-and-single-robot-slam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pytest.ini
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_sim_bridge.py
  - tests/test_slam_pipeline.py
  - tests/test_octomap_builder.py
  - tests/test_drift_metrics.py
  - src/__init__.py
  - src/bridge/__init__.py
  - src/bridge/sensor_types.py
  - src/bridge/env_config.py
  - scripts/discover_simworld.py
  - requirements.txt
autonomous: true
requirements:
  - SIM-01
  - SIM-02
  - SIM-04

must_haves:
  truths:
    - "Test infrastructure exists and pytest runs successfully (even if tests are pending/skipped)"
    - "SimWorld observation space keys, shapes, and dtypes are documented in a discovery output"
    - "SimWorld action space format and gym registration ID are known"
    - "SimWorld step rate is measured and go/no-go criterion (5+ Hz) is evaluated"
    - "Shared data types for sensor frames and environment config exist for downstream plans"
  artifacts:
    - path: "pytest.ini"
      provides: "pytest configuration with timeout"
      contains: "[pytest]"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures (mock env, sample sensor data)"
      contains: "def mock_sensor_frame"
    - path: "tests/test_sim_bridge.py"
      provides: "Test stubs for SIM-01 through SIM-04"
      contains: "test_lifecycle"
    - path: "src/bridge/sensor_types.py"
      provides: "SensorFrame and Pose data classes"
      contains: "class SensorFrame"
    - path: "scripts/discover_simworld.py"
      provides: "Discovery script that prints obs/action space and step rate"
      contains: "env.reset()"
  key_links:
    - from: "tests/conftest.py"
      to: "src/bridge/sensor_types.py"
      via: "import for fixture typing"
      pattern: "from src.bridge.sensor_types import"
---

<objective>
Set up test infrastructure (Wave 0), define shared data types, and discover the SimWorld gym API empirically.

Purpose: All downstream plans depend on knowing the SimWorld observation/action space format and having test scaffolding. This plan resolves the biggest unknown (LOW confidence SimWorld API) and establishes the foundation for parallel development.

Output: pytest config, test stubs for all 8 requirements, shared SensorFrame/EnvConfig types, SimWorld discovery script with documented findings, requirements.txt with all dependencies.
</objective>

<execution_context>
@/home/prannayag/.claude/get-shit-done/workflows/execute-plan.md
@/home/prannayag/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-simulation-bridge-and-single-robot-slam/1-CONTEXT.md
@.planning/phases/01-simulation-bridge-and-single-robot-slam/01-RESEARCH.md
@.planning/phases/01-simulation-bridge-and-single-robot-slam/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test infrastructure and shared data types</name>
  <files>
    pytest.ini,
    requirements.txt,
    tests/__init__.py,
    tests/conftest.py,
    tests/test_sim_bridge.py,
    tests/test_slam_pipeline.py,
    tests/test_octomap_builder.py,
    tests/test_drift_metrics.py,
    src/__init__.py,
    src/bridge/__init__.py,
    src/bridge/sensor_types.py,
    src/bridge/env_config.py
  </files>
  <read_first>
    flake.nix (check Python version and existing deps),
    .planning/phases/01-simulation-bridge-and-single-robot-slam/01-VALIDATION.md (test structure requirements),
    .planning/phases/01-simulation-bridge-and-single-robot-slam/01-RESEARCH.md (data types and patterns)
  </read_first>
  <action>
    1. Create `requirements.txt` with all Phase 1 dependencies:
       ```
       gymnasium>=1.0.0
       open3d>=0.18.0
       octomap-python>=1.8.0
       rerun-sdk>=0.30.0
       evo>=1.0.0
       pynput>=1.7.6
       numpy>=1.26.0
       scipy>=1.15.0
       opencv-python-headless>=4.0.0
       pytest>=8.0.0
       pytest-timeout>=2.0.0
       ```

    2. Create `pytest.ini`:
       ```ini
       [pytest]
       testpaths = tests
       timeout = 30
       markers =
           integration: tests requiring SimWorld running
           unit: pure unit tests with no external deps
       ```

    3. Create `src/__init__.py` and `src/bridge/__init__.py` as empty package inits.

    4. Create `src/bridge/sensor_types.py` with data classes:
       ```python
       from dataclasses import dataclass
       import numpy as np

       @dataclass
       class SensorFrame:
           rgb: np.ndarray              # (H, W, 3) uint8
           depth: np.ndarray | None     # (H, W) float32 meters, None if monocular fallback
           ground_truth_pose: np.ndarray # (4, 4) float64 homogeneous transform
           sim_time: float              # simulation timestamp in seconds

       @dataclass
       class CameraIntrinsics:
           fx: float  # focal length x (pixels)
           fy: float  # focal length y (pixels)
           cx: float  # principal point x
           cy: float  # principal point y
           width: int
           height: int
       ```

    5. Create `src/bridge/env_config.py`:
       ```python
       from dataclasses import dataclass, field

       @dataclass
       class SimWorldEnvConfig:
           env_id: str = "SimWorldRobotics-v0"  # UPDATE after discovery
           render_mode: str | None = None
           max_episode_steps: int = 10000
           env_kwargs: dict = field(default_factory=dict)
           target_step_hz: float = 10.0  # target gym step rate
       ```

    6. Create `tests/__init__.py` (empty).

    7. Create `tests/conftest.py` with shared fixtures:
       - `mock_sensor_frame()`: Returns a SensorFrame with random (64, 64, 3) RGB, (64, 64) depth, identity pose, sim_time=0.0
       - `mock_camera_intrinsics()`: Returns CameraIntrinsics(fx=32.0, fy=32.0, cx=32.0, cy=32.0, width=64, height=64)
       - `sample_point_cloud()`: Returns (100, 3) random float64 array
       - `sample_poses()`: Returns list of 10 random (4,4) homogeneous transforms (valid rotation matrices via scipy Rotation.random)

    8. Create test files with PENDING stubs (tests marked with `pytest.mark.skip(reason="pending implementation")`):
       - `tests/test_sim_bridge.py`: `test_lifecycle`, `test_sensor_extraction`, `test_movement_command`, `test_ground_truth_pose` -- all marked `@pytest.mark.integration` AND `@pytest.mark.skip`
       - `tests/test_slam_pipeline.py`: `test_slam_processes_frames`, `test_point_cloud_output` -- marked `@pytest.mark.integration` AND `@pytest.mark.skip`
       - `tests/test_octomap_builder.py`: `test_occupancy_grid` -- marked `@pytest.mark.unit` AND `@pytest.mark.skip`
       - `tests/test_drift_metrics.py`: `test_ate_rpe` -- marked `@pytest.mark.unit` AND `@pytest.mark.skip`

    Each test stub should have a docstring describing what it will test (the requirement) and an `assert False, "pending"` body.
  </action>
  <verify>
    <automated>pip install pytest pytest-timeout && python -m pytest tests/ -v --timeout=30 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - pytest.ini contains `[pytest]` and `timeout = 30`
    - requirements.txt contains `gymnasium>=1.0.0` and `rerun-sdk>=0.30.0` and `pytest>=8.0.0`
    - src/bridge/sensor_types.py contains `class SensorFrame` and `class CameraIntrinsics`
    - src/bridge/env_config.py contains `class SimWorldEnvConfig`
    - tests/conftest.py contains `def mock_sensor_frame` and `def mock_camera_intrinsics` and `def sample_point_cloud`
    - tests/test_sim_bridge.py contains `def test_lifecycle` and `def test_sensor_extraction` and `def test_movement_command` and `def test_ground_truth_pose`
    - tests/test_slam_pipeline.py contains `def test_slam_processes_frames` and `def test_point_cloud_output`
    - tests/test_octomap_builder.py contains `def test_occupancy_grid`
    - tests/test_drift_metrics.py contains `def test_ate_rpe`
    - `python -m pytest tests/ -v` exits 0 (all tests skipped, none fail)
  </acceptance_criteria>
  <done>Test infrastructure runs, all 8 test stubs exist and are skipped, shared data types defined, requirements.txt lists all Phase 1 deps.</done>
</task>

<task type="auto">
  <name>Task 2: Create and run SimWorld discovery script</name>
  <files>
    scripts/discover_simworld.py,
    docs/simworld_discovery.md
  </files>
  <read_first>
    src/bridge/sensor_types.py (data types to validate against),
    src/bridge/env_config.py (env_id to update),
    .planning/phases/01-simulation-bridge-and-single-robot-slam/01-RESEARCH.md (discovery script pattern and known SimWorld structure)
  </read_first>
  <action>
    1. Clone SimWorld-Robotics if not already present:
       ```bash
       git clone https://github.com/SCAI-JHU/SimWorld-Robotics.git /tmp/SimWorld-Robotics 2>/dev/null || true
       ```

    2. Read the SimWorld gym source to discover API before running anything:
       - Read `simworld_gym/__init__.py` for registered env IDs
       - Read `simworld_gym/envs/simple_world.py` for observation_space and action_space definitions
       - Read `simworld_gym/envs/world_buffer.py` for sensor data structure
       - Search for `depth`, `rgb`, `image`, `camera`, `pose`, `orientation` in the env source files
       - Look for camera intrinsics or FOV settings

    3. Create `scripts/discover_simworld.py` that:
       - Prints observation space type and all keys with shapes/dtypes
       - Prints action space type and sample
       - Prints info dict keys from reset() and step()
       - Runs 50 steps with random actions and measures step rate (mean, min, max Hz)
       - Checks if depth is present in observations
       - Checks if ground-truth pose is available (in obs or info)
       - Prints camera intrinsics if found
       - Saves all findings to `docs/simworld_discovery.md`
       - Evaluates go/no-go: prints "GO" if step rate >= 5 Hz, "NO-GO: batch mode required" if below

    4. Create `docs/simworld_discovery.md` documenting ALL findings:
       - Gym environment ID (actual registered name)
       - Observation space keys and shapes
       - Action space shape and meaning
       - Depth availability (yes/no)
       - Ground-truth pose format and location
       - Camera intrinsics (if found) or "must be inferred from FOV + resolution"
       - Step rate measurement results
       - Go/no-go determination
       - Any surprises or deviations from research assumptions

    5. Update `src/bridge/env_config.py` `env_id` default to the actual discovered gym ID.

    6. Update `src/bridge/sensor_types.py` field names/types if the discovery reveals different observation structure than assumed.

    NOTE: If SimWorld cannot be installed or run in the current environment (no UE5, no GPU), document what was learned from SOURCE CODE READING alone and mark the script as "requires SimWorld runtime to execute." The source code reading is the critical deliverable -- runtime execution is bonus.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/discover_simworld.py').read()); print('syntax OK')" && test -f docs/simworld_discovery.md && echo "discovery doc exists"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/discover_simworld.py contains `env.reset()` and `env.step(` and `time.monotonic()`
    - scripts/discover_simworld.py contains a print statement with "Hz" (step rate measurement)
    - scripts/discover_simworld.py contains "GO" or "NO-GO" evaluation logic
    - docs/simworld_discovery.md contains "Observation Space" and "Action Space" and "Step Rate"
    - docs/simworld_discovery.md contains "Go/No-Go" determination
    - src/bridge/env_config.py env_id field is updated if discovery found a different ID
  </acceptance_criteria>
  <done>SimWorld API is documented from source code reading. Discovery script exists and is syntactically valid. env_config and sensor_types are updated to match actual API. Go/no-go determination is recorded.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -v --timeout=30` exits 0 (all stubs skipped)
- `scripts/discover_simworld.py` parses without syntax errors
- `docs/simworld_discovery.md` documents observation space, action space, step rate, and go/no-go
- `src/bridge/sensor_types.py` contains `class SensorFrame` with fields matching discovered API
</verification>

<success_criteria>
1. Test infrastructure is functional and all 8 requirement test stubs exist
2. SimWorld gym API is understood from source code reading (obs keys, action space, sensor availability)
3. Go/no-go criterion (5 Hz step rate) is evaluated and documented
4. Shared data types (SensorFrame, CameraIntrinsics, SimWorldEnvConfig) are defined and match reality
5. All downstream plans can reference sensor_types.py and env_config.py as stable interfaces
</success_criteria>

<output>
After completion, create `.planning/phases/01-simulation-bridge-and-single-robot-slam/01-01-SUMMARY.md`
</output>
