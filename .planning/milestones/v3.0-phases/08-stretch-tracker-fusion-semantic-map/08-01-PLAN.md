---
phase: 08-stretch-tracker-fusion-semantic-map
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/tracking/test_bytetrack_tracker.py
  - tests/tracking/test_bytetrack_association.py
  - tests/perception/test_fusion_manager.py
  - tests/perception/test_semantic_map.py
  - tests/perception/test_heterogeneous_backends.py
  - tests/integration/test_bytetrack_e2e.py
  - frontend/src/stores/__tests__/semanticMapStore.test.ts
autonomous: true
requirements:
  - DET-STRETCH-01
  - DET-STRETCH-02
  - DET-STRETCH-03
  - DET-STRETCH-04

must_haves:
  truths:
    - "All 7 test files exist and are importable"
    - "All skip-stubs pass with pytest.skip marker"
    - "Frontend vitest skip-stub passes with test.skip"
  artifacts:
    - path: "tests/tracking/test_bytetrack_tracker.py"
      provides: "SC#1 lockdown stub (100-frame stability)"
    - path: "tests/tracking/test_bytetrack_association.py"
      provides: "Association edge case stubs"
    - path: "tests/perception/test_fusion_manager.py"
      provides: "SC#2 lockdown stub (cross-robot fusion)"
    - path: "tests/perception/test_semantic_map.py"
      provides: "SC#3 lockdown stub (TTL expiry)"
    - path: "tests/perception/test_heterogeneous_backends.py"
      provides: "SC#4 lockdown stub (per-robot backends)"
    - path: "tests/integration/test_bytetrack_e2e.py"
      provides: "ByteTrack in coordinator loop stub"
    - path: "frontend/src/stores/__tests__/semanticMapStore.test.ts"
      provides: "Vitest stub for semanticMapStore"
  key_links: []
---

<objective>
Scaffold all Wave 0 test skip-stubs for Phase 8 so downstream plans have Nyquist-compliant verify targets.

Purpose: Every implementation plan needs a test file to verify against. Creating skip-stubs first ensures test infrastructure is in place before any production code lands.
Output: 6 Python pytest skip-stub files + 1 vitest skip-stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-stretch-tracker-fusion-semantic-map/08-CONTEXT.md
@.planning/phases/08-stretch-tracker-fusion-semantic-map/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 Python pytest skip-stubs for Phase 8 test files</name>
  <files>
    tests/tracking/test_bytetrack_tracker.py
    tests/tracking/test_bytetrack_association.py
    tests/perception/test_fusion_manager.py
    tests/perception/test_semantic_map.py
    tests/perception/test_heterogeneous_backends.py
    tests/integration/test_bytetrack_e2e.py
  </files>
  <read_first>
    tests/tracking/test_tracker_registry.py
    tests/perception/test_swap_backend.py
  </read_first>
  <action>
Create 6 skip-stub test files following the exact pattern from existing Phase 7 stubs (test_tracker_registry.py as template). Each file contains one or more `pytest.mark.skip(reason="Phase 8 Wave N stub")` test functions that name the specific behavior they will test when implemented.

**tests/tracking/test_bytetrack_tracker.py** (DET-STRETCH-01 SC#1):
```python
"""ByteTrack tracker — stable track_id across frames (DET-STRETCH-01 SC#1)."""
import pytest

@pytest.mark.skip(reason="Phase 8 Plan 02 — ByteTrack implementation")
def test_stationary_chair_keeps_single_track_id_100_frames():
    """SC#1: stationary chair keeps one track_id across >=100 frames."""

@pytest.mark.skip(reason="Phase 8 Plan 02 — ByteTrack implementation")
def test_track_creation_respects_track_thresh():
    """New track only created when confidence >= track_thresh (default 0.3)."""

@pytest.mark.skip(reason="Phase 8 Plan 02 — ByteTrack implementation")
def test_track_deletion_after_frame_gap():
    """Track deleted after frames_since_seen > frame_gap (default 30)."""

@pytest.mark.skip(reason="Phase 8 Plan 02 — ByteTrack implementation")
def test_geometry_fields_not_mutated():
    """Tracker MUST NOT mutate center, half_extents, quaternion, class_name, score."""

@pytest.mark.skip(reason="Phase 8 Plan 02 — ByteTrack implementation")
def test_capture_pose_timestamp_preserved():
    """capture_pose + capture_timestamp MUST NOT be mutated by tracker."""

@pytest.mark.skip(reason="Phase 8 Plan 02 — ByteTrack implementation")
def test_reset_clears_all_tracks():
    """reset() clears internal state; next frame creates fresh tracks."""
```

**tests/tracking/test_bytetrack_association.py** (DET-STRETCH-01 association):
```python
"""ByteTrack spatial association edge cases (DET-STRETCH-01)."""
import pytest

@pytest.mark.skip(reason="Phase 8 Plan 02 — ByteTrack implementation")
def test_within_gate_match():
    """Detection within match_thresh of existing track associates correctly."""

@pytest.mark.skip(reason="Phase 8 Plan 02 — ByteTrack implementation")
def test_cross_gate_miss():
    """Detection beyond match_thresh creates a new track, not a false match."""

@pytest.mark.skip(reason="Phase 8 Plan 02 — ByteTrack implementation")
def test_class_gate_prevents_cross_class_match():
    """Two different-class objects at same location get separate track_ids."""

@pytest.mark.skip(reason="Phase 8 Plan 02 — ByteTrack implementation")
def test_multi_object_disambiguation():
    """Multiple same-class objects are disambiguated by distance."""

@pytest.mark.skip(reason="Phase 8 Plan 02 — ByteTrack implementation")
def test_empty_detections():
    """Empty detections_3d returns empty items, no crash."""

@pytest.mark.skip(reason="Phase 8 Plan 02 — ByteTrack implementation")
def test_hungarian_optimal_assignment():
    """Ambiguous case: verify scipy Hungarian gives optimal assignment."""
```

**tests/perception/test_fusion_manager.py** (DET-STRETCH-02 SC#2):
```python
"""DetectionFusionManager cross-robot fusion (DET-STRETCH-02 SC#2)."""
import pytest

@pytest.mark.skip(reason="Phase 8 Plan 05 — FusionManager implementation")
def test_two_robots_same_chair_within_radius_fuse():
    """SC#2: two robots detecting same chair within 0.5m produce single fused entry."""

@pytest.mark.skip(reason="Phase 8 Plan 05 — FusionManager implementation")
def test_two_robots_different_locations_no_fuse():
    """Detections >0.5m apart remain separate fused entries."""

@pytest.mark.skip(reason="Phase 8 Plan 05 — FusionManager implementation")
def test_class_gate_prevents_cross_class_fusion():
    """Chair and table at same location are NOT fused."""

@pytest.mark.skip(reason="Phase 8 Plan 05 — FusionManager implementation")
def test_highest_confidence_is_representative():
    """Fused entry picks highest-confidence detection as representative OBB."""

@pytest.mark.skip(reason="Phase 8 Plan 05 — FusionManager implementation")
def test_single_robot_passthrough():
    """With one robot, all detections pass through as single-source fused entries."""
```

**tests/perception/test_semantic_map.py** (DET-STRETCH-03 SC#3):
```python
"""SemanticMap with TTL (DET-STRETCH-03 SC#3)."""
import pytest

@pytest.mark.skip(reason="Phase 8 Plan 05 — SemanticMap implementation")
def test_insert_object_and_retrieve():
    """Object inserted via update() appears in get_delta() active list."""

@pytest.mark.skip(reason="Phase 8 Plan 05 — SemanticMap implementation")
def test_expired_object_in_expired_ids():
    """SC#3: advance time past TTL, object appears in expired_ids."""

@pytest.mark.skip(reason="Phase 8 Plan 05 — SemanticMap implementation")
def test_refresh_resets_ttl():
    """Re-observing an object resets its last_seen, extending its life."""

@pytest.mark.skip(reason="Phase 8 Plan 05 — SemanticMap implementation")
def test_expired_removed_from_internal_dict():
    """After get_delta() returns expired_ids, those objects are gone from internal dict."""

@pytest.mark.skip(reason="Phase 8 Plan 05 — SemanticMap implementation")
def test_default_ttl_10_seconds():
    """Default TTL is 10.0 seconds per D-12."""
```

**tests/perception/test_heterogeneous_backends.py** (DET-STRETCH-04 SC#4):
```python
"""Heterogeneous per-robot backends (DET-STRETCH-04 SC#4)."""
import pytest

@pytest.mark.skip(reason="Phase 8 Plan 03 — heterogeneous backends")
def test_swap_backend_for_robot_updates_single_worker():
    """SC#4: swap_backend_for_robot(rid) changes only that robot's detector."""

@pytest.mark.skip(reason="Phase 8 Plan 03 — heterogeneous backends")
def test_per_robot_backends_dict_tracks_state():
    """_per_robot_backends[rid] updated after swap_backend_for_robot."""

@pytest.mark.skip(reason="Phase 8 Plan 03 — heterogeneous backends")
def test_swap_backend_all_robots_still_works():
    """swap_backend() (all robots) still works and updates all entries."""

@pytest.mark.skip(reason="Phase 8 Plan 03 — heterogeneous backends")
def test_crash_fallback_scoped_to_crashed_robot():
    """Crash in robot_1's backend only falls back robot_1, not robot_0."""
```

**tests/integration/test_bytetrack_e2e.py** (integration):
```python
"""ByteTrack in full coordinator loop (DET-STRETCH-01 integration)."""
import pytest

@pytest.mark.skip(reason="Phase 8 Plan 04 — ByteTrack integration wiring")
def test_bytetrack_stable_track_ids_in_ws_stream():
    """Run ByteTrack in coordinator loop for 30 frames, assert stable track_ids in output."""
```
  </action>
  <verify>
    <automated>cd /home/prannayag/pragnition/robotics/argus && uv run pytest tests/tracking/test_bytetrack_tracker.py tests/tracking/test_bytetrack_association.py tests/perception/test_fusion_manager.py tests/perception/test_semantic_map.py tests/perception/test_heterogeneous_backends.py tests/integration/test_bytetrack_e2e.py -v --co 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - tests/tracking/test_bytetrack_tracker.py contains `def test_stationary_chair_keeps_single_track_id_100_frames`
    - tests/tracking/test_bytetrack_association.py contains `def test_class_gate_prevents_cross_class_match`
    - tests/perception/test_fusion_manager.py contains `def test_two_robots_same_chair_within_radius_fuse`
    - tests/perception/test_semantic_map.py contains `def test_expired_object_in_expired_ids`
    - tests/perception/test_heterogeneous_backends.py contains `def test_swap_backend_for_robot_updates_single_worker`
    - tests/integration/test_bytetrack_e2e.py contains `def test_bytetrack_stable_track_ids_in_ws_stream`
    - All 6 files are collected by pytest (--co exits 0)
  </acceptance_criteria>
  <done>All 6 Python test files exist, are importable, and are collected by pytest with skip markers.</done>
</task>

<task type="auto">
  <name>Task 2: Create vitest skip-stub for semanticMapStore</name>
  <files>frontend/src/stores/__tests__/semanticMapStore.test.ts</files>
  <read_first>
    frontend/src/stores/__tests__/detectorStore.shape.test.ts
  </read_first>
  <action>
Create a vitest skip-stub at `frontend/src/stores/__tests__/semanticMapStore.test.ts` following the existing `detectorStore.shape.test.ts` pattern.

```typescript
import { describe, test } from 'vitest';

describe('semanticMapStore', () => {
  test.skip('store has objects, visible, currentSimTime fields', () => {
    // Phase 8 Plan 07 — semanticMapStore implementation
  });

  test.skip('addOrUpdate inserts new objects and updates existing', () => {
    // Phase 8 Plan 07 — semanticMapStore implementation
  });

  test.skip('remove deletes objects by fused_track_id', () => {
    // Phase 8 Plan 07 — semanticMapStore implementation
  });

  test.skip('clear removes all objects', () => {
    // Phase 8 Plan 07 — semanticMapStore implementation
  });

  test.skip('setVisible toggles visibility flag', () => {
    // Phase 8 Plan 07 — semanticMapStore implementation
  });
});
```
  </action>
  <verify>
    <automated>cd /home/prannayag/pragnition/robotics/argus/frontend && npx vitest run src/stores/__tests__/semanticMapStore.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/stores/__tests__/semanticMapStore.test.ts contains `describe('semanticMapStore'`
    - frontend/src/stores/__tests__/semanticMapStore.test.ts contains `test.skip`
    - vitest collects the file without error (exit 0 with skipped tests)
  </acceptance_criteria>
  <done>Vitest skip-stub exists and is collected successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- this plan creates only test scaffolds with skip markers.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | Wave 0 test stubs have no security surface |
</threat_model>

<verification>
All 7 test files exist and pass collection:
- `uv run pytest tests/tracking/test_bytetrack_tracker.py tests/tracking/test_bytetrack_association.py tests/perception/test_fusion_manager.py tests/perception/test_semantic_map.py tests/perception/test_heterogeneous_backends.py tests/integration/test_bytetrack_e2e.py --co` exits 0
- `cd frontend && npx vitest run src/stores/__tests__/semanticMapStore.test.ts` exits 0
</verification>

<success_criteria>
7 test skip-stub files created and collected by their respective test runners. No production code modified.
</success_criteria>

<output>
After completion, create `.planning/phases/08-stretch-tracker-fusion-semantic-map/08-01-SUMMARY.md`
</output>
