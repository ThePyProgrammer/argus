---
phase: 05-second-backends-boxer-rtdetr-owlv2
plan: 10
type: execute
wave: 5
depends_on: [07, 08, 09]
files_modified:
  - src/main.py
  - frontend/src/hooks/useWebSocket.ts
autonomous: true
requirements: [DET-MODELS-02, DET-MODELS-03, DET-MODELS-06]
must_haves:
  truths:
    - "src/main.py passes streaming_viz to DetectorWorkerPool via set_streaming_viz() after both objects exist"
    - "src/main.py restart block imports src.perception.backends (already does per current code) — RTDETRv2 + BoxeR surface via side-effect registration without additional main.py changes"
    - "frontend/src/hooks/useWebSocket.ts crash_fallback subsystem=='detector' branch populates useDetectorStore.getState().setCrashMessage and calls setActive('yolov11', 'YOLOv11-nano', {}) — replaces the console.log placeholder"
  artifacts:
    - path: src/main.py
      provides: streaming_viz hookup on DetectorWorkerPool (one-line setter call after pool rebuild)
      contains: "set_streaming_viz"
    - path: frontend/src/hooks/useWebSocket.ts
      provides: detector-subsystem crash_fallback handler (not a new component, just replaces the console.log else-branch)
      contains: "setCrashMessage"
  key_links:
    - from: src/main.py restart block
      to: DetectorWorkerPool._streaming_viz
      via: "detector_pool.set_streaming_viz(streaming_viz) called after both objects exist"
      pattern: "set_streaming_viz"
    - from: frontend/src/hooks/useWebSocket.ts crash_fallback handler
      to: useDetectorStore.getState().setCrashMessage + setActive
      via: "subsystem === 'detector' branch"
      pattern: "setCrashMessage"
---

<objective>
Wave 3 integration: wire the Wave 2 infrastructure into the main.py restart block (streaming_viz ref on DetectorWorkerPool) and complete the useWebSocket.ts handler so the frontend actually renders CrashToast for detector crashes (not just for SLAM).

Purpose: Both Wave 2 pieces (RTDETRv2 backend + BoxeR backend + pool crash handler) already register via side-effect imports and are enumerated by the existing /api/detectors/backends route. The only wiring main.py needs is hooking streaming_viz into the pool so on_backend_crash has a queue to append to. The only frontend edit is replacing the 4-line console.log placeholder (currently at useWebSocket.ts:197-202) with the real setCrashMessage + setActive calls — this matches the SLAM branch shape.

Output: main.py one-liner setter call; useWebSocket.ts 4-line edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-CONTEXT.md
@.planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-RESEARCH.md
@src/main.py
@frontend/src/hooks/useWebSocket.ts
@frontend/src/stores/detectorStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire streaming_viz into DetectorWorkerPool in main.py restart block</name>
  <read_first>
    - src/main.py lines ~440–610 (the restart block where DetectorWorkerPool is constructed; find the exact line after detector_pool = DetectorWorkerPool(...) creation)
    - src/main.py for where the streaming_viz variable is constructed and how it flows into the restart block
    - src/perception/worker_pool.py::DetectorWorkerPool.set_streaming_viz (added in Plan 09)
    - .planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-RESEARCH.md "D-03 — Crash watchdog ownership" Step 3 for the main.py wiring expectation
  </read_first>
  <action>
    Edit `src/main.py`. Find the block (around line 530, inside the restart logic) where `detector_pool = DetectorWorkerPool(...)` is constructed. Immediately AFTER the `detector_pool.warmup_all(dummy_frames); detector_pool.start()` lines (around line 557–558), and BEFORE `coordinator._detector_pool = detector_pool` (around line 559), INSERT three lines with the same indentation as the surrounding restart-block code:

    ```
                        # Phase 5 D-03 — wire streaming_viz so pool.on_backend_crash
                        # can append crash_fallback messages to the WS queue.
                        if streaming_viz is not None:
                            detector_pool.set_streaming_viz(streaming_viz)
    ```

    Preserve the existing indentation level (the block is inside a try which is inside `if should_restart:` inside `_run_simulation_loop` — match the surrounding code indentation exactly).

    Do NOT modify the existing DetectorWorkerPool constructor call — streaming_viz is passed via setter, not kwarg, so pool construction stays untouched. (The Plan 09 pool also accepts `streaming_viz=` as an __init__ kwarg, but using the setter is cleaner here because the pool is constructed BEFORE we have a guaranteed-non-None streaming_viz reference in the restart logic flow.)

    If you grep for `streaming_viz` in src/main.py you will see it is constructed near the top of `_run_simulation_loop` and passed around. The setter call lands inside the existing try block that builds the pool — keep the try/except semantics intact.

    Do NOT add any import — set_streaming_viz is already a method on DetectorWorkerPool (added in Plan 09).
  </action>
  <verify>
    <automated>grep -q "detector_pool.set_streaming_viz(streaming_viz)" src/main.py && grep -q "Phase 5 D-03" src/main.py && uv run python -c "import ast; ast.parse(open('src/main.py').read())" ; echo verify-status=$?</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "detector_pool.set_streaming_viz(streaming_viz)" src/main.py` returns 1
    - `grep -c "Phase 5 D-03" src/main.py` returns at least 1 (the comment we added)
    - `uv run python -c "import ast; ast.parse(open('src/main.py').read())"` exits 0 (valid Python after edit)
    - Edit is inside the restart block, NOT at module scope: `grep -B2 "detector_pool.set_streaming_viz" src/main.py | grep -q "streaming_viz is not None"` (the if-guard we added should precede the setter call)
    - `uv run pytest tests/ -x --timeout=120 -m "not slow_boxer and not network"` exits 0 (no regression)
  </acceptance_criteria>
  <done>main.py wires streaming_viz into the detector pool after warmup_all; file parses; no regression in app boot path.</done>
</task>

<task type="auto">
  <name>Task 2: Replace useWebSocket.ts console.log placeholder with setCrashMessage + setActive</name>
  <read_first>
    - frontend/src/hooks/useWebSocket.ts lines 180-205 (the crash_fallback handler with the console.log placeholder at line 197-202)
    - frontend/src/stores/detectorStore.ts (confirm setCrashMessage + setActive signatures match Plan 03 SLAM side — both already shipped)
    - .planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-CONTEXT.md D-03 step "Frontend CrashToast renders from detectorStore.crashMessage which useWebSocket.ts:188-200 populates"
    - .planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-RESEARCH.md Open Risk #8 (interpretation of "ZERO frontend changes" — replacing a placeholder counts as activating existing stubbed code, not adding a new component)
  </read_first>
  <action>
    Edit `frontend/src/hooks/useWebSocket.ts`. Find the `case 'crash_fallback':` block (around line 188). Current code reads approximately:

    ```
    case 'crash_fallback': {
      const payload = msg.payload as { subsystem?: string; crashed_backend: string; fallback_backend: string };
      const subsystem = payload.subsystem ?? 'slam'; // backward-compat default
      if (subsystem === 'slam') {
        const slamState = useSlamStore.getState();
        slamState.setCrashMessage(
          `Backend ${payload.crashed_backend} crashed, fell back to ICP`
        );
        slamState.setActive('icp', 'ICP Odometry', {});
      } else {
        // Phase 5 DET-MODELS-06 will handle subsystem === 'detector' here.
        console.log(
          `[crash_fallback] subsystem=${subsystem}: ${payload.crashed_backend} -> ${payload.fallback_backend}`
        );
      }
      break;
    }
    ```

    REPLACE the `else { ... }` branch (the console.log placeholder) with a real detector branch PLUS a new else-of-last-resort console.warn. The new block shape:

    ```
    } else if (subsystem === 'detector') {
      // Phase 5 DET-MODELS-06 — populate detectorStore.crashMessage so CrashToast renders,
      // and mirror the SLAM pattern by setting active backend to the fallback (yolov11).
      const detectorState = useDetectorStore.getState();
      detectorState.setCrashMessage(
        `Backend ${payload.crashed_backend} crashed, fell back to ${payload.fallback_backend}`
      );
      detectorState.setActive(payload.fallback_backend, 'YOLOv11-nano', {});
    } else {
      console.warn(
        `[crash_fallback] unknown subsystem=${subsystem}: ${payload.crashed_backend} -> ${payload.fallback_backend}`
      );
    }
    ```

    Verify the top-of-file import block already imports `useDetectorStore` from `'../stores/detectorStore'` (line 5 of useWebSocket.ts). Based on the existing file, `useDetectorStore` is already imported (along with `fetchDetectorState`) so no new import needed. If for some reason the import is missing, ADD it.

    Do NOT modify the SLAM branch or any other case. The overall structure becomes:
    - `if (subsystem === 'slam')` — unchanged
    - `else if (subsystem === 'detector')` — new, real implementation
    - `else` — new console.warn fallback for unknown subsystems

    Do NOT add new components, new routes, new WebSocket message types, or new stores. The change is a 4-line body edit + a 1-line else-guard adjustment.
  </action>
  <verify>
    <automated>grep -q "detectorState.setCrashMessage" frontend/src/hooks/useWebSocket.ts && grep -q "detectorState.setActive(payload.fallback_backend" frontend/src/hooks/useWebSocket.ts && ! grep -q "Phase 5 DET-MODELS-06 will handle subsystem" frontend/src/hooks/useWebSocket.ts ; echo "grep-pass=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "detectorState.setCrashMessage" frontend/src/hooks/useWebSocket.ts` returns 1
    - `grep -c "detectorState.setActive(payload.fallback_backend" frontend/src/hooks/useWebSocket.ts` returns 1
    - `grep -c "Phase 5 DET-MODELS-06 will handle subsystem" frontend/src/hooks/useWebSocket.ts` returns 0 (placeholder comment is gone)
    - `grep -c "else if (subsystem === 'detector')" frontend/src/hooks/useWebSocket.ts` returns 1
    - `grep -c "console.warn" frontend/src/hooks/useWebSocket.ts` returns at least 1 (the new unknown-subsystem fallback)
    - `grep -c "useDetectorStore" frontend/src/hooks/useWebSocket.ts` returns at least 2 (existing import + new getState call)
    - The file still imports `useDetectorStore` from `'../stores/detectorStore'` — `grep -c "from '../stores/detectorStore'" frontend/src/hooks/useWebSocket.ts` returns 1
    - If `tsc` is available in the frontend: running `cd frontend && npx tsc --noEmit` exits 0 (no type errors); if tsc is not installed at plan time, skip this gate (file parses by grep checks above).
  </acceptance_criteria>
  <done>useWebSocket.ts detector branch is real; SLAM branch unchanged; typecheck clean.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-02 | Tampering | crash_fallback message payload rendered in frontend | mitigate | React renders string literals via textContent by default (not innerHTML); the crashed_backend + fallback_backend + reason strings are set on detectorStore.crashMessage and consumed by CrashToast via textContent. No HTML injection surface. |

No other applicable threats — this plan only wires existing objects and replaces a console.log.
</threat_model>

<verification>
- `uv run python -c "import ast; ast.parse(open('src/main.py').read())"` exits 0.
- `uv run pytest tests/ -x --timeout=120 -m "not slow_boxer and not network"` exits 0.
- `grep -c "set_streaming_viz" src/main.py` returns 1; `grep -c "setCrashMessage" frontend/src/hooks/useWebSocket.ts` returns at least 1 (the new detector branch).
</verification>

<success_criteria>
main.py streaming_viz wiring committed inside the restart-block try; useWebSocket.ts detector crash branch populates detectorStore and mirrors SLAM pattern; SLAM branch untouched; no new components, no new WS types, no new routes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-10-SUMMARY.md`.
</output>
