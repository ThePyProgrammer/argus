---
phase: 05-second-backends-boxer-rtdetr-owlv2
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
autonomous: true
requirements: [DET-MODELS-02, DET-MODELS-03, DET-MODELS-07]
must_haves:
  truths:
    - "pyproject.toml declares onnxruntime>=1.19.0 in perception extra (D-07)"
    - "pyproject.toml declares optimum[exporters]>=1.20.0 in dev extra (D-06)"
    - "pyproject.toml registers slow_boxer and network pytest markers"
    - ".gitignore excludes subprocess_venvs/ (D-01)"
  artifacts:
    - path: pyproject.toml
      provides: ORT + optimum deps + pytest marker registration
      contains: "onnxruntime"
    - path: .gitignore
      provides: subprocess_venvs/ exclusion
      contains: "subprocess_venvs/"
  key_links:
    - from: pyproject.toml [tool.pytest.ini_options]
      to: tests that use @pytest.mark.slow_boxer / @pytest.mark.network
      via: marker registration silences PytestUnknownMarkWarning
      pattern: "slow_boxer"
---

<objective>
Wave 0 dependency + tooling scaffolding. Add ONNX Runtime + optimum to pyproject.toml extras, register the two pytest markers Phase 5 introduces, and exclude the subprocess venv directory from git. This is the foundation every later wave reads from.

Purpose: Without this, Wave 2 RT-DETRv2 backend cannot even `import onnxruntime`; Wave 1 setup script will create `subprocess_venvs/boxer/` and pollute git; Wave 0+ tests using new markers will spew warnings.

Output: Modified pyproject.toml + .gitignore ready for every downstream plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-CONTEXT.md
@.planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-RESEARCH.md
@pyproject.toml
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend pyproject.toml with ORT + optimum extras and register pytest markers</name>
  <read_first>
    - pyproject.toml (full file — current state has perception + dev extras but NO ort/optimum, NO [tool.pytest.ini_options])
    - .planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-RESEARCH.md section "D-07 — ORT runtime + EP" and "D-06 — ONNX acquisition"
    - .planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-VALIDATION.md Wave 0 Requirements section
  </read_first>
  <action>
    Edit pyproject.toml in three places:

    1. In `[project.optional-dependencies].perception` list, APPEND the line `"onnxruntime>=1.19.0",` after the existing `"scikit-learn>=1.4.0",` entry. Final perception list MUST contain: torch>=2.10.0, transformers>=5.3.0, ultralytics>=8.4.24, scikit-learn>=1.4.0, onnxruntime>=1.19.0.

    2. In `[project.optional-dependencies].dev` list, REPLACE the single-line list with multi-line form and APPEND `"optimum[exporters]>=1.20.0",`. Final dev list MUST contain: pytest>=8.0.0, pytest-timeout>=2.0.0, pytest-asyncio>=0.23.0, optimum[exporters]>=1.20.0.

    3. At end of file, APPEND a new section:
    ```toml
    [tool.pytest.ini_options]
    markers = [
        "slow_boxer: tests requiring a real BoxeR subprocess (skipped unless explicitly selected; CI runs nightly)",
        "network: tests requiring network access to HuggingFace or GitHub (skipped by default)",
    ]
    ```

    Do NOT add any additional markers (keep scope tight — only slow_boxer and network are introduced by Phase 5). Do NOT bump any existing version floor.
  </action>
  <verify>
    <automated>grep -q "onnxruntime>=1.19.0" pyproject.toml && grep -q "optimum\[exporters\]>=1.20.0" pyproject.toml && grep -q "slow_boxer" pyproject.toml && grep -q "\[tool.pytest.ini_options\]" pyproject.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onnxruntime>=1.19.0" pyproject.toml` returns 1
    - `grep -c "optimum\[exporters\]>=1.20.0" pyproject.toml` returns 1
    - `grep -c "slow_boxer" pyproject.toml` returns 1
    - `grep -c "^network:" pyproject.toml` returns 0 AND `grep -c "\"network:" pyproject.toml` returns 1 (marker is inside a string inside a list)
    - `grep -c "^\[tool.pytest.ini_options\]" pyproject.toml` returns 1
    - File parses as valid TOML: `uv run python -c "import tomllib; tomllib.loads(open('pyproject.toml').read())"` exits 0
  </acceptance_criteria>
  <done>pyproject.toml parses as TOML, contains onnxruntime + optimum deps, and declares both pytest markers.</done>
</task>

<task type="auto">
  <name>Task 2: Add subprocess_venvs/ to .gitignore</name>
  <read_first>
    - .gitignore (current state — contains models/orbslam3/ORBvoc entries, .desloppify/, node_modules/ etc.; does NOT contain subprocess_venvs)
    - .planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-CONTEXT.md D-01 (venv layout)
  </read_first>
  <action>
    Append a new section at end of .gitignore:
    ```
    # Phase 5 (D-01): BoxeR subprocess venv — owns its own torch/DINOv3/BoxerNet stack
    subprocess_venvs/
    ```
    Do NOT modify existing entries. Do NOT add models/ (already covered by existing orbslam3-specific entries and the models/ convention — planner-confirmed in RESEARCH D-11).
  </action>
  <verify>
    <automated>grep -q "^subprocess_venvs/$" .gitignore && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^subprocess_venvs/$" .gitignore` returns 1
    - `git check-ignore -v subprocess_venvs/boxer/.ready` matches against .gitignore and prints the rule
  </acceptance_criteria>
  <done>.gitignore excludes subprocess_venvs/ directory; a hypothetical subprocess_venvs/boxer/.ready file would be ignored.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-03 | Tampering | pyproject.toml dep pinning | accept | We pin lower bounds only; upper-bound drift is a general supply-chain concern handled by sha256 manifests at checkpoint-download time (Plan 11), not pip-version-range time. |

No other applicable threats — this plan only mutates toml config + gitignore.
</threat_model>

<verification>
- `uv lock --check` still succeeds (no dep resolution conflict introduced).
- `uv run pytest --collect-only tests/perception/test_subprocess_bridge_skeleton.py` emits no PytestUnknownMarkWarning for slow_boxer/network (after Wave 0 test scaffolds land, but the marker registration must not break existing collection).
</verification>

<success_criteria>
pyproject.toml valid TOML; contains ort + optimum extras and registers slow_boxer + network markers. .gitignore excludes subprocess_venvs/.
</success_criteria>

<output>
After completion, create `.planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-01-SUMMARY.md`.
</output>
