---
phase: 03
plan: 07
type: execute
wave: 2
depends_on:
  - 03-02
  - 03-04
files_modified:
  - frontend/src/components/DetectorDropdown.tsx
  - frontend/src/components/LifterDropdown.tsx
autonomous: true
requirements:
  - DET-UI-01
  - DET-UI-02
tags:
  - frontend
  - ui-component
  - dropdown
must_haves:
  truths:
    - "DetectorDropdown renders backends from useDetectorStore.backends; clicking an available entry calls onSelect(name); active entry is highlighted"
    - "Each dropdown option row shows capability badges for framework, license, cpu_latency_hint_ms (D-06)"
    - "Unavailable backends render greyed-out with reason tooltip (mirror of AlgorithmDropdown:34-77 pattern)"
    - "LifterDropdown mirrors DetectorDropdown shape but reads useDetectorStore.lifters + activeLifter; dispatches onSelect(lifter)"
    - "cd frontend && npx tsc --noEmit exits 0 after both components exist"
  artifacts:
    - path: frontend/src/components/DetectorDropdown.tsx
      provides: "Detector picker with capability-badge option rows (D-01, D-06)"
      min_lines: 130
      contains: "useDetectorStore"
    - path: frontend/src/components/LifterDropdown.tsx
      provides: "Lifter picker mirroring DetectorDropdown — reads detectorStore.lifters (D-07)"
      min_lines: 120
      contains: "activeLifter"
  key_links:
    - from: frontend/src/components/DetectorDropdown.tsx
      to: frontend/src/stores/detectorStore.ts
      via: "useDetectorStore selectors for backends + activeBackend + activeDisplay"
      pattern: "useDetectorStore\\(\\(s\\)\\s*=>\\s*s\\.(backends|activeBackend|activeDisplay)"
    - from: frontend/src/components/DetectorDropdown.tsx
      to: frontend/src/components/CapabilityBadge.tsx
      via: "Badge row per option using new {label, value} signature"
      pattern: "CapabilityBadge\\s+label="
    - from: frontend/src/components/LifterDropdown.tsx
      to: frontend/src/stores/detectorStore.ts
      via: "useDetectorStore selectors for lifters + activeLifter + activeLifterDisplay"
      pattern: "useDetectorStore\\(\\(s\\)\\s*=>\\s*s\\.(lifters|activeLifter|activeLifterDisplay)"

user_setup: []
---

<objective>
Clone `AlgorithmDropdown.tsx` (149 lines) into two new files (per D-01, D-06, D-07):

1. `DetectorDropdown.tsx` — reads `useDetectorStore.backends/activeBackend/activeDisplay`, fires `onSelect(name)`. Each option row shows the 3-key capability badge pack (framework / license / cpu_latency_hint_ms per D-06) using the new `CapabilityBadge label={...} value={...}` signature from Plan 02.
2. `LifterDropdown.tsx` — same shape but reads `useDetectorStore.lifters/activeLifter/activeLifterDisplay`. Lifters only need to show `license` and `outputs_oriented` pills (D-05-analogous for lifters); keep option-row compact.

Both dropdowns handle unavailable backends (greyed-out + tooltip) mirroring `AlgorithmDropdown.tsx:34-77`.

Purpose: DET-UI-01 (detector dropdown with badges) + DET-UI-02 (lifter dropdown exists — visibility gate lives in Plan 10's DetectorSection). Plan 10 mounts both inside DetectorSection and wires the onSelect handlers to the restart flow.

Output: 2 new component files; tsc clean; ready for DetectorSection consumption.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-frontend-picker-and-ui/03-CONTEXT.md
@.planning/phases/03-frontend-picker-and-ui/03-RESEARCH.md

<interfaces>
<!-- AlgorithmDropdown.tsx:1-149 (template source — verified in-tree 2026-04-14) -->
<!-- Key patterns to clone: -->
<!--   - Click-outside + Escape keyboard close (useEffect + document listeners) -->
<!--   - Trigger style (bg #12122a, border animated on hover) -->
<!--   - List style (absolute positioned, z-index 100, max-height 240px scrollable) -->
<!--   - Per-option: active indicator (2px solid #2ecc71 left border, bg rgba(46,204,113,0.08)) -->
<!--   - Unavailable: opacity 0.4, cursor not-allowed, title={reason} -->
<!--   - Capability badges row under display name (map over capabilities filtered) -->

<!-- detectorStore D-02 keys (from Plan 04): -->
<!--   backends: DetectorBackend[] — each has {name, display, available, capabilities, parameter_schema, reason?} -->
<!--   activeBackend: string -->
<!--   activeDisplay: string -->
<!--   lifters: LifterBackend[] -->
<!--   activeLifter: string -->
<!--   activeLifterDisplay: string -->

<!-- CapabilityBadge new signature (from Plan 02): -->
<!--   <CapabilityBadge label="framework" value={backend.capabilities.framework} /> -->
<!--   <CapabilityBadge label="license" value={backend.capabilities.license} /> -->
<!--   <CapabilityBadge label="cpu_latency_hint_ms" value={backend.capabilities.cpu_latency_hint_ms} /> -->
<!--   D-04 handles null/undefined safely (returns null) so missing capability keys are no-ops. -->

<!-- D-05 badge keys for DetectorSection (same as DetectorDropdown option rows): -->
<!--   framework, license, cpu_latency_hint_ms -->
<!-- outputs_3d_natively is FUNCTIONAL — used by Plan 10's visibility gate, NOT rendered -->

<!-- Lifter capabilities (from median_depth.py:117-122) — -->
<!--   {requires_depth, requires_point_cloud, outputs_oriented, license} -->
<!-- D-08-analogous for lifter dropdown option rows — show: license + outputs_oriented (boolean) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Clone AlgorithmDropdown → DetectorDropdown with 3-key capability badges</name>
  <read_first>
    - frontend/src/components/AlgorithmDropdown.tsx (149 lines — exact template)
    - frontend/src/components/CapabilityBadge.tsx (new {label, value} signature from Plan 02)
    - frontend/src/stores/detectorStore.ts (DetectorBackend shape, useDetectorStore surface)
    - .planning/phases/03-frontend-picker-and-ui/03-CONTEXT.md §D-06 (option-row badge keys: framework, license, cpu_latency_hint_ms)
  </read_first>
  <files>frontend/src/components/DetectorDropdown.tsx</files>
  <action>
Create `frontend/src/components/DetectorDropdown.tsx`. Copy AlgorithmDropdown.tsx verbatim, then apply these renames/changes:

### Find-and-replace:
- `from '../stores/slamStore'` → `from '../stores/detectorStore'`
- `useSlamStore` → `useDetectorStore`
- `AlgorithmDropdown` → `DetectorDropdown`
- `'Loading backends...'` → keep
- Props type name (if any) can stay `{ onSelect }`

### Per-option badge row change (line 106-140 of template):
Replace the current boolean-filter capability rendering:
```typescript
const capabilities = Object.entries(backend.capabilities)
  .filter(([, v]) => v === true)
  .map(([k]) => k);
// ...
{capabilities.map((cap) => (
  <CapabilityBadge key={cap} name={cap} />
))}
```

With the D-06 three-key badge pack (heterogeneous values):
```typescript
const BADGE_KEYS: Array<keyof typeof backend.capabilities> = [
  'framework',
  'license',
  'cpu_latency_hint_ms',
];
// render row:
<div style={{ marginTop: '4px', display: 'flex', flexWrap: 'wrap' }}>
  {BADGE_KEYS.map((key) => (
    <CapabilityBadge
      key={key}
      label={key}
      value={backend.capabilities[key]}
    />
  ))}
</div>
```

The `CapabilityBadge` D-04 logic handles missing/null/undefined (returns null) so this is safe even if a backend omits a key.

### DO NOT change:
- The trigger/list/item style objects (copy verbatim)
- Click-outside / Escape handlers
- Active-highlight logic
- Unavailable-item rendering (opacity 0.4 + title={reason})

After saving, run `cd frontend && npx tsc --noEmit` — must exit 0. The DetectorBackend `capabilities: Record<string, string | number | boolean>` type from Plan 04 means `backend.capabilities['framework']` is typed as `string | number | boolean | undefined`, which is exactly what CapabilityBadge's `value` prop accepts.
  </action>
  <acceptance_criteria>
    - File `frontend/src/components/DetectorDropdown.tsx` exists
    - `grep -q "export function DetectorDropdown" frontend/src/components/DetectorDropdown.tsx`
    - `grep -q "useDetectorStore" frontend/src/components/DetectorDropdown.tsx`
    - `grep -q "from '../stores/detectorStore'" frontend/src/components/DetectorDropdown.tsx`
    - `grep -q "'framework'" frontend/src/components/DetectorDropdown.tsx`
    - `grep -q "'license'" frontend/src/components/DetectorDropdown.tsx`
    - `grep -q "'cpu_latency_hint_ms'" frontend/src/components/DetectorDropdown.tsx`
    - `grep -q "CapabilityBadge" frontend/src/components/DetectorDropdown.tsx`
    - `! grep -q "useSlamStore" frontend/src/components/DetectorDropdown.tsx`
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && test -f src/components/DetectorDropdown.tsx && grep -q "useDetectorStore" src/components/DetectorDropdown.tsx && grep -q "'cpu_latency_hint_ms'" src/components/DetectorDropdown.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>DetectorDropdown functional clone of AlgorithmDropdown with D-06 3-key badge pack; tsc clean; ready for DetectorSection mount.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Clone DetectorDropdown → LifterDropdown with lifter-specific store selectors</name>
  <read_first>
    - frontend/src/components/DetectorDropdown.tsx (JUST-written from Task 1 — clone template)
    - frontend/src/stores/detectorStore.ts (LifterBackend shape, lifters/activeLifter/activeLifterDisplay state + setActiveLifter setter — Plan 04)
    - src/perception/lifters/median_depth.py lines 117-122 (real lifter CAPABILITIES keys: requires_depth, requires_point_cloud, outputs_oriented, license)
    - .planning/phases/03-frontend-picker-and-ui/03-CONTEXT.md §D-07 (LifterDropdown inline inside DetectorSection — this plan only creates the component)
  </read_first>
  <files>frontend/src/components/LifterDropdown.tsx</files>
  <action>
Create `frontend/src/components/LifterDropdown.tsx` by cloning DetectorDropdown. Changes:

### Find-and-replace:
- `DetectorDropdown` → `LifterDropdown`
- `backends` (state field) → `lifters`
- `activeBackend` → `activeLifter`
- `activeDisplay` → `activeLifterDisplay`
- `backend` (loop variable) → `lifter`
- `'Loading backends...'` → `'Loading lifters...'`
- `'No backends available'` → `'No lifters available'`

### Lifter-specific badge keys (D-05-analogous):
```typescript
const LIFTER_BADGE_KEYS: Array<keyof typeof lifter.capabilities> = [
  'license',
  'outputs_oriented',
];
```

Only two badge keys for lifters — license (e.g., `MIT`) + outputs_oriented (boolean — true for PCA-OBB/Phase 4, false for MedianDepth). D-04's rendering rules handle both types correctly.

### onSelect prop unchanged — caller supplies `(name: string) => void`.

After saving, `cd frontend && npx tsc --noEmit` must exit 0.

DO NOT add any visibility gate here (Plan 10 wraps the component in a conditional `{activeBackendInfo?.capabilities?.outputs_3d_natively === false && <LifterDropdown />}`). The component itself is always render-ready.
  </action>
  <acceptance_criteria>
    - File `frontend/src/components/LifterDropdown.tsx` exists
    - `grep -q "export function LifterDropdown" frontend/src/components/LifterDropdown.tsx`
    - `grep -q "s.lifters" frontend/src/components/LifterDropdown.tsx`
    - `grep -q "s.activeLifter" frontend/src/components/LifterDropdown.tsx`
    - `grep -q "s.activeLifterDisplay" frontend/src/components/LifterDropdown.tsx`
    - `grep -q "'license'" frontend/src/components/LifterDropdown.tsx`
    - `grep -q "'outputs_oriented'" frontend/src/components/LifterDropdown.tsx`
    - `! grep -q "DetectorDropdown" frontend/src/components/LifterDropdown.tsx` (renamed, no leftover)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && test -f src/components/LifterDropdown.tsx && grep -q "export function LifterDropdown" src/components/LifterDropdown.tsx && grep -q "s.lifters" src/components/LifterDropdown.tsx && grep -q "'outputs_oriented'" src/components/LifterDropdown.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>LifterDropdown clone exists with lifter-specific store selectors + 2-key badge pack; tsc clean; ready for DetectorSection conditional mount.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend.display / backend.reason (server-authored) → React render | Registry authors trusted; React auto-escapes strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | T (Tampering) | Dropdown renders backend.capabilities values as badge text | accept | Registry authors trusted (internal repo); React auto-escapes JSX interpolation. No dangerouslySetInnerHTML. |
| T-03-19 | I (Info Disclosure) | `title={reason}` tooltip exposes install-hint strings | accept | Install hints are user-facing by design (e.g., "run `make download-models`"). |
</threat_model>

<verification>
```bash
cd frontend && npx tsc --noEmit
```

Must exit 0.
</verification>

<success_criteria>
- [ ] DetectorDropdown.tsx exists (≥130 lines)
- [ ] LifterDropdown.tsx exists (≥120 lines)
- [ ] Both use useDetectorStore (not useSlamStore)
- [ ] DetectorDropdown shows 3-key badge pack (framework, license, cpu_latency_hint_ms)
- [ ] LifterDropdown shows 2-key badge pack (license, outputs_oriented)
- [ ] Both handle unavailable entries (greyed + reason tooltip)
- [ ] `cd frontend && npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-picker-and-ui/03-07-SUMMARY.md` documenting the clone lineage (AlgorithmDropdown → DetectorDropdown → LifterDropdown), the badge key divergence, and confirmation that neither component owns the visibility gate (Plan 10 owns it).
</output>
