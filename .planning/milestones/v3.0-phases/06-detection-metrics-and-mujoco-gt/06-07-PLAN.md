---
phase: 6
plan: 7
type: execute
wave: 1
depends_on: []
files_modified:
  - data/scenes/scene_office1_gt.yaml
autonomous: true
requirements:
  - DET-METRICS-02
tags: [perception, metrics, mujoco, ground-truth, data]
must_haves:
  truths:
    - "data/scenes/scene_office1_gt.yaml exists and parses as YAML"
    - "Every body name in the YAML resolves to a valid body_id in scene_office1.xml (mj_name2id >= 0)"
    - "Every resolved body has at least one geom (for geom_xpos lookup)"
    - "File covers `chair` and `dining_table` classes at minimum (per RESEARCH Example 2 draft)"
  artifacts:
    - path: "data/scenes/scene_office1_gt.yaml"
      provides: "COCO class → MuJoCo body-name mapping for scene_office1 per CONTEXT D-08"
      contains: "chair:"
      min_lines: 20
  key_links:
    - from: "Plan 10 coordinator pump"
      to: "data/scenes/scene_office1_gt.yaml"
      via: "MuJoCoGTExtractor(mapping_yaml_path, mj_model, mj_data)"
      pattern: "scene_office1_gt\\.yaml"
---

<objective>
Commit the canonical `data/scenes/scene_office1_gt.yaml` mapping file that pairs COCO class names to the specific MuJoCo body names in `data/scenes/scene_office1.xml` (CONTEXT D-08). Validates every body name resolves via `mj_name2id` and has at least one geom — a missing body breaks SC#2 at runtime.

Purpose: DET-METRICS-02 data requirement. RESEARCH §Example 2 captures the verified live body-names + geom_xpos values; this plan commits that draft as a tracked source file and runs a validation script to catch typos before any downstream plan tries to load it.
Output: One committed YAML file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-detection-metrics-and-mujoco-gt/06-CONTEXT.md
@.planning/phases/06-detection-metrics-and-mujoco-gt/06-RESEARCH.md
@data/scenes/scene_office1.xml

<interfaces>
<!-- RESEARCH §Example 2 draft content (verified live via mj_name2id + data.geom_xpos): -->

```yaml
# COCO class name -> list of MuJoCo body names representing instances of that class.
# Body names verified live via mj_name2id against scene_office1.xml.
#
# CRITICAL: extractor reads data.geom_xpos[first_geom_of_body], NOT data.xpos
# (RESEARCH F1) — all bodies in this scene have body-origin at world (0,0,0).

chair:
  - body_BezierCurve_009_SwivlChair                         # geom_xpos ~ (0.68, -2.44, 0.51)
  - body_BezierCurve_009_SwivlChair_BLACK                   # geom_xpos ~ (0.71, 0.07, 0.72)
  - body_Chocofur_free_12_plastic_cycles_013_BrownChair     # geom_xpos ~ (-0.36, 7.99, 0.47)

dining_table:
  - body_Plane_029_meetingTable                             # geom_xpos ~ (-0.35, 7.99, 0.55)
  - body_Cube_013_BigWhiteTable                             # geom_xpos ~ (1.30, 0.89, 0.59)
  - body_Cube_007_woodenDesk                                # geom_xpos ~ (-0.06, -6.13, 0.45)
  - body_Cube_017_SmallWhiteTable_low_001                   # geom_xpos ~ (1.87, -1.51, 0.50)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scene_office1_gt.yaml + validate every body resolves</name>
  <files>data/scenes/scene_office1_gt.yaml</files>
  <read_first>
    - .planning/phases/06-detection-metrics-and-mujoco-gt/06-RESEARCH.md §Example 2 (verbatim draft content)
    - .planning/phases/06-detection-metrics-and-mujoco-gt/06-CONTEXT.md §D-08
    - data/scenes/scene_office1.xml (confirm body names — grep for `<body.*name=` on lines 1257-1317)
  </read_first>
  <action>
    ## Step 1 — Audit body names in scene_office1.xml

    Run:
    ```
    grep -nE '<body[[:space:]]+name="(body_BezierCurve_009_SwivlChair|body_BezierCurve_009_SwivlChair_BLACK|body_Chocofur_free_12_plastic_cycles_013_BrownChair|body_Plane_029_meetingTable|body_Cube_013_BigWhiteTable|body_Cube_007_woodenDesk|body_Cube_017_SmallWhiteTable_low_001)"' data/scenes/scene_office1.xml
    ```

    All 7 names MUST appear. If any does not match, spawn `python -c "import mujoco; m=mujoco.MjModel.from_xml_path('data/scenes/scene_office1.xml'); names=[mujoco.mj_id2name(m, mujoco.mjtObj.mjOBJ_BODY, i) for i in range(m.nbody)]; print('\n'.join(n for n in names if n))"` to enumerate the actual names, and update the YAML to match exactly. Do NOT guess.

    ## Step 2 — Write `data/scenes/scene_office1_gt.yaml`

    Write the file with exactly the content shown in the interfaces block (header comments + `chair:` + `dining_table:` sections). Use 2-space indentation. No trailing whitespace. ASCII only. Preserve the inline geom_xpos comments — they document expected values for future debugging.

    ## Step 3 — Validate every body resolves (one-shot script, do NOT commit)

    Run this validation:
    ```python
    python -c "
    import mujoco, yaml
    m = mujoco.MjModel.from_xml_path('data/scenes/scene_office1.xml')
    mapping = yaml.safe_load(open('data/scenes/scene_office1_gt.yaml'))
    problems = []
    for cls, bodies in mapping.items():
        for b in bodies:
            bid = mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_BODY, b)
            if bid < 0:
                problems.append(f'MISSING: {cls} -> {b}')
                continue
            has_geom = any(m.geom_bodyid[g] == bid for g in range(m.ngeom))
            if not has_geom:
                problems.append(f'NO_GEOM: {cls} -> {b}')
    if problems:
        raise SystemExit('Validation failed:\n' + '\n'.join(problems))
    print('OK: {} classes, {} bodies resolved'.format(len(mapping), sum(len(v) for v in mapping.values())))
    "
    ```

    If it fails, fix the YAML (usually a typo in a body name) until it prints `OK: ...`.
  </action>
  <verify>
    <automated>python -c "
import mujoco, yaml
m = mujoco.MjModel.from_xml_path('data/scenes/scene_office1.xml')
mapping = yaml.safe_load(open('data/scenes/scene_office1_gt.yaml'))
assert isinstance(mapping, dict) and mapping, 'mapping must be non-empty dict'
for cls, bodies in mapping.items():
    assert isinstance(bodies, list) and bodies, f'{cls} must be non-empty list'
    for b in bodies:
        bid = mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_BODY, b)
        assert bid >= 0, f'body {b!r} (class {cls!r}) missing from scene_office1.xml'
        assert any(m.geom_bodyid[g] == bid for g in range(m.ngeom)), f'body {b!r} has no geom'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - File `data/scenes/scene_office1_gt.yaml` exists
    - `python -c "import yaml; d=yaml.safe_load(open('data/scenes/scene_office1_gt.yaml')); assert 'chair' in d and 'dining_table' in d"` exits 0
    - Every body name in the YAML resolves via `mj_name2id` to a non-negative id
    - Every resolved body has at least one geom
    - File is ASCII-only: `file -b --mime-encoding data/scenes/scene_office1_gt.yaml` contains `us-ascii` or `utf-8`
    - Each chair/dining_table entry has its geom_xpos comment (human-debugging aid)
  </acceptance_criteria>
  <done>scene_office1_gt.yaml committed; every body resolves via mj_name2id; every resolved body has a geom; chair + dining_table classes present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → runtime | Committed YAML is loaded by MuJoCoGTExtractor; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-04 | Tampering (YAML RCE) | mapping file content | transfer | Plan 05 owns the `yaml.safe_load` enforcement; this plan just provides the data |
| T-6-08 | DoS (bad data crashes coordinator boot) | scene_office1_gt.yaml content | mitigate | Validation script in acceptance criteria catches typos at planning time. Plan 08 wraps `MuJoCoGTExtractor.__init__` in try/except for graceful degradation |
</threat_model>

<verification>
- YAML parses cleanly
- Every body resolves to a valid body_id with a geom
- File covers at least `chair` and `dining_table`
</verification>

<success_criteria>
The automated validation command exits 0 with `OK` printed.
</success_criteria>

<output>
Create `.planning/phases/06-detection-metrics-and-mujoco-gt/06-07-SUMMARY.md`
</output>
