---
phase: 6
plan: 2
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/metrics/__init__.py
  - tests/metrics/test_detection_metrics_tracker.py
  - tests/metrics/test_mujoco_gt.py
  - tests/contract/__init__.py
  - tests/contract/test_no_map_in_ui.py
  - tests/contract/test_no_map_in_payload.py
  - tests/integration/test_detections_export.py
  - tests/integration/test_rss_smoke_backends.py
  - tests/test_main_args.py
autonomous: true
requirements:
  - DET-METRICS-01
  - DET-METRICS-02
  - DET-METRICS-03
  - DET-METRICS-04
  - DET-METRICS-05
tags: [perception, metrics, testing, scaffolding]
must_haves:
  truths:
    - "pytest collection (`pytest --collect-only -q`) passes without ImportError or CollectError"
    - "Every Phase 6 test file exists as a skip-stub that names its target requirement"
    - "`tests/contract/` is a collectable pytest package (fresh — did not exist before this plan)"
  artifacts:
    - path: "tests/metrics/__init__.py"
      provides: "pytest package marker for metrics unit tests"
    - path: "tests/metrics/test_detection_metrics_tracker.py"
      provides: "skip-stub for DET-METRICS-01"
      contains: "pytest.skip"
    - path: "tests/metrics/test_mujoco_gt.py"
      provides: "skip-stub for DET-METRICS-02"
      contains: "pytest.skip"
    - path: "tests/contract/__init__.py"
      provides: "pytest package marker (NEW directory per RESEARCH §F5)"
    - path: "tests/contract/test_no_map_in_ui.py"
      provides: "skip-stub for DET-METRICS-03 grep invariant"
      contains: "pytest.skip"
    - path: "tests/contract/test_no_map_in_payload.py"
      provides: "skip-stub for DET-METRICS-03 payload runtime guard"
      contains: "pytest.skip"
    - path: "tests/integration/test_detections_export.py"
      provides: "skip-stub for DET-METRICS-04"
      contains: "pytest.skip"
    - path: "tests/integration/test_rss_smoke_backends.py"
      provides: "skip-stub for DET-METRICS-05 parametrized RSS test"
      contains: "pytest.skip"
    - path: "tests/test_main_args.py"
      provides: "skip-stub for --labeled-eval-set CLI reservation test"
      contains: "pytest.skip"
  key_links:
    - from: "pytest runner"
      to: "Phase 6 test files"
      via: "pytest collection"
      pattern: "pytest\\.skip.*Wave 0 scaffold"
---

<objective>
Create skip-stubs for every Phase 6 test file listed in 06-VALIDATION.md Wave 0 Requirements, so pytest collection passes *before* any implementation lands. Each stub module calls `pytest.skip(..., allow_module_level=True)` with a message naming the target requirement ID and the plan that will replace the stub.

Purpose: GSD Nyquist validation protocol — every task emits an `<automated>` command, but implementation plans arrive AFTER Wave 0. The collection-passes-but-everything-skips state lets downstream plans flip stubs to real tests incrementally without breaking CI.
Output: 9 skeleton Python files covering all 5 DET-METRICS-* requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-detection-metrics-and-mujoco-gt/06-VALIDATION.md
@.planning/phases/06-detection-metrics-and-mujoco-gt/06-RESEARCH.md
@.planning/phases/06-detection-metrics-and-mujoco-gt/06-CONTEXT.md

<interfaces>
<!-- Existing scaffold precedent: Phase 5 Plan 04 skip-stubs -->
<!-- Pattern (exact canonical stub body): -->

```python
"""Skip-stub for {REQ-ID}: {short description}.

Replaced in full by Plan {XX} (Wave {N}).
"""
import pytest
pytest.skip(
    "Wave 0 scaffold — implementation lands in 06-{XX}-PLAN.md",
    allow_module_level=True,
)
```

<!-- Phase 5 pattern, for reference, at tests/integration/test_detector_crash_fallback.py (existing) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/metrics/ package + two skip-stubs</name>
  <files>
    tests/metrics/__init__.py
    tests/metrics/test_detection_metrics_tracker.py
    tests/metrics/test_mujoco_gt.py
  </files>
  <read_first>
    - .planning/phases/06-detection-metrics-and-mujoco-gt/06-VALIDATION.md §Wave 0 Requirements
    - tests/perception/ (existing sibling package — confirms empty `__init__.py` is the established convention)
  </read_first>
  <action>
    1. Create `tests/metrics/__init__.py` as an EMPTY file (0 bytes — package marker only).

    2. Create `tests/metrics/test_detection_metrics_tracker.py` with EXACTLY this content:

       ```python
       """Skip-stub for DET-METRICS-01: DetectionMetricsTracker unit tests.

       Replaced in full by 06-04-PLAN.md (Wave 1). Implementation lands alongside
       `src/metrics/detection_metrics_tracker.py` (CONTEXT D-01, D-02, D-03).
       """
       import pytest
       pytest.skip(
           "Wave 0 scaffold — DetectionMetricsTracker tests land in 06-04-PLAN.md",
           allow_module_level=True,
       )
       ```

    3. Create `tests/metrics/test_mujoco_gt.py` with EXACTLY this content:

       ```python
       """Skip-stub for DET-METRICS-02: MuJoCoGTExtractor unit tests.

       Replaced in full by 06-05-PLAN.md (Wave 1). Fixture:
       tests/perception/fixtures/scene_rotated_chair.xml (Phase 4 heritage —
       labeled chair body with explicit `pos=` attribute).
       """
       import pytest
       pytest.skip(
           "Wave 0 scaffold — MuJoCoGTExtractor tests land in 06-05-PLAN.md",
           allow_module_level=True,
       )
       ```

    Use ASCII only. No trailing whitespace. Match the docstring → pytest.skip order exactly.
  </action>
  <verify>
    <automated>pytest tests/metrics/ --collect-only -q 2>&amp;1 | grep -E "(skipped|collected)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/metrics/__init__.py` exists and is 0 bytes (`wc -c tests/metrics/__init__.py` → `0 tests/metrics/__init__.py`)
    - `pytest tests/metrics/test_detection_metrics_tracker.py -q` exits 0 with output containing `skipped` (1 skip at module level)
    - `pytest tests/metrics/test_mujoco_gt.py -q` exits 0 with output containing `skipped`
    - `grep -c 'allow_module_level=True' tests/metrics/test_*.py` returns `2` (one per stub)
    - `grep -c 'DET-METRICS-01' tests/metrics/test_detection_metrics_tracker.py` returns `1`
    - `grep -c 'DET-METRICS-02' tests/metrics/test_mujoco_gt.py` returns `1`
  </acceptance_criteria>
  <done>tests/metrics/ package exists with two collectable-but-skipped test modules naming their target requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/contract/ package + two skip-stubs (NEW directory per F5)</name>
  <files>
    tests/contract/__init__.py
    tests/contract/test_no_map_in_ui.py
    tests/contract/test_no_map_in_payload.py
  </files>
  <read_first>
    - .planning/phases/06-detection-metrics-and-mujoco-gt/06-RESEARCH.md §F5 (tests/contract/ directory does not exist)
    - .planning/phases/06-detection-metrics-and-mujoco-gt/06-CONTEXT.md §D-10 (grep test pattern)
  </read_first>
  <action>
    1. Confirm `tests/contract/` does NOT exist yet (RESEARCH §F5). Create the directory implicitly by writing `tests/contract/__init__.py` as an EMPTY 0-byte file.

    2. Create `tests/contract/test_no_map_in_ui.py` with EXACTLY this content:

       ```python
       """Skip-stub for DET-METRICS-03 (SC#3): `mAP` absent in frontend/src/.

       Replaced in full by 06-12-PLAN.md (Wave 5). Implementation runs
       `git grep -n mAP -- frontend/src/` with cwd=REPO_ROOT and asserts
       returncode == 1 (no match). See CONTEXT.md D-10.
       """
       import pytest
       pytest.skip(
           "Wave 0 scaffold — grep invariant test lands in 06-12-PLAN.md",
           allow_module_level=True,
       )
       ```

    3. Create `tests/contract/test_no_map_in_payload.py` with EXACTLY this content:

       ```python
       """Skip-stub for DET-METRICS-03 (runtime guard): `mAP`/`map_50`/`map_75`
       keys absent from WebStreamingViz._update_stats payload.

       Replaced in full by 06-08-PLAN.md (Wave 2). Implementation constructs
       a stats payload via a real WebStreamingViz instance and asserts
       forbidden keys are not present at any nesting depth.
       """
       import pytest
       pytest.skip(
           "Wave 0 scaffold — payload runtime guard test lands in 06-08-PLAN.md",
           allow_module_level=True,
       )
       ```

    No trailing whitespace. ASCII only.
  </action>
  <verify>
    <automated>test -d tests/contract &amp;&amp; pytest tests/contract/ --collect-only -q 2>&amp;1 | grep -E "(skipped|collected)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/contract/` exists as a directory
    - `tests/contract/__init__.py` exists and is 0 bytes
    - `pytest tests/contract/ -q` exits 0 with `2 skipped` in output
    - `grep -l 'DET-METRICS-03' tests/contract/*.py | wc -l` returns `2`
    - `grep -c 'allow_module_level=True' tests/contract/test_no_map_in_ui.py` returns `1`
  </acceptance_criteria>
  <done>tests/contract/ directory exists as a new pytest package with two DET-METRICS-03-tagged skip-stubs.</done>
</task>

<task type="auto">
  <name>Task 3: Create integration + main-args skip-stubs</name>
  <files>
    tests/integration/test_detections_export.py
    tests/integration/test_rss_smoke_backends.py
    tests/test_main_args.py
  </files>
  <read_first>
    - tests/integration/ (existing sibling — confirm `__init__.py` present; do NOT recreate)
    - .planning/phases/06-detection-metrics-and-mujoco-gt/06-CONTEXT.md §D-14, §D-15, §D-10 (--labeled-eval-set reservation)
  </read_first>
  <action>
    1. Verify `tests/integration/__init__.py` already exists (ls command). Do NOT recreate.

    2. Create `tests/integration/test_detections_export.py` with EXACTLY this content:

       ```python
       """Skip-stub for DET-METRICS-04: /api/detections/export JSONL round-trip.

       Replaced in full by 06-09-PLAN.md (Wave 2). Implementation spawns a
       coordinator for 30 frames against scene_office1.xml, hits the export
       endpoint, and round-trips every line through `OrientedBox3D.from_wire`.
       See CONTEXT.md D-14.
       """
       import pytest
       pytest.skip(
           "Wave 0 scaffold — export round-trip test lands in 06-09-PLAN.md",
           allow_module_level=True,
       )
       ```

    3. Create `tests/integration/test_rss_smoke_backends.py` with EXACTLY this content:

       ```python
       """Skip-stub for DET-METRICS-05: parametrized RSS smoke across backends.

       Replaced in full by 06-13-PLAN.md (Wave 5). Implementation uses
       @pytest.mark.parametrize over ["yolov11", "rtdetrv2",
       pytest.param("boxer", marks=pytest.mark.slow_boxer)] per CONTEXT
       D-15, with honest-skip on missing model/venv artifacts. OWLv2 is
       intentionally absent — superseded by Phase 5 D-10 (see RESEARCH F4).
       """
       import pytest
       pytest.skip(
           "Wave 0 scaffold — parametrized RSS smoke lands in 06-13-PLAN.md",
           allow_module_level=True,
       )
       ```

    4. Create `tests/test_main_args.py` with EXACTLY this content:

       ```python
       """Skip-stub for DET-METRICS-03: --labeled-eval-set CLI reservation.

       Replaced in full by 06-10-PLAN.md (Wave 3). Implementation asserts
       `argus --labeled-eval-set /tmp/foo` raises NotImplementedError with
       the exact CONTEXT D-10 message.
       """
       import pytest
       pytest.skip(
           "Wave 0 scaffold — CLI reservation test lands in 06-10-PLAN.md",
           allow_module_level=True,
       )
       ```

    No trailing whitespace. ASCII only.
  </action>
  <verify>
    <automated>pytest tests/integration/test_detections_export.py tests/integration/test_rss_smoke_backends.py tests/test_main_args.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist
    - `pytest tests/integration/test_detections_export.py -q` outputs `skipped`
    - `pytest tests/integration/test_rss_smoke_backends.py -q` outputs `skipped`
    - `pytest tests/test_main_args.py -q` outputs `skipped`
    - `grep -c 'RESEARCH F4' tests/integration/test_rss_smoke_backends.py` returns `1` (OWLv2 supersession documented)
    - `grep -c 'DET-METRICS-04' tests/integration/test_detections_export.py` returns `1`
    - `grep -c 'DET-METRICS-05' tests/integration/test_rss_smoke_backends.py` returns `1`
  </acceptance_criteria>
  <done>Three skip-stubs exist and collect-pass; each names its target requirement and the plan that replaces it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Scaffold files only — no runtime code introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | — | — | N/A | Skip-stubs contain no runtime logic beyond `pytest.skip()` |
</threat_model>

<verification>
- `pytest tests/metrics tests/contract tests/integration/test_detections_export.py tests/integration/test_rss_smoke_backends.py tests/test_main_args.py -q` exits 0 with 9 `skipped` entries
- `tests/contract/` directory now exists (previously MISSING per F5)
- No production code modified
</verification>

<success_criteria>
All 9 files exist. `pytest --collect-only -q tests/metrics tests/contract tests/integration tests/test_main_args.py` collects without error. Every stub skips at module level with a message naming the replacing plan.
</success_criteria>

<output>
Create `.planning/phases/06-detection-metrics-and-mujoco-gt/06-02-SUMMARY.md`
</output>
