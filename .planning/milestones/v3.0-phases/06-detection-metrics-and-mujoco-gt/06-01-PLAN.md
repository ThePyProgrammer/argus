---
phase: 6
plan: 1
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
autonomous: true
requirements:
  - DET-METRICS-02
tags: [perception, metrics, dependencies]
must_haves:
  truths:
    - "PyYAML is declared in pyproject.toml [project.optional-dependencies].perception with >=6.0 pin"
  artifacts:
    - path: "pyproject.toml"
      provides: "Explicit PyYAML declaration in perception extra"
      contains: "PyYAML>=6.0"
  key_links:
    - from: "src/metrics/mujoco_gt.py (future Plan 05)"
      to: "PyYAML (installed via perception extra)"
      via: "`import yaml` + `yaml.safe_load`"
      pattern: "PyYAML>=6\\.0"
---

<objective>
Declare PyYAML explicitly in `pyproject.toml` under `[project.optional-dependencies].perception` so downstream plans (Plan 05 `MuJoCoGTExtractor`) can rely on `import yaml` being an officially-declared dependency.

Purpose: RESEARCH §F6 — PyYAML 6.0.3 is installed system-wide but not declared in the project manifest. Phase 6 introduces the first runtime code path that depends on it (`src/metrics/mujoco_gt.py` uses `yaml.safe_load` for the scene GT mapping). Declaring it explicitly prevents a "works on my machine" failure when another dev installs only the declared `perception` extras.
Output: One-line edit to `pyproject.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-detection-metrics-and-mujoco-gt/06-CONTEXT.md
@.planning/phases/06-detection-metrics-and-mujoco-gt/06-RESEARCH.md
@pyproject.toml

<interfaces>
<!-- Current perception extra (from pyproject.toml:28-36) -->
[project.optional-dependencies]
perception = [
    "torch>=2.10.0",
    "transformers>=5.3.0",
    "ultralytics>=8.4.24",
    "scikit-learn>=1.4.0",
    "onnx>=1.17.0",
    "onnxruntime>=1.19.0",
]
<!-- Phase 6 adds: "PyYAML>=6.0" as the 7th entry -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PyYAML to perception extra</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (lines 28-36 — the `perception` extra block)
    - .planning/phases/06-detection-metrics-and-mujoco-gt/06-RESEARCH.md §F6 + §Standard Stack
  </read_first>
  <action>
    Open `pyproject.toml`. Locate the `[project.optional-dependencies]` table's `perception` list (lines 29-36). Append `"PyYAML>=6.0",` as the final entry of the list, preserving the existing trailing comma style. The final block must read exactly:

    ```toml
    perception = [
        "torch>=2.10.0",
        "transformers>=5.3.0",
        "ultralytics>=8.4.24",
        "scikit-learn>=1.4.0",
        "onnx>=1.17.0",
        "onnxruntime>=1.19.0",
        "PyYAML>=6.0",
    ]
    ```

    Do NOT touch `dependencies`, `[tool.pytest.ini_options]`, or any other section. Do NOT reorder existing entries.
  </action>
  <verify>
    <automated>grep -q '"PyYAML>=6.0"' pyproject.toml &amp;&amp; python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'PyYAML>=6.0' in d['project']['optional-dependencies']['perception'], 'PyYAML not in perception extra'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PyYAML>=6.0' pyproject.toml` returns `1` (exactly one declaration)
    - `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` exits 0 (TOML still parses)
    - `perception` list length grows from 6 → 7 entries
    - `dependencies` list is unchanged (same length, same contents)
  </acceptance_criteria>
  <done>PyYAML explicitly declared in perception extra; pyproject.toml parses; no other sections modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Build manifest edit — no runtime trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | — | — | N/A | Dependency declaration only; no runtime attack surface introduced |
| T-6-04 | Tampering | yaml.safe_load usage (Plan 05) | transfer | Plan 05 owns the `yaml.safe_load` invariant; this plan only declares the dependency so the invariant can be enforced there |
</threat_model>

<verification>
- pyproject.toml parses via `tomllib`
- PyYAML>=6.0 listed exactly once in perception extra
- No diff outside the perception list
</verification>

<success_criteria>
After execution: `python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['optional-dependencies']['perception'][-1] == 'PyYAML>=6.0'"` exits 0.
</success_criteria>

<output>
Create `.planning/phases/06-detection-metrics-and-mujoco-gt/06-01-SUMMARY.md`
</output>
