---
phase: 04
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - tests/perception/fixtures/scene_rotated_chair.xml
autonomous: true
requirements:
  - DET-3D-01
tags:
  - dependency
  - fixture
  - wave-0
user_setup: []

must_haves:
  truths:
    - "scikit-learn is installable as part of the perception extra (D-02)."
    - "tests/perception/fixtures/scene_rotated_chair.xml loads via mujoco.MjModel.from_xml_path without error."
    - "The MuJoCo fixture exposes a body named 'chair' at a non-zero yaw (D-11)."
    - "The fixture is self-contained — no external mesh assets required."
  artifacts:
    - path: "pyproject.toml"
      provides: "scikit-learn>=1.4.0 added to perception extra"
      contains: "scikit-learn"
      min_lines: 30
    - path: "tests/perception/fixtures/scene_rotated_chair.xml"
      provides: "MuJoCo scene with chair body + depth-capable camera"
      contains: "body name=\"chair\""
      min_lines: 30
  key_links:
    - from: "pyproject.toml"
      to: "src/perception/lifters/point_cluster.py"
      via: "perception optional dependency exposes sklearn.cluster.DBSCAN"
      pattern: "scikit-learn"
    - from: "tests/perception/fixtures/scene_rotated_chair.xml"
      to: "tests/perception/test_point_cluster_lifter.py"
      via: "MjModel.from_xml_path fixture path + data.body('chair').xpos/xquat access"
      pattern: "scene_rotated_chair\\.xml"
---

<objective>
Wave 0 scaffolding plan #1: add the `scikit-learn>=1.4.0` dependency required for
`PointClusterLifter` (D-02) and create the MuJoCo GT fixture with a known-yaw
chair body consumed by the SC#1 integration test (D-11). Both artifacts must
exist BEFORE Plan 04 (PointClusterLifter) can run, and BEFORE Plan 07 (MuJoCo
integration test) can resolve `mj_data.body("chair").xpos`/`.xquat`.

Purpose: unblock downstream plans by shipping the data + dep surface they expect.
Output: one `pyproject.toml` diff and one new MuJoCo XML fixture file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-3d-obb-pipeline/04-CONTEXT.md
@.planning/phases/04-real-3d-obb-pipeline/04-RESEARCH.md
@pyproject.toml

<interfaces>
Current perception extra on pyproject.toml:29:
```toml
[project.optional-dependencies]
perception = ["torch>=2.10.0", "transformers>=5.3.0", "ultralytics>=8.4.24"]
```

CameraIntrinsics.from_fov (src/bridge/sensor_types.py:88-93) — referenced so
the fixture camera uses the same vertical FOV convention that
`CameraIntrinsics.from_fov(w, h)` assumes at main.py:641:
```python
@classmethod
def from_fov(cls, width: int, height: int, fov_degrees: float = 70.0) -> "CameraIntrinsics":
    fov_rad = math.radians(fov_degrees)
    fx = height / (2.0 * math.tan(fov_rad / 2.0))
    return cls(fx=fx, fy=fx, cx=width / 2.0, cy=height / 2.0, width=width, height=height)
```
The fixture's camera element MUST declare `fovy="70"` so the GT unprojection in
the Plan 07 integration test matches the runtime `CameraIntrinsics.from_fov`
convention.

MuJoCo camera-fovy doc (canonical): https://mujoco.readthedocs.io/en/stable/XMLreference.html#camera-fovy
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add scikit-learn>=1.4.0 to the perception extra</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current perception extra at line 29)
    - .planning/phases/04-real-3d-obb-pipeline/04-CONTEXT.md §D-02
    - .planning/phases/04-real-3d-obb-pipeline/04-RESEARCH.md §Standard Stack table (version pin rationale)
  </read_first>
  <action>
    Edit `pyproject.toml` at line 29. Change:
    ```
    perception = ["torch>=2.10.0", "transformers>=5.3.0", "ultralytics>=8.4.24"]
    ```
    to:
    ```
    perception = [
        "torch>=2.10.0",
        "transformers>=5.3.0",
        "ultralytics>=8.4.24",
        "scikit-learn>=1.4.0",
    ]
    ```
    Notes:
    - The `>=1.4.0` floor is documented in 04-RESEARCH.md §Standard Stack (sklearn
      1.4.0 released Jan 2024; 1.8.0 is latest). Per D-02.
    - Do NOT reorder or modify the other three entries — torch/transformers/ultralytics
      versions are locked by Phase 1 and 2 decisions.
    - Do NOT add `scikit-learn` to top-level `dependencies`. It belongs ONLY to
      the perception extra (mirror: ultralytics is perception-only).
  </action>
  <verify>
    <automated>python -c "import tomllib; d=tomllib.loads(open('pyproject.toml').read()); extras=d['project']['optional-dependencies']['perception']; assert any(e.startswith('scikit-learn') and '>=1.4.0' in e for e in extras), f'scikit-learn>=1.4.0 missing from perception extra: {extras}'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'scikit-learn>=1.4.0' pyproject.toml` returns `1`.
    - `python -c "import tomllib; tomllib.loads(open('pyproject.toml').read())"` exits 0 (valid TOML).
    - Top-level `dependencies` list in pyproject.toml does NOT contain `scikit-learn`.
    - The four perception entries (torch, transformers, ultralytics, scikit-learn) are all present.
  </acceptance_criteria>
  <done>pyproject.toml parses as valid TOML and advertises scikit-learn>=1.4.0 under the perception optional extra.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create MuJoCo scene_rotated_chair.xml fixture (D-11)</name>
  <files>tests/perception/fixtures/scene_rotated_chair.xml</files>
  <read_first>
    - .planning/phases/04-real-3d-obb-pipeline/04-CONTEXT.md §D-11 (fixture spec) + §Specifics (chair body name="chair", 480x640 RGBD)
    - .planning/phases/04-real-3d-obb-pipeline/04-RESEARCH.md §Open Question 6 (compound-primitive chair decision)
    - .planning/phases/04-real-3d-obb-pipeline/04-RESEARCH.md §Assumptions Log A4 (YOLO recognition risk + fallback)
    - src/bridge/sensor_types.py:88-93 (CameraIntrinsics.from_fov defaults fov_degrees=70.0)
    - https://mujoco.readthedocs.io/en/stable/XMLreference.html#camera-fovy (MuJoCo camera-fovy reference)
    - https://mujoco.readthedocs.io/en/stable/XMLreference.html#body (MuJoCo body reference)
  </read_first>
  <action>
    Create `tests/perception/fixtures/scene_rotated_chair.xml` as a self-contained
    MuJoCo scene that Plan 07's integration test will render with a depth-capable
    camera at the same intrinsics that `CameraIntrinsics.from_fov(640, 480, 70.0)`
    encodes.

    Required XML structure (per D-11 + 04-RESEARCH Open Question 6 compound-primitive
    decision — NO external mesh dependency):

    ```xml
    <mujoco model="scene_rotated_chair">
      <compiler angle="radian" coordinate="local"/>
      <option timestep="0.002" gravity="0 0 -9.81"/>

      <visual>
        <headlight ambient="0.4 0.4 0.4" diffuse="0.6 0.6 0.6"/>
      </visual>

      <worldbody>
        <!-- Floor plane -->
        <geom name="floor" type="plane" size="5 5 0.1" rgba="0.8 0.8 0.8 1"/>

        <!-- Robot-side camera: placed 2.0m in front of the chair looking at it.
             fovy=70 MUST match CameraIntrinsics.from_fov default — SC#1 integration
             test pipes this frame through runtime intrinsics. 480x640 matches the
             CameraStrip resolution used by DetectorWorker._intrinsics. -->
        <camera name="robot_cam" pos="0 -2.0 0.6" xyaxes="1 0 0 0 0 1" fovy="70"/>

        <!-- Chair body parented at a KNOWN non-zero yaw (30 degrees about +Z).
             Compound primitives approximate chair silhouette for YOLO recognition
             per 04-RESEARCH Open Question 6 + A4. If A4 fails (YOLO confidence
             < 0.25), fall back to mesh chair — Plan 07 documents that path. -->
        <body name="chair" pos="0 0 0.45" euler="0 0 0.5236">
          <!-- 0.5236 rad ≈ 30 deg yaw ABOUT +Z -->
          <!-- Seat: 0.5 x 0.5 x 0.06 slab at ~0.45m -->
          <geom name="chair_seat" type="box" size="0.25 0.25 0.03" pos="0 0 0" rgba="0.55 0.35 0.20 1"/>
          <!-- Backrest: thin upright slab behind seat -->
          <geom name="chair_back" type="box" size="0.25 0.03 0.30" pos="0 0.22 0.33" rgba="0.55 0.35 0.20 1"/>
          <!-- Four legs: 0.03 x 0.03 x 0.45 uprights at seat corners -->
          <geom name="chair_leg_fl" type="box" size="0.03 0.03 0.22" pos="-0.20 -0.20 -0.25" rgba="0.45 0.25 0.15 1"/>
          <geom name="chair_leg_fr" type="box" size="0.03 0.03 0.22" pos=" 0.20 -0.20 -0.25" rgba="0.45 0.25 0.15 1"/>
          <geom name="chair_leg_bl" type="box" size="0.03 0.03 0.22" pos="-0.20  0.20 -0.25" rgba="0.45 0.25 0.15 1"/>
          <geom name="chair_leg_br" type="box" size="0.03 0.03 0.22" pos=" 0.20  0.20 -0.25" rgba="0.45 0.25 0.15 1"/>
        </body>
      </worldbody>
    </mujoco>
    ```

    Key invariants (encoded in the XML literal above):
    - Body name is exactly `chair` (Plan 07 accesses `data.body("chair")`).
    - Euler yaw = `0.5236` radians (~30°) about Z — the ground truth the SC#1 test asserts within ±15°.
    - Chair center hovers at z=0.45 so `data.body("chair").xpos` returns (0, 0, 0.45) — matches Plan 07's ±0.15m center-error gate.
    - Camera fovy=70 matches `CameraIntrinsics.from_fov(640, 480, 70.0)` default.
    - Chair position (0, 0, 0.45) is 2.0m in front of camera at (0, -2.0, 0.6) → depth ≈ 2.0m, within MedianDepthLifter's default 0.1–15.0m valid-depth window.
    - No `<asset>` or `<mesh>` tags — zero external dependencies.
    - Color hints (rgba brown-ish) improve YOLO's chance of classifying the compound
      silhouette as "chair" per A4 mitigation.

    After writing, confirm the file parses via MuJoCo in-tree (see verify).
  </action>
  <verify>
    <automated>python -c "import mujoco; m=mujoco.MjModel.from_xml_path('tests/perception/fixtures/scene_rotated_chair.xml'); d=mujoco.MjData(m); mujoco.mj_forward(m, d); b=d.body('chair'); import numpy as np; xp=np.asarray(b.xpos); xq=np.asarray(b.xquat); print('xpos=', xp, 'xquat=', xq); assert abs(xp[0])<1e-6 and abs(xp[1])<1e-6, f'chair x,y not at origin: {xp}'; assert abs(xp[2]-0.45)<1e-6, f'chair z not 0.45: {xp}'; from scipy.spatial.transform import Rotation; w,x,y,z=xq; yaw=Rotation.from_quat([x,y,z,w]).as_euler('zyx')[0]; assert abs(yaw-0.5236)<1e-3, f'chair yaw not ~30deg: {yaw}'; print('FIXTURE OK: yaw_rad=', yaw)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/perception/fixtures/scene_rotated_chair.xml` succeeds.
    - `python -c "import mujoco; mujoco.MjModel.from_xml_path('tests/perception/fixtures/scene_rotated_chair.xml')"` exits 0.
    - `grep -c 'body name="chair"' tests/perception/fixtures/scene_rotated_chair.xml` returns `1` exactly.
    - `grep -c 'fovy="70"' tests/perception/fixtures/scene_rotated_chair.xml` returns `1`.
    - The verify command extracts yaw = 0.5236 rad ±1e-3 and xpos ≈ (0, 0, 0.45).
    - XML contains no `<mesh>` or `<asset>` tags (self-contained invariant).
    - `grep -c "<mesh\|<asset" tests/perception/fixtures/scene_rotated_chair.xml` returns `0`.
  </acceptance_criteria>
  <done>scene_rotated_chair.xml exists, loads as a valid MuJoCo model, and the chair body reports xpos=(0,0,0.45) and ~30° yaw when queried via mj_forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo file → developer install | `pip install -e '.[perception]'` pulls a NEW dep (sklearn) into the dev env |
| test runner → MuJoCo XML loader | MuJoCo parses the committed XML; no network, no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | pyproject.toml perception extra | mitigate | Pin sklearn `>=1.4.0` (Jan 2024 floor); add only to perception extra (never top-level) so non-perception CI paths remain unaffected |
| T-04-02 | Tampering | scene_rotated_chair.xml fixture | mitigate | Self-contained (no external mesh / asset refs); committed to repo and reviewed; MuJoCo's XML loader does not execute arbitrary code |
| T-04-03 | DoS | MuJoCo parse of malformed XML | accept | Committed fixture is hand-authored and verified by the Task 2 automated check; runtime risk is limited to the test suite |
</threat_model>

<verification>
- `python -c "import tomllib; tomllib.loads(open('pyproject.toml').read())['project']['optional-dependencies']['perception']"` contains `scikit-learn>=1.4.0`.
- `python -c "import mujoco; mujoco.MjModel.from_xml_path('tests/perception/fixtures/scene_rotated_chair.xml')"` exits 0.
- Fixture's chair body yaw extracted via `data.body('chair').xquat` + scipy = 0.5236 rad ± 1e-3.
</verification>

<success_criteria>
- Plan 04 (PointClusterLifter) can `import sklearn.cluster` after `pip install -e '.[perception]'`.
- Plan 07 (MuJoCo integration test) can load the fixture and read GT via `data.body('chair').xpos/.xquat`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-3d-obb-pipeline/04-01-SUMMARY.md`.
</output>
