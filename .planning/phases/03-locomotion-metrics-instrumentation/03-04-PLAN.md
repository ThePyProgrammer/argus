---
phase: 03-locomotion-metrics-instrumentation
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - tests/locomotion/test_locomotion_metrics_collector.py
  - tests/locomotion/test_locomotion_metrics_foot_mapping.py
  - tests/locomotion/test_argus_go2_env_metrics.py
autonomous: true
requirements:
  - LOC-METRICS-01
  - LOC-METRICS-02
  - LOC-METRICS-03
  - LOC-METRICS-04
must_haves:
  truths:
    - "All Phase 3 Nyquist quick tests execute through `.venv/bin/python` and pass."
    - "Test coverage includes behavior-level assertions for locked decisions D-01 through D-16 and requirements LOC-METRICS-01 through LOC-METRICS-04."
    - "Decision coverage citations: D-01: hybrid collector-plus-info metrics surface; D-02: nested info[\"locomotion_metrics\"] payload; D-03: reset starts a fresh episode buffer while preserving explicit baselines; D-04: per-step records and episode summaries; D-05: conservative locomotion failure gate; D-06: failure thresholds return terminated=True; D-07: scenario success means survival without threshold failure; D-08: configurable threshold defaults; D-09: explicit validated Go2 foot mapping; D-10: foot slip per-foot world-frame XY velocity; D-11: swing foot clearance relative to terrain estimate; D-12: contact timing and duty factor; D-13: action delta and jerk proxy; D-14: position-servo effort proxy labels; D-15: commanded and observed joint-limit violations; D-16: near-limit position-servo saturation proxy."
    - "No Phase 4 export/runner scope, new controller family implementation, frontend visualization, RL, MPC, WBC, ROS, or hardware work appears in Phase 3 tests."
  artifacts:
    - path: "tests/locomotion/test_locomotion_metrics_collector.py"
      provides: "final assertions for collector source coverage"
      contains: "LOC-METRICS-01"
    - path: "tests/locomotion/test_locomotion_metrics_foot_mapping.py"
      provides: "final assertions for contact/terrain source coverage"
      contains: "LOC-METRICS-04"
    - path: "tests/locomotion/test_argus_go2_env_metrics.py"
      provides: "final assertions for env info/termination source coverage"
      contains: "D-01"
  key_links:
    - from: "03-VALIDATION.md"
      to: "tests/locomotion/*metrics*.py"
      via: "required Nyquist test names"
      pattern: "test_records_command_tracking_errors|test_failure_threshold_sets_terminated_true|test_step_info_contains_nested_locomotion_metrics"
---

<objective>
Harden Phase 3 validation coverage and run the final quick/full checks for source-audit closure.

Purpose: The prior plans implement metrics. This plan prevents shallow completion by adding explicit test traceability for D-01 through D-16, LOC-METRICS-01 through LOC-METRICS-04, threat mitigations, and out-of-scope boundaries.
Output: Strengthened tests and green phase validation commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-CONTEXT.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-RESEARCH.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-VALIDATION.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-PATTERNS.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-01-SUMMARY.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-02-SUMMARY.md
@.planning/phases/03-locomotion-metrics-instrumentation/03-03-SUMMARY.md
@tests/locomotion/test_locomotion_metrics_collector.py
@tests/locomotion/test_locomotion_metrics_foot_mapping.py
@tests/locomotion/test_argus_go2_env_metrics.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source-audit and threat regression assertions to metrics tests</name>
  <files>tests/locomotion/test_locomotion_metrics_collector.py, tests/locomotion/test_locomotion_metrics_foot_mapping.py, tests/locomotion/test_argus_go2_env_metrics.py</files>
  <read_first>
    - .planning/phases/03-locomotion-metrics-instrumentation/03-CONTEXT.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-RESEARCH.md
    - .planning/phases/03-locomotion-metrics-instrumentation/03-VALIDATION.md
    - tests/locomotion/test_locomotion_metrics_collector.py
    - tests/locomotion/test_locomotion_metrics_foot_mapping.py
    - tests/locomotion/test_argus_go2_env_metrics.py
  </read_first>
  <behavior>
    - Add or extend tests so comments/docstrings or assertion messages mention each requirement ID `LOC-METRICS-01`, `LOC-METRICS-02`, `LOC-METRICS-03`, and `LOC-METRICS-04` in the file that verifies it.
    - Add explicit assertions for threat mitigations: malformed finite/shape config rejection, proxy labels, bounded history, strict foot mapping, and compact info payload.
    - Add out-of-scope guard assertions using source scans in tests or direct grep-friendly constants that fail if Phase 3 introduces `jsonl`, `csv`, `evaluation_runner`, `rl_policy`, `mpc`, `wbc`, `ros2`, or frontend visualization strings in the new metrics tests.
  </behavior>
  <action>Update only the three Phase 3 test files. Add traceability comments or docstrings containing exact decision IDs `D-01` through `D-16` near behavior-level assertions that cover them: D-01/D-02/D-04 in env info/summary surface tests, D-03 in reset-baseline and `last_locomotion_metrics_summary` persistence tests, D-05/D-06/D-07/D-08 in failure/summary/config override tests, D-09 through D-12 in foot mapping/contact/terrain-height tests, and D-13 through D-16 in action-quality collector tests. Add exact requirement strings in relevant test docstrings: `LOC-METRICS-01`, `LOC-METRICS-02`, `LOC-METRICS-03`, `LOC-METRICS-04`. If any locked decision lacks an existing assertion, add the smallest focused assertion in the existing test file. This is a test-only hardening plan: do not modify production code; if a new assertion exposes an implementation gap, stop and report the gap in `03-04-SUMMARY.md` for replanning rather than fixing source here. Do not add documentation files.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py tests/locomotion/test_argus_go2_env_metrics.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "LOC-METRICS-01" tests/locomotion/test_locomotion_metrics_collector.py` returns at least one match.
    - `grep -n "LOC-METRICS-04" tests/locomotion/test_locomotion_metrics_foot_mapping.py` returns at least one match.
    - `grep -n "D-01" tests/locomotion/test_argus_go2_env_metrics.py` returns at least one match.
    - `grep -n "D-16" tests/locomotion/test_locomotion_metrics_collector.py` returns at least one match.
    - Behavior-level D-01/D-02/D-04 assertions prove nested compact `info["locomotion_metrics"]`, terminal `info["locomotion_metrics_summary"]`, and no top-level metric key sprawl.
    - Behavior-level D-03 assertions prove reset starts a fresh active buffer while explicit baseline and `last_locomotion_metrics_summary` survive until the next completed episode.
    - Behavior-level D-05/D-06/D-07/D-08 assertions prove failure thresholds terminate immediately, scenario success is survival-to-truncation, and thresholds are config-overridable.
    - Behavior-level D-09/D-10/D-11/D-12 assertions prove strict FL/FR/RL/RR mapping, slip while in contact, non-flat slope/rough clearance via terrain-height helper, and duty-factor/contact timing summaries.
    - Behavior-level D-13/D-14/D-15/D-16 assertions prove action delta/jerk proxy, position-servo effort proxy labels, commanded/observed joint-limit counts, and near-limit saturation proxy labels.
    - `.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py tests/locomotion/test_argus_go2_env_metrics.py -q` exits 0.
  </acceptance_criteria>
  <done>Tests carry behavior-level traceability for every Phase 3 requirement and locked decision, with grep-visible IDs as supporting evidence plus threat mitigation assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Run final Phase 3 validation commands and record any implementation gaps</name>
  <files>tests/locomotion/test_locomotion_metrics_collector.py, tests/locomotion/test_locomotion_metrics_foot_mapping.py, tests/locomotion/test_argus_go2_env_metrics.py</files>
  <read_first>
    - .planning/phases/03-locomotion-metrics-instrumentation/03-VALIDATION.md
    - tests/locomotion/test_locomotion_metrics_collector.py
    - tests/locomotion/test_locomotion_metrics_foot_mapping.py
    - tests/locomotion/test_argus_go2_env_metrics.py
  </read_first>
  <action>Run the exact quick command from `03-VALIDATION.md` using `.venv/bin/python`, then run `.venv/bin/python -m pytest tests/locomotion tests/metrics -q`. If both pass, run the full suite command `.venv/bin/python -m pytest tests/locomotion tests/metrics tests/bridge/test_sim_bridge.py tests/bridge/test_multi_bridge.py -q`. If a failure identifies missing source coverage from LOC-METRICS-01 through LOC-METRICS-04 or D-01 through D-16, do not modify production files in this plan. Fix only incorrect/insufficient test assertions in the three listed test files. If the tests are correct and production behavior is missing or wrong, stop and record the exact implementation gap in `03-04-SUMMARY.md` for replanning. If a failure points outside Phase 3 scope, record it in the plan summary as a blocker and do not implement Phase 4/export/controller-family/frontend/RL/MPC/WBC/ROS/hardware work.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py tests/locomotion/test_argus_go2_env_metrics.py -q && .venv/bin/python -m pytest tests/locomotion tests/metrics -q</automated>
  </verify>
  <acceptance_criteria>
    - Quick command exits 0: `.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py tests/locomotion/test_argus_go2_env_metrics.py -q`.
    - Wave command exits 0: `.venv/bin/python -m pytest tests/locomotion tests/metrics -q`.
    - Full command is attempted and result is recorded in `03-04-SUMMARY.md`: `.venv/bin/python -m pytest tests/locomotion tests/metrics tests/bridge/test_sim_bridge.py tests/bridge/test_multi_bridge.py -q`.
    - `grep -R -n "jsonl\|csv\|evaluation_runner\|rl_policy\|mpc\|wbc\|ros2\|frontend" tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py tests/locomotion/test_argus_go2_env_metrics.py | grep -v "out-of-scope"` returns no matches.
    - No production files are modified by Task 2; any production implementation gap is summarized for replanning instead of patched here.
  </acceptance_criteria>
  <done>Phase 3 Nyquist quick and wave validation are green, full suite result is known, and any remaining blocker is documented without scope creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests -> implementation confidence | Tests define whether metric telemetry can be trusted for benchmark claims. |
| source decisions -> code behavior | Locked decisions D-01 through D-16 must not be accidentally dropped. |
| phase scope -> implementation | Phase 3 must not leak into Phase 4 exports or deferred controller families. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | malformed metric/action/config tests | mitigate | Add assertions for invalid finite/shape/config inputs and run quick tests. |
| T-03-02 | Repudiation / Integrity | proxy label regressions | mitigate | Assert proxy field names and forbid physical-energy labels in test coverage. |
| T-03-03 | Denial of Service | unbounded metric history | mitigate | Assert bounded history eviction and compact info payload. |
| T-03-04 | Integrity | ambiguous foot mapping | mitigate | Assert missing/duplicate/no-heuristic strict mapping cases remain present. |
| T-03-05 | Information Disclosure / DoS | oversized per-step info payload | mitigate | Assert no top-level metric sprawl and no raw MuJoCo arrays in per-step info. |
</threat_model>

<verification>
Final validation commands:

```bash
.venv/bin/python -m pytest tests/locomotion/test_locomotion_metrics_collector.py tests/locomotion/test_locomotion_metrics_foot_mapping.py tests/locomotion/test_argus_go2_env_metrics.py -q
.venv/bin/python -m pytest tests/locomotion tests/metrics -q
.venv/bin/python -m pytest tests/locomotion tests/metrics tests/bridge/test_sim_bridge.py tests/bridge/test_multi_bridge.py -q
```
</verification>

<success_criteria>
- Phase 3 has behavior-level test traceability for D-01 through D-16 and LOC-METRICS-01 through LOC-METRICS-04; grep-visible IDs are supporting traceability only, not the acceptance mechanism.
- Required Nyquist tests from `03-VALIDATION.md` exist and pass using `.venv/bin/python`.
- No Phase 4 JSONL/CSV/CLI runner/export work, new controller-family implementations, RL/MPC/WBC, ROS/hardware, or frontend visualization work is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/03-locomotion-metrics-instrumentation/03-04-SUMMARY.md`.
</output>
