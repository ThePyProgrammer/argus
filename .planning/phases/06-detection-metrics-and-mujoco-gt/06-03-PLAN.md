---
phase: 6
plan: 3
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/stores/__tests__/metricsStore.detection.shape.test.ts
autonomous: true
requirements:
  - DET-METRICS-01
tags: [perception, metrics, frontend, vitest]
must_haves:
  truths:
    - "Vitest collection passes (no test error); stub test runs and skips via `it.skip(...)`"
  artifacts:
    - path: "frontend/src/stores/__tests__/metricsStore.detection.shape.test.ts"
      provides: "skip-stub for DET-METRICS-01 frontend store-shape invariant"
      contains: "it.skip"
  key_links:
    - from: "vitest runner"
      to: "metricsStore.detection.shape.test.ts"
      via: "vitest --run frontend/src/stores/__tests__/"
      pattern: "skip.*Wave 0"
---

<objective>
Create a vitest skip-stub at `frontend/src/stores/__tests__/metricsStore.detection.shape.test.ts` that will become the DET-METRICS-01 frontend-side structural invariant (mirrors Phase 3's `detectorStore.shape.test.ts`). The stub lets vitest run green in Wave 0 while the real assertions land in Plan 11.

Purpose: Phase 3 Plan 11 established the store-shape test pattern for DET-UI-06. Phase 6 adds `detectionPerRobot` + `detectionHistory` slices to `metricsStore`; this stub reserves the test file slot and names the replacing plan, so Plan 11 executors know exactly where to put the real test.
Output: One vitest stub file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-detection-metrics-and-mujoco-gt/06-VALIDATION.md
@.planning/phases/06-detection-metrics-and-mujoco-gt/06-CONTEXT.md
@frontend/src/stores/__tests__/detectorStore.shape.test.ts

<interfaces>
<!-- Phase 3 Plan 11 precedent: frontend/src/stores/__tests__/detectorStore.shape.test.ts -->
<!-- Exports: none (Vitest test file) -->
<!-- Uses: `describe` + `it.skip` + Zustand snapshot diff -->

<!-- Stub body for Phase 6 (exact content below): -->
```typescript
import { describe, it } from 'vitest';

describe('metricsStore detection slice shape (DET-METRICS-01)', () => {
  it.skip('Wave 0 scaffold — detection store-shape test lands in 06-11-PLAN.md', () => {
    // Implementation mirrors detectorStore.shape.test.ts from Phase 3 Plan 11.
    // Asserts `detectionPerRobot: Record<string, DetectionMetrics>` and
    // `detectionHistory: Record<string, DetectionMetricHistory>` slices exist
    // on the Zustand store; asserts `updateAllMetrics` accepts the two new
    // args in a single set() call (preserves the one-re-render-per-stats
    // invariant from Phase v2.0 13-01).
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write metricsStore detection-shape skip-stub</name>
  <files>frontend/src/stores/__tests__/metricsStore.detection.shape.test.ts</files>
  <read_first>
    - frontend/src/stores/__tests__/detectorStore.shape.test.ts (Phase 3 Plan 11 precedent — mirror this file's skeleton)
    - .planning/phases/06-detection-metrics-and-mujoco-gt/06-CONTEXT.md §D-04 (store slice shape)
    - .planning/phases/06-detection-metrics-and-mujoco-gt/06-VALIDATION.md §Wave 0 Requirements (last bullet)
  </read_first>
  <action>
    Create `frontend/src/stores/__tests__/metricsStore.detection.shape.test.ts` with EXACTLY this content:

    ```typescript
    import { describe, it } from 'vitest';

    describe('metricsStore detection slice shape (DET-METRICS-01)', () => {
      it.skip('Wave 0 scaffold — detection store-shape test lands in 06-11-PLAN.md', () => {
        // Implementation mirrors detectorStore.shape.test.ts from Phase 3 Plan 11.
        // Asserts `detectionPerRobot: Record<string, DetectionMetrics>` and
        // `detectionHistory: Record<string, DetectionMetricHistory>` slices exist
        // on the Zustand store; asserts `updateAllMetrics` accepts the two new
        // args in a single set() call (preserves the one-re-render-per-stats
        // invariant from Phase v2.0 13-01).
      });
    });
    ```

    Match indentation exactly (2-space). ASCII only, no trailing whitespace. No other tests in this file.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/stores/__tests__/metricsStore.detection.shape.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the exact path `frontend/src/stores/__tests__/metricsStore.detection.shape.test.ts`
    - `grep -c 'DET-METRICS-01' frontend/src/stores/__tests__/metricsStore.detection.shape.test.ts` returns `1`
    - `grep -c '06-11-PLAN.md' frontend/src/stores/__tests__/metricsStore.detection.shape.test.ts` returns `1`
    - `grep -c 'it.skip' frontend/src/stores/__tests__/metricsStore.detection.shape.test.ts` returns `1`
    - Vitest run output contains `1 skipped` (not `1 failed`)
  </acceptance_criteria>
  <done>Vitest stub exists, skips cleanly, and names its replacing plan.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | — | — | N/A | Skip-stub; no runtime code |
</threat_model>

<verification>
- Vitest run collects the file without error
- Single skipped `it.skip` entry
</verification>

<success_criteria>
`cd frontend && npx vitest run src/stores/__tests__/metricsStore.detection.shape.test.ts` exits 0 with `1 skipped`.
</success_criteria>

<output>
Create `.planning/phases/06-detection-metrics-and-mujoco-gt/06-03-SUMMARY.md`
</output>
