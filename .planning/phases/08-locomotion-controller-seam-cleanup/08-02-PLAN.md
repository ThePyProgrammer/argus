---
phase: 08-locomotion-controller-seam-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locomotion/controllers.py
  - docs/locomotion-benchmark.md
  - tests/locomotion/test_locomotion_controller_registry.py
  - tests/test_locomotion_benchmark_docs.py
autonomous: true
requirements:
  - LOC-CTRL-03
  - LOC-REPORT-02
requirements_addressed:
  - LOC-CTRL-03
  - LOC-REPORT-02
must_haves:
  truths:
    - "WBC remains discoverable as an unavailable placeholder controller."
    - "WBC placeholder metadata does not advertise `torque_or_joint_position` as a v4.0 env action mode."
    - "Controller-family documentation and registry tests use the same WBC deferred action-contract vocabulary."
    - "Evaluation action-mode rejection behavior from Phase 7 remains unchanged after metadata cleanup."
  artifacts:
    - path: "src/locomotion/controllers.py"
      provides: "WBC placeholder capability metadata with explicit deferred action contract"
      contains: "undefined_deferred"
    - path: "docs/locomotion-benchmark.md"
      provides: "Controller-family matrix row explaining WBC has no runnable v4.0 env action mode"
      contains: "future action contract `undefined_deferred`"
    - path: "tests/locomotion/test_locomotion_controller_registry.py"
      provides: "Registry guard for WBC placeholder vocabulary"
      contains: "test_wbc_placeholder_advertises_deferred_action_contract"
    - path: "tests/test_locomotion_benchmark_docs.py"
      provides: "Docs/registry drift guard for WBC placeholder vocabulary"
      contains: "test_wbc_documentation_matches_registry_deferred_action_contract"
  key_links:
    - from: "src/locomotion/controllers.py"
      to: "src/locomotion/actions.py"
      via: "WBC action_mode is deliberately not in available_action_modes because available=False and action contract is deferred"
      pattern: "undefined_deferred"
    - from: "docs/locomotion-benchmark.md"
      to: "ControllerRegistry.list_controllers()[wbc]"
      via: "test_wbc_documentation_matches_registry_deferred_action_contract"
      pattern: "future action contract `undefined_deferred`"
---

<objective>
Resolve WBC placeholder metadata audit debt for LOC-CTRL-03 and LOC-REPORT-02 by replacing misleading `torque_or_joint_position` vocabulary with an explicit unavailable/deferred action contract and guarding docs/registry/evaluator behavior.

Purpose: Make future-controller boundaries honest without adding new runnable controller implementations or new env/evaluator action modes.
Output: Updated WBC registry capabilities, controller-family matrix wording, registry tests, docs drift guards, and Phase 7 evaluator rejection preservation checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v4.0-MILESTONE-AUDIT.md
@.planning/phases/08-locomotion-controller-seam-cleanup/08-RESEARCH.md
@.planning/phases/08-locomotion-controller-seam-cleanup/08-VALIDATION.md
@.planning/phases/08-locomotion-controller-seam-cleanup/08-PATTERNS.md
@src/locomotion/controllers.py
@src/locomotion/actions.py
@src/locomotion/evaluation.py
@docs/locomotion-benchmark.md
@tests/locomotion/test_locomotion_controller_registry.py
@tests/test_locomotion_benchmark_docs.py
@tests/locomotion/test_locomotion_evaluation_runner.py

<interfaces>
<!-- Key contracts from Phase 8 research/pattern map. Use these directly. -->

Canonical v4.0 env action-mode constants from `src/locomotion/actions.py`:
```python
ACTION_MODE_VELOCITY = "velocity_command"
ACTION_MODE_JOINT_POSITION = "joint_position"
ACTION_MODE_RESIDUAL_BASELINE = "residual_baseline"
```

Required placeholder capability keys from `src/locomotion/controllers.py` include `action_mode`, `model_requirements`, and `reproducibility`. Avoid adding a new required top-level capability key unless every controller gets it. Store WBC-specific deferred vocabulary in existing capability fields.

Chosen Phase 8 WBC vocabulary:
- `capabilities["action_mode"] == "undefined_deferred"`
- `capabilities["model_requirements"]["action_contract"] == "undefined_deferred"`
- `capabilities["model_requirements"]["env_action_mode"] is None`
- `available is False`
- `"undefined_deferred" not in available_action_modes()`

Do not add `torque_or_joint_position` to `src/locomotion/actions.py`, CLI help, docs action-mode table, or evaluator-supported modes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WBC placeholder vocabulary and evaluator-preservation regressions</name>
  <files>tests/locomotion/test_locomotion_controller_registry.py, tests/test_locomotion_benchmark_docs.py</files>
  <read_first>
    - .planning/phases/08-locomotion-controller-seam-cleanup/08-VALIDATION.md
    - .planning/phases/08-locomotion-controller-seam-cleanup/08-PATTERNS.md
    - tests/locomotion/test_locomotion_controller_registry.py
    - tests/test_locomotion_benchmark_docs.py
    - tests/locomotion/test_locomotion_evaluation_runner.py
    - src/locomotion/controllers.py
    - src/locomotion/actions.py
    - docs/locomotion-benchmark.md
  </read_first>
  <behavior>
    - `test_wbc_placeholder_advertises_deferred_action_contract`: registry entry `wbc` is unavailable, `capabilities["action_mode"] == "undefined_deferred"`, the value is not in `available_action_modes()`, `capabilities["model_requirements"]["action_contract"] == "undefined_deferred"`, and `capabilities["model_requirements"]["env_action_mode"] is None`.
    - `test_wbc_documentation_matches_registry_deferred_action_contract`: docs contain exact row text tying controller id `wbc` to future action contract `undefined_deferred`, and docs do not contain `torque_or_joint_position`.
    - Existing Phase 7 evaluator tests `test_unknown_action_mode_fails_before_env_construction` and `test_unsupported_action_modes_fail_before_env_construction` still pass without edits.
  </behavior>
  <action>Add the tests before production/doc changes. In `tests/locomotion/test_locomotion_controller_registry.py`, add `test_wbc_placeholder_advertises_deferred_action_contract` beside the residual placeholder action-mode test. The test must import `available_action_modes` and `ControllerRegistry`, fetch `entries["wbc"]`, and assert the exact values listed in the behavior block. In `tests/test_locomotion_benchmark_docs.py`, add `test_wbc_documentation_matches_registry_deferred_action_contract` that reads `docs/locomotion-benchmark.md`, fetches `entries["wbc"]`, asserts the exact registry values listed above, asserts `"torque_or_joint_position" not in guide`, and asserts the exact Markdown sentence fragment `Controller id `wbc`; future action contract `undefined_deferred` (not a v4.0 env action mode)` appears in the guide. Do not relax or edit Phase 7 evaluator rejection tests.</action>
  <verify>
    <automated>uv run python -m pytest tests/locomotion/test_locomotion_controller_registry.py::test_wbc_placeholder_advertises_deferred_action_contract tests/test_locomotion_benchmark_docs.py::test_wbc_documentation_matches_registry_deferred_action_contract tests/locomotion/test_locomotion_evaluation_runner.py::test_unknown_action_mode_fails_before_env_construction tests/locomotion/test_locomotion_evaluation_runner.py::test_unsupported_action_modes_fail_before_env_construction -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "def test_wbc_placeholder_advertises_deferred_action_contract" tests/locomotion/test_locomotion_controller_registry.py` returns one match.
    - `grep -R "def test_wbc_documentation_matches_registry_deferred_action_contract" tests/test_locomotion_benchmark_docs.py` returns one match.
    - `grep -R "undefined_deferred" tests/locomotion/test_locomotion_controller_registry.py tests/test_locomotion_benchmark_docs.py` returns at least four matches.
    - `grep -R "torque_or_joint_position.*not in guide" tests/test_locomotion_benchmark_docs.py` returns one match.
    - The automated pytest command initially fails before Task 2 because registry/docs still advertise the old WBC vocabulary.
  </acceptance_criteria>
  <done>Tests encode the WBC placeholder contract and preserve Phase 7 evaluator action-mode rejection behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update WBC registry metadata and controller-family docs to explicit deferred vocabulary</name>
  <files>src/locomotion/controllers.py, docs/locomotion-benchmark.md</files>
  <read_first>
    - src/locomotion/controllers.py
    - src/locomotion/actions.py
    - docs/locomotion-benchmark.md
    - tests/locomotion/test_locomotion_controller_registry.py
    - tests/test_locomotion_benchmark_docs.py
    - .planning/v4.0-MILESTONE-AUDIT.md
    - .planning/phases/08-locomotion-controller-seam-cleanup/08-RESEARCH.md
    - .planning/phases/08-locomotion-controller-seam-cleanup/08-PATTERNS.md
  </read_first>
  <action>In `src/locomotion/controllers.py`, update WBC placeholder capabilities so the WBC class no longer calls `_placeholder_capabilities("wbc", "torque_or_joint_position")` without qualification. Implement the exact target state by creating the WBC capabilities from `_placeholder_capabilities("wbc", "undefined_deferred")`, then setting `CAPABILITIES["model_requirements"]["action_contract"] = "undefined_deferred"` and `CAPABILITIES["model_requirements"]["env_action_mode"] = None` for WBC only. Keep `WBCController.available()` false through `_UnavailableControllerBase` and keep `WBC_UNAVAILABLE_REASON` unchanged. In `docs/locomotion-benchmark.md`, update the WBC controller-family matrix row so the Argus hook/seam cell contains exactly `Controller id `wbc`; future action contract `undefined_deferred` (not a v4.0 env action mode)`. Keep the env-supported action-mode table limited to `velocity_command`, `joint_position`, and `residual_baseline`; do not add `torque_or_joint_position` or `undefined_deferred` to that table as an env-supported action mode.</action>
  <verify>
    <automated>uv run python -m pytest tests/locomotion/test_locomotion_controller_registry.py tests/test_locomotion_benchmark_docs.py tests/locomotion/test_locomotion_evaluation_runner.py::test_unknown_action_mode_fails_before_env_construction tests/locomotion/test_locomotion_evaluation_runner.py::test_unsupported_action_modes_fail_before_env_construction -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "_placeholder_capabilities(\"wbc\", \"undefined_deferred\")" src/locomotion/controllers.py` returns one match.
    - `grep -R "action_contract.*undefined_deferred" src/locomotion/controllers.py` returns one match.
    - `grep -R "env_action_mode.*None" src/locomotion/controllers.py` returns one match.
    - `grep -R "torque_or_joint_position" src/locomotion/controllers.py docs/locomotion-benchmark.md src/locomotion/actions.py` returns no matches.
    - `grep -R 'Controller id `wbc`; future action contract `undefined_deferred` (not a v4.0 env action mode)' docs/locomotion-benchmark.md` returns one match.
    - `grep -R "undefined_deferred" src/locomotion/actions.py` returns no matches.
    - The automated pytest command passes.
  </acceptance_criteria>
  <done>WBC registry metadata and controller-family docs agree that WBC has an unavailable future action contract named `undefined_deferred`, and no code/docs imply `torque_or_joint_position` is a runnable v4.0 env action mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ControllerRegistry metadata -> docs/evaluation users | Placeholder metadata can be mistaken for runnable controller/action support. |
| CLI action-mode input -> evaluation runner | User-provided action-mode strings must fail before env construction/artifact creation when unsupported. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | MultiRobotBridge controller output | transfer | Not modified in this plan; Plan 08-01 mitigates with shared `apply_controller_target()` validation. |
| T-08-02 | Tampering | MultiRobotBridge actuator indices | transfer | Not modified in this plan; Plan 08-01 mitigates with indexed control validation and no-mutation tests. |
| T-08-03 | Spoofing | `WBCController.CAPABILITIES` and controller-family docs | mitigate | Replace `torque_or_joint_position` with `undefined_deferred`, add `model_requirements["action_contract"] == "undefined_deferred"` and `env_action_mode is None`, keep `available=False`, and verify registry/docs guards. |
| T-08-04 | Repudiation | `argus eval-locomotion` action-mode rejection/reporting | mitigate | Preserve Phase 7 tests `test_unknown_action_mode_fails_before_env_construction` and `test_unsupported_action_modes_fail_before_env_construction`; do not add `undefined_deferred` to `available_action_modes()` or evaluator-runnable modes. |
</threat_model>

<verification>
Run the plan-level command:

`uv run python -m pytest tests/locomotion/test_locomotion_controller_registry.py tests/test_locomotion_benchmark_docs.py tests/locomotion/test_locomotion_evaluation_runner.py::test_unknown_action_mode_fails_before_env_construction tests/locomotion/test_locomotion_evaluation_runner.py::test_unsupported_action_modes_fail_before_env_construction -q`

Then run the phase quick command after both Phase 8 plans complete:

`uv run python -m pytest tests/locomotion/test_controller_dispatch.py tests/locomotion/test_locomotion_controller_registry.py tests/bridge/test_multi_bridge.py tests/test_locomotion_benchmark_docs.py -q`
</verification>

<success_criteria>
- WBC remains registered, discoverable, unavailable, and selection-time failing through `UnavailableControllerError`.
- `torque_or_joint_position` is absent from `src/locomotion/controllers.py`, `docs/locomotion-benchmark.md`, and `src/locomotion/actions.py`.
- WBC registry metadata contains `action_mode == "undefined_deferred"`, `model_requirements["action_contract"] == "undefined_deferred"`, and `model_requirements["env_action_mode"] is None`.
- Docs contain the exact WBC row phrase `Controller id `wbc`; future action contract `undefined_deferred` (not a v4.0 env action mode)`.
- Phase 7 evaluator action-mode rejection tests pass unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/08-locomotion-controller-seam-cleanup/08-02-SUMMARY.md`.
</output>
