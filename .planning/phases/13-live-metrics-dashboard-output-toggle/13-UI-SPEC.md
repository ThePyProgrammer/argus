---
phase: 13
slug: live-metrics-dashboard-output-toggle
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-23
---

# Phase 13 -- UI Design Contract

> Visual and interaction contract for the Live Metrics Dashboard + Output Toggle. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (custom React inline styles) |
| Icon library | none (Unicode arrows/symbols for toggles) |
| Font | system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif |

Source: Extracted from `frontend/src/App.css` body rule. This phase adds no new design tooling.

---

## Spacing Scale

Declared values. The existing codebase uses practical spacing that clusters around multiples of 4 with occasional 6px and 10px. This phase follows the same established values.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, dot indicator margins |
| sm | 8px | Compact element spacing, sparkline-to-label gap, toggle button gaps |
| md | 12px | Panel internal padding, section separators |
| lg | 16px | Section header padding, layout-level gaps |
| xl | 24px | Major section breaks, empty state padding |
| 2xl | 32px | Collapsed bar height (metrics toggle bar) |

Exceptions:
- 6px: Used between metric rows within per-robot columns (matches existing RobotCard 6px marginBottom pattern)
- 10px: Used for marginTop between toggle button groups (matches existing ControlPanel 10px marginTop pattern)
- 30px: Sparkline SVG height (content-driven dimension, not a layout spacing token)

Source: Extracted from existing component inline styles (ControlPanel.tsx, CameraStrip.tsx, RobotCard.tsx). Not a strict 8-point grid -- follows the project's established practical spacing.

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 13px | 400 | 1.5 | Metric values in live table, sparkline percentage deltas |
| Label | 12px | 600 | 1.3 | Section headers ("METRICS", "RENDERING"), metric labels (ATE, RPE, ms/frame), toggle bar text |
| Small | 11px | 400 | 1.3 | Metric units, baseline reference text, tooltip content |
| Heading | 16px | 700 | 1.2 | Not used in this phase (only in Sidebar header "C2 Command") |

Monospace override: Metric numeric values (ATE, RPE, ms/frame) use `fontFamily: 'monospace'` at 13px weight 400 for tabular alignment. This matches the existing cloud config button pattern in ControlPanel.tsx.

Source: Extracted from existing components. ControlPanel buttons use 13px/600, labels use 12px/600/#888, small text uses 10-11px/400. This phase uses 11px minimum (not 10px) for metric data legibility.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #0d0d1a | Page background, viewer area background |
| Secondary (30%) | #1a1a2e / #1a1a3e | Sidebar background (#1a1a2e), panel backgrounds (#1a1a3e), metrics panel background (#111122) |
| Accent (10%) | #2ecc71 | Active toggle indicator borders, active output mode button border, sparkline current-value stroke, tracking OK dot |
| Destructive | #e74c3c | Tracking LOST dot only |

### Semantic Status Colors

| Status | Color | Usage |
|--------|-------|-------|
| OK | #2ecc71 | Tracking status dot, sparkline stroke for improving metrics |
| INITIALIZING / RELOCALIZING | #f1c40f | Tracking status dot |
| LOST | #e74c3c | Tracking status dot |

Source: CONTEXT.md locked decisions for tracking status dot colors. #2ecc71 accent from existing CapabilityBadge.tsx and cloud config active state.

### Per-Robot Column Colors

Robot columns in the metrics panel use the Okabe-Ito palette from `frontend/src/utils/palette.ts`:
- Robot 0 (A): #0072B2 (Blue) -- column header left-border color
- Robot 1 (B): #E69F00 (Orange) -- column header left-border color
- Additional robots wrap around the 8-color palette

These are applied as `borderLeft: 3px solid {robotColor(index)}` on each robot column, matching the existing RobotCard.tsx pattern.

### Accent Reserved For

1. Active output mode toggle button border (`border: 2px solid #2ecc71`)
2. Active view mode tab ("Live" or "vs Baseline") text color
3. Sparkline polyline stroke color for current metric values
4. Tracking status OK dot fill

Accent is NOT used for: metric labels, panel borders, section headers, inactive toggle states, or general interactive elements.

### Surface Colors (Specific)

| Element | Background | Border |
|---------|-----------|--------|
| Metrics panel (expanded) | #111122 | top: 1px solid #2a2a4a |
| Metrics panel (collapsed bar) | #111122 | top: 1px solid #2a2a4a |
| Per-robot column | #1a1a3e | left: 3px solid {robotColor} |
| View mode tab (active) | #1a1a3e | bottom: 2px solid #2ecc71 |
| View mode tab (inactive) | transparent | bottom: 2px solid transparent |
| Output mode button (active) | #1a3a2a | border: 2px solid #2ecc71 |
| Output mode button (inactive) | #263238 | border: 1px solid #444 |
| Sparkline container | transparent | none |
| Baseline reference line | n/a | stroke: #555, dashed (3,2) |

Source: Panel background #111122 from CameraStrip.tsx. Button active/inactive states from ControlPanel.tsx cloud config pattern.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Metrics panel toggle (collapsed) | "^ Metrics" |
| Metrics panel toggle (expanded) | "v Metrics" |
| View mode tab: live | "Live" |
| View mode tab: baseline | "vs Baseline" |
| Output mode: cloud | "Point Cloud" |
| Output mode: voxel | "Voxel Grid" |
| Output mode: mesh | "Mesh" |
| Metric label: ATE | "ATE" with unit "(m)" |
| Metric label: RPE | "RPE" with unit "(m)" |
| Metric label: timing | "ms/frame" |
| Metric label: tracking | "Status" |
| Empty state: no metrics (live view) | Heading: "No metrics yet" / Body: "Start a session to see live SLAM metrics." |
| Empty state: no baseline (vs Baseline view) | Heading: "No baseline recorded" / Body: "Run a session with ICP backend first to establish baseline metrics." |
| Empty state: mesh unavailable | Tooltip on disabled Mesh button: "Mesh mode requires Open3D. Install open3d to enable." |
| Baseline label in sparkline | "ICP baseline" (text below the dashed reference line, 10px, #555) |
| Percentage delta (positive, worse) | "+{N}%" in #e74c3c (red) |
| Percentage delta (negative, better) | "-{N}%" in #2ecc71 (green) |
| Percentage delta (equal) | "0%" in #888 (muted) |
| Section header in ControlPanel | "RENDERING" (12px, 600 weight, #888, uppercase) |

Note: The metrics panel collapse/expand uses Unicode triangle characters matching the existing CameraStrip pattern (U+25B2 up triangle for collapsed, U+25BC down triangle for expanded).

Source: Collapse toggle copy from CameraStrip.tsx. Section header pattern from ControlPanel.tsx "CONTROLS" and AlgorithmSection.tsx "SLAM ALGORITHM". Empty state pattern from Sidebar.tsx "Waiting for robots..." and CameraStrip.tsx "No robots connected".

---

## Component Inventory

### New Components

| Component | Type | Location | Description |
|-----------|------|----------|-------------|
| MetricsPanel | React (.tsx) | `frontend/src/components/MetricsPanel.tsx` | Bottom panel with collapsible toggle, live metrics table, baseline sparkline view |
| Sparkline | React (.tsx) | `frontend/src/components/Sparkline.tsx` | SVG polyline chart with optional horizontal baseline reference line |
| VoxelManager | Imperative (.ts) | `frontend/src/components/VoxelManager.ts` | Three.js InstancedMesh voxel rendering manager |
| MeshManager | Imperative (.ts) | `frontend/src/components/MeshManager.ts` | Three.js BufferGeometry mesh rendering manager |
| metricsStore | Zustand (.ts) | `frontend/src/stores/metricsStore.ts` | Store for metrics, baseline, view mode, output mode, mesh data |

### Modified Components

| Component | Modification |
|-----------|-------------|
| App.tsx | Add `.metrics-area` div between viewer and camera strip |
| App.css | 3-row grid: `grid-template-rows: 1fr auto auto`; sidebar spans rows 1-2 |
| ControlPanel.tsx | Add "RENDERING" section with output mode toggle buttons |
| SceneViewer.tsx | Instantiate VoxelManager + MeshManager; switch active manager based on outputMode |
| useWebSocket.ts | Extend 'stats' handler to dispatch slam_metrics/baseline/history to metricsStore |
| messageTypes.ts | Add SlamMetrics interface; extend StatsPayload with slam_metrics, baseline, metric_history fields |

---

## Interaction Contracts

### Metrics Panel Collapse/Expand

- **Collapsed height:** 32px (thin bar showing "^ Metrics" centered)
- **Expanded height:** auto (content-driven, approximately 120-160px)
- **Transition:** `height 0.2s ease` (matches CameraStrip.tsx)
- **Default state:** collapsed
- **Toggle target:** Click anywhere on the 32px bar

### View Mode Tab Switch (Live / vs Baseline)

- **Location:** Inside metrics panel, top-left, horizontal tabs
- **Active indicator:** Bottom border 2px solid #2ecc71, text color #2ecc71
- **Inactive indicator:** Bottom border 2px solid transparent, text color #888
- **Transition:** None (instant switch, no animation)
- **Mutual exclusion:** Only one view visible at a time
- **Default state:** "Live"

### Output Mode Toggle (ControlPanel)

- **Location:** ControlPanel.tsx, new "RENDERING" section below existing color mode toggle
- **Layout:** 3 buttons in a horizontal row with 6px gap
- **Active state:** Background #1a3a2a, border 2px solid #2ecc71, text color #2ecc71
- **Inactive state:** Background #263238, border 1px solid #444, text color #666
- **Disabled state (mesh unavailable):** opacity 0.3, cursor not-allowed, title tooltip with unavailability reason
- **Default state:** "Point Cloud" active
- **Transition on click:** Immediate store update; Three.js scene performs 300ms cross-fade

### Three.js Rendering Mode Cross-Fade

- **Duration:** 300ms
- **Mechanism:** requestAnimationFrame loop interpolating material.opacity
- **Fade-out:** Current geometry opacity 1.0 -> 0.0 over 300ms; `material.transparent = true`, `material.depthWrite = false` during fade
- **Fade-in:** New geometry opacity 0.0 -> 1.0 over 300ms; `material.depthWrite = true` when opacity reaches 1.0
- **Overlap:** Both geometries visible during transition (staggered is acceptable; simultaneous is ideal)
- **Edge case:** If user clicks a new mode during an in-progress fade, cancel current fade, immediately set old geometry opacity to 0 and remove, then start new fade-in

### Sparkline Interaction

- **Dimensions:** 120px wide x 30px tall
- **Data:** Array of up to 60 entries (ring buffer size from backend)
- **Current value stroke:** 1.5px solid, color #2ecc71 (or robot-specific Okabe-Ito color if per-robot sparklines)
- **Baseline reference line:** 1px dashed #555 (stroke-dasharray "3,2"), horizontal at baseline Y position
- **Hover:** None (no tooltip on sparkline -- keep it simple, information-dense)
- **Empty state (<2 data points):** Render nothing (return null)

### Live Metrics Table Layout

- **Structure:** Horizontal per-robot columns side by side
- **Each column contains:** Robot name header (colored left border), 4 metric rows (ATE, RPE, ms/frame, Status)
- **Metric row:** Label left-aligned (12px, #888), value right-aligned (13px monospace, #e0e0e0)
- **Tracking status:** Colored dot (8px diameter circle) + status text (13px)
- **Update frequency:** Values update in-place; no flash or highlight animation on update (flight telemetry density aesthetic)

### Baseline Comparison Layout

- **Structure:** One row per metric (ATE, RPE, ms/frame), each containing: label, sparkline, current value, delta percentage
- **Sparkline position:** Inline after label, before numeric values
- **Current value:** Right of sparkline, 13px monospace #e0e0e0
- **Delta percentage:** Right of current value, 11px, color-coded (#2ecc71 better / #e74c3c worse / #888 equal)
- **Baseline reference:** Dashed horizontal line within sparkline at baseline value

---

## Layout Contract

### CSS Grid Update

```
Before (2-row):
grid-template-rows: 1fr auto
Row 1: viewer (col 1) + sidebar (col 2)
Row 2: cameras (col 1-2)

After (3-row):
grid-template-rows: 1fr auto auto
Row 1: viewer (col 1) + sidebar (col 2, spans rows 1-2)
Row 2: metrics (col 1)
Row 3: cameras (col 1-2)
```

| Grid Area | Column | Row | Notes |
|-----------|--------|-----|-------|
| .viewer-area | 1 | 1 | Unchanged |
| .sidebar-area | 2 | 1 / 3 | Spans viewer + metrics rows |
| .metrics-area | 1 | 2 | New: between viewer and cameras |
| .camera-strip-area | 1 / -1 | 3 | Was row 2, now row 3 |

Source: RESEARCH.md CSS Grid layout section, validated against current App.css.

### Metrics Panel Internal Layout

```
+------------------------------------------------------------------+
| [v Metrics]  [Live] [vs Baseline]                  (toggle bar)  |
+------------------------------------------------------------------+
|  Robot A (blue border)  |  Robot B (orange border)   (live view) |
|  ATE:       0.042m      |  ATE:       0.067m                    |
|  RPE:       0.018m      |  RPE:       0.031m                    |
|  ms/frame:  12.4        |  ms/frame:  8.7                       |
|  Status:    . OK        |  Status:    . OK                      |
+------------------------------------------------------------------+
```

```
+------------------------------------------------------------------+
| [v Metrics]  [Live] [vs Baseline]                  (toggle bar)  |
+------------------------------------------------------------------+
|  ATE    [sparkline~~~~---baseline---] 0.042m  -15%  (baseline)   |
|  RPE    [sparkline~~~~---baseline---] 0.018m  -22%               |
|  ms/frm [sparkline~~~~---baseline---] 12.4    +8%                |
+------------------------------------------------------------------+
```

### ControlPanel Rendering Section

```
Existing:
  [Hide Scene Mesh]
  [Switch to True RGB]

New (appended below):
  RENDERING                              (section label)
  [Point Cloud] [Voxel Grid] [Mesh]      (3 toggle buttons, horizontal)
```

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |

No shadcn. No third-party registries. All components are custom React with inline styles. No external UI dependencies introduced in this phase.

---

## Accessibility Notes

- Output mode toggle buttons use `aria-pressed` to indicate active state
- Disabled Mesh button uses `aria-disabled="true"` and `title` attribute for tooltip explanation
- Tracking status dots are decorative (adjacent text provides the status); dots use `aria-hidden="true"`
- Metrics panel toggle bar is a `<button>` element (not a div with onClick) for keyboard accessibility
- View mode tabs use `role="tablist"` and `role="tab"` with `aria-selected`
- Color contrast: All text meets WCAG AA minimum (4.5:1 ratio). #888 on #1a1a3e = 4.6:1. #e0e0e0 on #111122 = 12.8:1

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
