---
phase: 04-evaluation-runner-and-regression
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - tests/locomotion/test_locomotion_baseline_regression.py
autonomous: true
requirements:
  - LOC-EVAL-04
must_haves:
  truths:
    - "D-14: A pytest regression gate exercises analytical trot on flat ground over a small fixed seed set."
    - "D-13: The gate asserts no locomotion failure plus bounded tracking, stability, and distance metrics for translational command schedules using robust thresholds rather than golden snapshots."
    - "Synthetic degraded summaries fail the same threshold helper so the gate is not toothless."
    - "D-15: The real MuJoCo smoke path skips cleanly when MuJoCo/assets are unavailable while fast fake-env/unit tests cover runner, export, reload, and aggregation behavior."
  artifacts:
    - path: "tests/locomotion/test_locomotion_baseline_regression.py"
      provides: "Analytical flat-ground threshold regression and synthetic threshold-failure tests"
  key_links:
    - from: "tests/locomotion/test_locomotion_baseline_regression.py"
      to: "src.locomotion.evaluation.run_evaluation_matrix"
      via: "fixed analytical_trot flat_ground seed matrix"
      pattern: "analytical_trot"
    - from: "tests/locomotion/test_locomotion_baseline_regression.py"
      to: "summary.json/episode rows"
      via: "threshold helper over saved metrics"
      pattern: "assert_analytical_flat_ground_thresholds"
---

<objective>
Add the analytical flat-ground baseline regression gate required to prevent silent degradation of the current locomotion baseline.

Purpose: Phase 4 needs at least one real threshold gate proving benchmark artifacts catch regressions in the baseline controller/scenario path.
Output: A focused pytest file with synthetic unit failure coverage and a marked real MuJoCo smoke/regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-evaluation-runner-and-regression/04-CONTEXT.md
@.planning/phases/04-evaluation-runner-and-regression/04-RESEARCH.md
@.planning/phases/04-evaluation-runner-and-regression/04-VALIDATION.md
@.planning/phases/04-evaluation-runner-and-regression/04-PATTERNS.md
@src/locomotion/evaluation.py
@src/locomotion/env.py
@tests/locomotion/test_argus_go2_env_contract.py
@tests/locomotion/test_argus_go2_env_metrics.py

<interfaces>
Regression source APIs:
```python
EvaluationMatrix(controllers=("analytical_trot",), scenarios=("flat_ground",), seeds=(101, 202, 303), action_mode="velocity_command", max_episode_steps=...)
EvaluationRunConfig(output_root=tmp_path, fail_on_locomotion_failure=True, ...)
run_evaluation_matrix(matrix, config) -> EvaluationResult
```

Existing integration skip pattern:
```python
def _skip_if_mujoco_python_unsupported() -> None:
    if not ((3, 10) <= sys.version_info < (3, 13)):
        pytest.skip("MuJoCo integration requires Python >=3.10,<3.13 and mujoco installed")
    try:
        import mujoco  # noqa: F401
    except ImportError:
        pytest.skip("MuJoCo integration requires Python >=3.10,<3.13 and mujoco installed")
```
Also skip if `models/unitree_go2/go2.xml` is absent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add threshold helper and synthetic degradation tests</name>
  <files>tests/locomotion/test_locomotion_baseline_regression.py</files>
  <read_first>
    - tests/locomotion/test_locomotion_baseline_regression.py (if it already exists)
    - src/locomotion/evaluation.py
    - .planning/phases/04-evaluation-runner-and-regression/04-CONTEXT.md
    - .planning/phases/04-evaluation-runner-and-regression/04-VALIDATION.md
    - .planning/phases/04-evaluation-runner-and-regression/04-PATTERNS.md
    - tests/locomotion/test_argus_go2_env_metrics.py
  </read_first>
  <behavior>
    - A helper `assert_analytical_flat_ground_thresholds(rows)` passes for two successful rows with bounded metrics.
    - The helper fails when any row has `success` false or non-empty `failure_reason`.
    - The helper fails when `tracking_rmse` exceeds `1.50`.
    - The helper fails when `distance_xy_m` is below `0.01` for a translational command summary whose `commanded_velocity` is nonzero, for example `[0.4, 0.0, 0.0]`.
    - The helper fails when rows omit `commanded_velocity` or `command_source`, because threshold meaning depends on command context.
    - The helper fails when `base_height_min_m` is below `0.15`, `roll_abs_max_rad` exceeds `0.90`, or `pitch_abs_max_rad` exceeds `0.90`.
  </behavior>
  <action>Create `tests/locomotion/test_locomotion_baseline_regression.py` with unit tests for a local threshold helper before adding the real MuJoCo test. Use explicit constants: `TRACKING_RMSE_MAX = 1.50`, `DISTANCE_XY_MIN_M = 0.01`, `BASE_HEIGHT_MIN_M = 0.15`, `ROLL_ABS_MAX_RAD = 0.90`, and `PITCH_ABS_MAX_RAD = 0.90`. These initial robust bounds are intentionally margin-based per D-13; if the real baseline calibration in Task 2 shows they are too tight or too loose, adjust them in the same file with a comment containing the observed fixed-seed ranges. Use row dicts shaped like `episodes.csv` rows from Plan 02: `success`, `failure_reason`, `commanded_velocity`, `command_source`, `tracking_rmse`, `distance_xy_m`, `base_height_min_m`, `roll_abs_max_rad`, and `pitch_abs_max_rad`. Include source comments with `LOC-EVAL-04`, `D-13`, `D-14`, `D-15`, and `T-04-03`.</action>
  <verify>
    <automated>/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/locomotion/test_locomotion_baseline_regression.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/locomotion/test_locomotion_baseline_regression.py` contains `TRACKING_RMSE_MAX = 1.50` or a calibrated replacement constant with an adjacent `Observed fixed-seed ranges` comment.
    - `tests/locomotion/test_locomotion_baseline_regression.py` contains `DISTANCE_XY_MIN_M = 0.01` or a calibrated replacement constant with an adjacent `Observed fixed-seed ranges` comment.
    - `tests/locomotion/test_locomotion_baseline_regression.py` contains `commanded_velocity` and `command_source`.
    - `tests/locomotion/test_locomotion_baseline_regression.py` contains `def assert_analytical_flat_ground_thresholds(`.
    - `tests/locomotion/test_locomotion_baseline_regression.py` contains `def test_threshold_helper_rejects_locomotion_failure(`.
    - `tests/locomotion/test_locomotion_baseline_regression.py` contains `def test_threshold_helper_rejects_tracking_degradation(`.
    - The automated pytest command exits 0 after helper implementation.
  </acceptance_criteria>
  <done>Synthetic threshold tests prove the baseline regression helper fails on explicit locomotion, tracking, distance, and stability degradation cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add marked real analytical flat-ground smoke regression</name>
  <files>tests/locomotion/test_locomotion_baseline_regression.py</files>
  <read_first>
    - tests/locomotion/test_locomotion_baseline_regression.py
    - src/locomotion/evaluation.py
    - src/locomotion/env.py
    - .planning/phases/04-evaluation-runner-and-regression/04-CONTEXT.md
    - .planning/phases/04-evaluation-runner-and-regression/04-RESEARCH.md
    - .planning/phases/04-evaluation-runner-and-regression/04-PATTERNS.md
    - tests/locomotion/test_argus_go2_env_contract.py
  </read_first>
  <action>Add a real MuJoCo regression test to `tests/locomotion/test_locomotion_baseline_regression.py` using `@pytest.mark.integration`. Use fixed seeds `(101, 202, 303)` per D-14, controller `analytical_trot`, scenario `flat_ground`, action mode `velocity_command`, and a small smoke horizon such as `max_episode_steps=25` unless calibration requires a nearby value that still stays under the project pytest timeout. Use the established skip helper: skip when Python is outside `&gt;=3.10,&lt;3.13`, `import mujoco` fails, or `models/unitree_go2/go2.xml` does not exist. Run `run_evaluation_matrix(EvaluationMatrix(...), EvaluationRunConfig(output_root=tmp_path, fail_on_locomotion_failure=True, verbose=False))`, assert `result.exit_code == 0`, assert `result.had_locomotion_failure is False`, assert `result.run_dir / "summary.json"` exists, then pass `result.episode_rows` to `assert_analytical_flat_ground_thresholds`.

If the first real run fails only because initial constants are not calibrated, inspect the produced rows and update constants with an inline comment exactly beginning `# Observed fixed-seed ranges (101, 202, 303):` followed by the measured commanded_velocity/command_source context plus tracking_rmse, distance_xy_m, base_height_min_m, roll_abs_max_rad, and pitch_abs_max_rad ranges. Do not use golden snapshots. Do not broaden thresholds enough to pass `success=False`, missing command context, zero distance on a commanded translational scenario, or roll/pitch/base-height failures.</action>
  <verify>
    <automated>/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/locomotion/test_locomotion_baseline_regression.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/locomotion/test_locomotion_baseline_regression.py` contains `@pytest.mark.integration`.
    - `tests/locomotion/test_locomotion_baseline_regression.py` contains `seeds=(101, 202, 303)` or `seeds = (101, 202, 303)`.
    - `tests/locomotion/test_locomotion_baseline_regression.py` contains `analytical_trot`.
    - `tests/locomotion/test_locomotion_baseline_regression.py` contains `flat_ground`.
    - `tests/locomotion/test_locomotion_baseline_regression.py` contains `summary.json`.
    - `tests/locomotion/test_locomotion_baseline_regression.py` contains `commanded_velocity` and `command_source`.
    - `tests/locomotion/test_locomotion_baseline_regression.py` does not contain `golden` outside a comment saying golden snapshots are not used.
    - The automated pytest command exits 0 or skips only with an explicit MuJoCo/assets skip reason.
  </acceptance_criteria>
  <done>The analytical flat-ground fixed-seed regression runs through the evaluation runner, writes artifacts, and gates bounded baseline behavior without golden snapshots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixed matrix -> real MuJoCo env | Regression test invokes local simulation and writes temp artifacts. |
| Saved regression artifacts -> assertions | Episode rows and summary data become pass/fail thresholds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Regression matrix values | mitigate | Hardcode controller `analytical_trot`, scenario `flat_ground`, seeds `(101, 202, 303)`, and action mode `velocity_command` in test. |
| T-04-02 | Tampering | Regression output directory | mitigate | Use pytest `tmp_path` as `EvaluationRunConfig.output_root`; Plan 02 path controls keep artifacts inside temp root. |
| T-04-03 | Repudiation | Threshold calibration and artifacts | mitigate | Assert `summary.json` exists and document observed fixed-seed ranges if thresholds are calibrated. |
| T-04-04 | Tampering | CSV output from regression | mitigate | Regression uses Plan 02 CSV sanitizer; this plan asserts artifacts exist but does not add new CSV writer paths. |
| T-04-05 | Denial of Service | Real MuJoCo smoke runtime | mitigate | Use exactly 3 fixed seeds and a small max episode step count under pytest timeout; skip only when MuJoCo/assets unavailable. |
</threat_model>

<verification>
Run `/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/locomotion/test_locomotion_baseline_regression.py -q`.
</verification>

<success_criteria>
- Synthetic degradation tests prove threshold helper rejects failures, missing command context, stationary rows for translational-command expectations, and metric regressions.
- D-16: Real analytical flat-ground regression is marked `integration`, uses seeds `(101, 202, 303)`, writes summary artifacts, exits nonzero on locomotion failure through the evaluation runner, and passes or cleanly skips for missing MuJoCo/assets.
- No golden snapshots are used.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evaluation-runner-and-regression/04-04-SUMMARY.md`.
</output>
