---
phase: 02-controller-plugin-baseline
plan: 06
type: execute
wave: 4
depends_on: [02-03, 02-04, 02-05]
files_modified:
  - tests/locomotion/test_locomotion_controller_registry.py
  - tests/locomotion/test_locomotion_controller_protocol.py
  - tests/locomotion/test_controller_dispatch.py
  - tests/locomotion/test_argus_go2_env_contract.py
  - tests/bridge/test_sim_bridge.py
  - tests/bridge/test_multi_bridge.py
autonomous: true
requirements: [LOC-CTRL-01, LOC-CTRL-02, LOC-CTRL-03, LOC-CTRL-04]
must_haves:
  truths:
    - "All Phase 2 focused controller, env metadata, and bridge seam tests pass together."
    - "Every LOC-CTRL requirement is represented by a passing focused test file."
    - "Every locked decision D-01 through D-15 is covered by implementation or explicit regression assertions."
    - "Deferred RL/direct/MPC/WBC/ROS/hardware/frontend work remains unimplemented beyond unavailable placeholder registry entries."
  artifacts:
    - path: "tests/locomotion/test_locomotion_controller_registry.py"
      provides: "LOC-CTRL-01/02/03 registry and placeholder regression coverage"
    - path: "tests/locomotion/test_locomotion_controller_protocol.py"
      provides: "LOC-CTRL-01/02 protocol and analytical equivalence coverage"
    - path: "tests/locomotion/test_controller_dispatch.py"
      provides: "LOC-CTRL-01/04 shared dispatch coverage"
    - path: "tests/locomotion/test_argus_go2_env_contract.py"
      provides: "Controller metadata in reset/step info coverage"
    - path: "tests/bridge/test_sim_bridge.py"
      provides: "Single-robot bridge public contract preservation"
    - path: "tests/bridge/test_multi_bridge.py"
      provides: "Multi-robot bridge public contract and per-robot controller independence"
  key_links:
    - from: "ControllerRegistry"
      to: "ArgusGo2Env/MuJoCoBridge/MultiRobotBridge"
      via: "registered analytical_trot controller instances"
      pattern: "ControllerRegistry.create"
    - from: "controller_dispatch"
      to: "data.ctrl sinks"
      via: "apply_controller_target validation"
      pattern: "apply_controller_target"
---

<objective>
Run the final Phase 2 integration hardening pass across controller, environment, and bridge seams.

Purpose: Close cross-plan gaps after parallel Wave 3 work by making all Phase 2 focused tests pass as one suite and ensuring D-01 through D-15 plus LOC-CTRL-01 through LOC-CTRL-04 are covered without scope creep.
Output: Focused suite green, with test-only hardening in this plan and blocking gap-closure plans for any production integration defects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-controller-plugin-baseline/02-CONTEXT.md
@.planning/phases/02-controller-plugin-baseline/02-RESEARCH.md
@.planning/phases/02-controller-plugin-baseline/02-VALIDATION.md
@.planning/phases/02-controller-plugin-baseline/02-PATTERNS.md
@.planning/phases/02-controller-plugin-baseline/02-01-SUMMARY.md
@.planning/phases/02-controller-plugin-baseline/02-02-SUMMARY.md
@.planning/phases/02-controller-plugin-baseline/02-03-SUMMARY.md
@.planning/phases/02-controller-plugin-baseline/02-04-SUMMARY.md
@.planning/phases/02-controller-plugin-baseline/02-05-SUMMARY.md
@src/locomotion/controllers.py
@src/locomotion/controller_dispatch.py
@src/locomotion/env.py
@src/bridge/sim_bridge.py
@src/bridge/multi_bridge.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cross-plan phase coverage assertions for decisions, requirements, and deferred boundaries</name>
  <files>tests/locomotion/test_locomotion_controller_registry.py, tests/locomotion/test_locomotion_controller_protocol.py, tests/locomotion/test_controller_dispatch.py, tests/locomotion/test_argus_go2_env_contract.py, tests/bridge/test_sim_bridge.py, tests/bridge/test_multi_bridge.py</files>
  <read_first>
    - .planning/phases/02-controller-plugin-baseline/02-CONTEXT.md
    - .planning/phases/02-controller-plugin-baseline/02-VALIDATION.md
    - .planning/phases/02-controller-plugin-baseline/02-01-SUMMARY.md
    - .planning/phases/02-controller-plugin-baseline/02-02-SUMMARY.md
    - .planning/phases/02-controller-plugin-baseline/02-03-SUMMARY.md
    - .planning/phases/02-controller-plugin-baseline/02-04-SUMMARY.md
    - .planning/phases/02-controller-plugin-baseline/02-05-SUMMARY.md
    - tests/locomotion/test_locomotion_controller_registry.py
    - tests/locomotion/test_locomotion_controller_protocol.py
    - tests/locomotion/test_controller_dispatch.py
    - tests/locomotion/test_argus_go2_env_contract.py
    - tests/bridge/test_sim_bridge.py
    - tests/bridge/test_multi_bridge.py
  </read_first>
  <behavior>
    - Registry tests cover D-05/D-06/D-07/D-08 and LOC-CTRL-01/02/03.
    - Protocol tests cover D-01/D-02/D-03/D-04/D-08 and finite analytical output.
    - Dispatch tests cover D-09/D-12 and T-2-03 finite target gates.
    - Env contract tests cover D-13/D-14/D-15 reset/step metadata.
    - Bridge tests cover D-10/D-11/D-12 and T-2-04 public return contracts.
    - Placeholder tests prove residual/direct/MPC/WBC entries are unavailable and no test expects implemented RL/MPC/WBC behavior.
  </behavior>
  <action>Review the six test files and add only missing assertions needed to make coverage explicit. Prefer adding small tests to existing files rather than creating a new meta-test file. Do not alter production code in this task. Required exact assertion strings across tests: `analytical_trot`, `residual_policy`, `direct_policy`, `mpc`, `wbc`, `controller_metadata`, `parameter_hash`, `parameter_summary`, `Unknown locomotion controller`, `UnavailableControllerError`, `SensorFrame`, and `dict[str, SensorFrame]` or an equivalent signature assertion using `dict`. Do not add assertions that require real MuJoCo rendering or external services.</action>
  <verify>
    <automated>/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/locomotion/test_locomotion_controller_registry.py tests/locomotion/test_locomotion_controller_protocol.py tests/locomotion/test_controller_dispatch.py tests/locomotion/test_argus_go2_env_contract.py tests/bridge/test_sim_bridge.py tests/bridge/test_multi_bridge.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' tests/locomotion/test_locomotion_controller_registry.py | grep -c 'UnavailableControllerError'` outputs at least `1`.
    - `grep -v '^#' tests/locomotion/test_locomotion_controller_registry.py | grep -c 'parameter_hash\|parameter_summary'` outputs at least `2`.
    - `grep -v '^#' tests/locomotion/test_argus_go2_env_contract.py | grep -c 'controller_metadata'` outputs at least `2`.
    - `grep -v '^#' tests/bridge/test_sim_bridge.py | grep -c 'SensorFrame'` outputs at least `2`.
    - `grep -v '^#' tests/bridge/test_multi_bridge.py | grep -c 'dict'` outputs at least `1`.
    - The automated command passes in the project venv. If this task exposes a production integration defect, Task 2 must fix it within the listed production files or create a new blocking gap-closure PLAN.md before Phase 2 completion.
  </acceptance_criteria>
  <done>Cross-plan tests explicitly cover requirements, decisions, threat gates, and deferred boundaries, and the focused command passes or has a blocking production-gap path created before completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run Phase 2 focused suite and route production defects to blocking gap-closure plans</name>
  <files>tests/locomotion/test_locomotion_controller_registry.py, tests/locomotion/test_locomotion_controller_protocol.py, tests/locomotion/test_controller_dispatch.py, tests/locomotion/test_argus_go2_env_contract.py, tests/bridge/test_sim_bridge.py, tests/bridge/test_multi_bridge.py</files>
  <read_first>
    - .planning/phases/02-controller-plugin-baseline/02-CONTEXT.md
    - .planning/phases/02-controller-plugin-baseline/02-VALIDATION.md
    - .planning/phases/02-controller-plugin-baseline/02-01-SUMMARY.md
    - .planning/phases/02-controller-plugin-baseline/02-02-SUMMARY.md
    - .planning/phases/02-controller-plugin-baseline/02-03-SUMMARY.md
    - .planning/phases/02-controller-plugin-baseline/02-04-SUMMARY.md
    - .planning/phases/02-controller-plugin-baseline/02-05-SUMMARY.md
    - tests/locomotion/test_locomotion_controller_registry.py
    - tests/locomotion/test_locomotion_controller_protocol.py
    - tests/locomotion/test_controller_dispatch.py
    - tests/locomotion/test_argus_go2_env_contract.py
    - tests/bridge/test_sim_bridge.py
    - tests/bridge/test_multi_bridge.py
  </read_first>
  <action>Run the full Phase 2 focused command from `02-VALIDATION.md`. If failures are caused by stale mocks, signature assertions, import order, or duplicated setup across tests, fix the tests. If failures reveal production defects in `src/locomotion/controllers.py`, `src/locomotion/controller_dispatch.py`, `src/locomotion/env.py`, `src/bridge/sim_bridge.py`, or `src/bridge/multi_bridge.py`, do not edit production files in 02-06. Instead, create a new blocking gap-closure PLAN.md before Phase 2 completion with the exact failing assertion, affected production files, and required verification command; do not use the summary as a substitute for passing LOC-CTRL behavior. Keep this plan a hardening/verification pass focused on integration and test drift, not new feature scope. Do not add or remove any deferred controller implementation assertions.</action>
  <verify>
    <automated>/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/locomotion tests/bridge/test_sim_bridge.py tests/bridge/test_multi_bridge.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' tests/locomotion/test_locomotion_controller_registry.py | grep -c 'torch\|ultralytics\|transformers\|mujoco\|gymnasium\|rclpy'` outputs at least `1` because the lightweight import guard exists.
    - `grep -v '^#' tests/locomotion/test_controller_dispatch.py | grep -c 'np.nan\|np.inf'` outputs at least `1`.
    - `grep -v '^#' tests/bridge/test_multi_bridge.py | grep -c '_controllers'` outputs at least `2`.
    - The automated command passes in the project venv. A missing dependency/environment blocker may be recorded only when the supported project environment cannot run; production LOC-CTRL defects must be converted into a blocking gap-closure PLAN.md before Phase 2 completion.
  </acceptance_criteria>
  <done>Phase 2 focused suite is green in the project venv, or a blocking gap-closure PLAN.md exists for any production defect outside this plan's listed files before Phase 2 completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| combined phase tests -> shipped phase confidence | Separate plan tests may pass while integrated controller/env/bridge behavior regresses. |
| deferred-placeholder assertions -> future-controller scope | Tests could accidentally encode implemented RL/MPC/WBC behavior instead of unavailable seams. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering / Repudiation | Phase suite registry tests | mitigate | Suite includes unknown-controller id failure and no default fallback assertions. |
| T-2-02 | Denial of Service | Phase suite placeholder tests | mitigate | Suite includes unavailable-placeholder create-time failure assertions. |
| T-2-03 | Tampering | Phase suite dispatch/env/bridge tests | mitigate | Suite includes finite `(12,)` validation and invalid-write no-mutation assertions. |
| T-2-04 | Tampering / Denial of Service | Phase suite bridge tests | mitigate | Suite includes public signature/return contract and per-robot controller independence assertions. |
| T-2-05 | Repudiation | Phase suite metadata tests | mitigate | Suite includes full reset metadata and compact per-step attribution assertions. |
</threat_model>

<verification>
Run `/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/locomotion tests/bridge/test_sim_bridge.py tests/bridge/test_multi_bridge.py -q`.
</verification>

<success_criteria>
All Phase 2 focused tests pass together in the project venv. If final hardening discovers a production defect, Phase 2 cannot complete until a new blocking gap-closure PLAN.md is created and executed; summary-only documentation does not satisfy LOC-CTRL behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-controller-plugin-baseline/02-06-SUMMARY.md`.
</output>
