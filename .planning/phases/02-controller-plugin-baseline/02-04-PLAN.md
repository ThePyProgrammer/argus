---
phase: 02-controller-plugin-baseline
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - tests/bridge/test_sim_bridge.py
  - src/bridge/sim_bridge.py
autonomous: true
requirements: [LOC-CTRL-02, LOC-CTRL-04]
must_haves:
  truths:
    - "MuJoCoBridge keeps `step(action: np.ndarray | None = None) -> SensorFrame`."
    - "MuJoCoBridge uses a per-bridge registered analytical_trot controller for velocity-buffered walking when no direct action is supplied."
    - "Direct 12-element actions are still accepted but validated before `data.ctrl` mutation."
    - "Calling `step()` before `start()` still raises the existing `RuntimeError` containing `not started`."
  artifacts:
    - path: "src/bridge/sim_bridge.py"
      provides: "Single-robot bridge control loop using controller seam without public API changes"
      contains: "ControllerRegistry.create"
    - path: "tests/bridge/test_sim_bridge.py"
      provides: "Regression tests for bridge public contract and controller seam"
  key_links:
    - from: "MuJoCoBridge._velocity_to_ctrl"
      to: "controller_dispatch"
      via: "command_from_velocity + compute_controller_action"
      pattern: "command_from_velocity"
    - from: "MuJoCoBridge.step"
      to: "apply_controller_target"
      via: "validated full control sink write"
      pattern: "apply_controller_target"
---

<objective>
Align the single-robot bridge with the registered controller seam while preserving its public runtime contract.

Purpose: Satisfy D-09, D-10, D-11, and D-12 for `MuJoCoBridge` by replacing hard-coded analytical gait ownership with the registered analytical baseline and shared target validation, without adding user-facing selection or changing bridge return types.
Output: Updated `src/bridge/sim_bridge.py` and focused bridge tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-controller-plugin-baseline/02-CONTEXT.md
@.planning/phases/02-controller-plugin-baseline/02-RESEARCH.md
@.planning/phases/02-controller-plugin-baseline/02-VALIDATION.md
@.planning/phases/02-controller-plugin-baseline/02-PATTERNS.md
@.planning/phases/02-controller-plugin-baseline/02-01-SUMMARY.md
@.planning/phases/02-controller-plugin-baseline/02-02-SUMMARY.md
@src/bridge/sim_bridge.py
@src/bridge/sensor_types.py
@src/locomotion/controllers.py
@src/locomotion/controller_dispatch.py
@tests/bridge/test_sim_bridge.py

<interfaces>
Preserve this public signature exactly:

```python
def step(self, action: np.ndarray | None = None) -> SensorFrame:
```

Use these Phase 2 helpers:

```python
from src.locomotion.controllers import ControllerRegistry
from src.locomotion.controller_dispatch import (
    apply_controller_target,
    command_from_velocity,
    compute_controller_action,
)
```

The single bridge must own exactly one controller instance per `MuJoCoBridge` object:

```python
self._controller = ControllerRegistry.create("analytical_trot")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend single-bridge tests for controller seam and public contract preservation</name>
  <files>tests/bridge/test_sim_bridge.py</files>
  <read_first>
    - .planning/phases/02-controller-plugin-baseline/02-CONTEXT.md
    - .planning/phases/02-controller-plugin-baseline/02-VALIDATION.md
    - .planning/phases/02-controller-plugin-baseline/02-PATTERNS.md
    - .planning/phases/02-controller-plugin-baseline/02-01-SUMMARY.md
    - .planning/phases/02-controller-plugin-baseline/02-02-SUMMARY.md
    - tests/bridge/test_sim_bridge.py
    - src/bridge/sim_bridge.py
    - src/bridge/sensor_types.py
    - src/locomotion/controllers.py
    - src/locomotion/controller_dispatch.py
  </read_first>
  <behavior>
    - `MuJoCoBridge.step` signature remains `action: np.ndarray | None = None` and return annotation remains `SensorFrame` if present in the file.
    - `MuJoCoBridge()` initializes a `_controller` whose metadata or class indicates `analytical_trot` / `AnalyticalTrotController`.
    - `step()` before `start()` still raises `RuntimeError` containing `not started`.
    - `set_velocity(np.array([1.0, 0.5]), 0.3)` still buffers `_linear_vel == [1.0, 0.5]` and `_angular_vel == 0.3`.
    - A fake-started bridge with `action=np.array([0.0, 0.9, -1.8] * 4)` writes the direct action through validation before stepping.
    - A fake-started bridge with `action=None` obtains a controller target through the shared controller dispatch path and writes it to `data.ctrl[:]` before stepping.
  </behavior>
  <action>Extend existing unit tests in `tests/bridge/test_sim_bridge.py`; do not require real MuJoCo for new tests. Use `inspect.signature(MuJoCoBridge.step)` for public signature checks. Use fake `_model`, `_data.ctrl`, `_capture_frame`, and patched `sys.modules["mujoco"] = SimpleNamespace(mj_step=MagicMock(...))` where needed, following existing fake-data patterns from env tests. Per D-10, keep existing tests for import/default config/startless step/set_velocity. Per T-2-04, assert public return type behavior remains `SensorFrame` in tests that call fake capture.</action>
  <verify>
    <automated>/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/bridge/test_sim_bridge.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' tests/bridge/test_sim_bridge.py | grep -c 'inspect.signature'` outputs at least `1`.
    - `grep -v '^#' tests/bridge/test_sim_bridge.py | grep -c 'analytical_trot\|AnalyticalTrotController'` outputs at least `1`.
    - `grep -v '^#' tests/bridge/test_sim_bridge.py | grep -c 'not started'` outputs at least `1`.
    - `grep -v '^#' tests/bridge/test_sim_bridge.py | grep -c 'action=None\|action = None'` outputs at least `1`.
    - The automated command fails before bridge implementation because `_controller` or dispatch behavior is absent.
  </acceptance_criteria>
  <done>Single-bridge tests pin controller seam usage and public API preservation before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor MuJoCoBridge internals to registered analytical controller seam</name>
  <files>src/bridge/sim_bridge.py</files>
  <read_first>
    - .planning/phases/02-controller-plugin-baseline/02-CONTEXT.md
    - .planning/phases/02-controller-plugin-baseline/02-RESEARCH.md
    - .planning/phases/02-controller-plugin-baseline/02-PATTERNS.md
    - .planning/phases/02-controller-plugin-baseline/02-01-SUMMARY.md
    - .planning/phases/02-controller-plugin-baseline/02-02-SUMMARY.md
    - src/bridge/sim_bridge.py
    - src/bridge/sensor_types.py
    - src/locomotion/controllers.py
    - src/locomotion/controller_dispatch.py
  </read_first>
  <action>Modify `src/bridge/sim_bridge.py` only. Replace direct `_gait = TrotGaitController()` ownership with `self._controller = ControllerRegistry.create("analytical_trot")` while preserving imports needed for `GaitParams` if still used elsewhere. In `start()`, compute standing targets through the registered controller using `LocomotionCommand(vx=0.0, vy=0.0, yaw_rate=0.0)` or `command_from_velocity(np.zeros(2), 0.0)` and apply with `apply_controller_target(self._data, standing.action)`. In `step(action=None)`, preserve the existing pre-start `RuntimeError("Bridge not started -- call start() first")` semantics and public return type. If `action is not None`, validate/apply it with `apply_controller_target(self._data, action)`. If `action is None`, call `_velocity_to_ctrl()` which must construct a `LocomotionCommand` from `_linear_vel` and `_angular_vel`, call `compute_controller_action(self._controller, {}, command, self._dt)`, and return the validated result action. Keep MuJoCo stepping, IMU collection, `_capture_frame()`, and `SensorFrame` return flow unchanged. Do not add C2 controller selection, metrics instrumentation, or evaluation-runner code.</action>
  <verify>
    <automated>/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/bridge/test_sim_bridge.py tests/locomotion/test_controller_dispatch.py tests/locomotion/test_locomotion_controller_protocol.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/bridge/sim_bridge.py | grep -c 'ControllerRegistry.create("analytical_trot")'` outputs at least `1`.
    - `grep -v '^#' src/bridge/sim_bridge.py | grep -c 'apply_controller_target'` outputs at least `2`.
    - `grep -v '^#' src/bridge/sim_bridge.py | grep -c 'command_from_velocity'` outputs at least `1`.
    - `grep -v '^#' src/bridge/sim_bridge.py | grep -c 'def step(self, action: np.ndarray | None = None) -> SensorFrame'` outputs `1`.
    - `grep -v '^#' src/bridge/sim_bridge.py | grep -c 'self._data.ctrl\[:\] = action\|self._data.ctrl\[:\] = ctrl'` outputs `0`.
    - The automated command passes.
  </acceptance_criteria>
  <done>MuJoCoBridge uses the shared controller/action seam internally while preserving public lifecycle, velocity buffering, direct action support, stepping, and `SensorFrame` return behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller direct action -> MuJoCoBridge.step | Optional caller-supplied 12-element action enters bridge control sink. |
| velocity buffer -> registered controller | Buffered navigation velocity becomes a controller command. |
| bridge internals -> public C2 runtime | Internal refactor must not change public bridge behavior used by non-Gym runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-03 | Tampering | `MuJoCoBridge.step`, `_velocity_to_ctrl` | mitigate | Use `apply_controller_target` and `compute_controller_action` validation before `data.ctrl` writes. |
| T-2-04 | Tampering / Denial of Service | `MuJoCoBridge.step` public contract | mitigate | Tests assert signature and `SensorFrame` return behavior; implementation keeps lifecycle and stepping flow unchanged. |
| T-2-01 | Repudiation | Single bridge controller selection | mitigate | Use explicit `ControllerRegistry.create("analytical_trot")`; no unknown-id input or silent fallback is introduced in this bridge plan. |
</threat_model>

<verification>
Run `/home/prannayag/pragnition/robotics/argus/.venv/bin/python -m pytest tests/bridge/test_sim_bridge.py tests/locomotion/test_controller_dispatch.py tests/locomotion/test_locomotion_controller_protocol.py -q -x`.
</verification>

<success_criteria>
LOC-CTRL-02 and the single-robot portion of LOC-CTRL-04 are covered: bridge internals use the registered analytical baseline and shared validation helper while the public bridge API remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-controller-plugin-baseline/02-04-SUMMARY.md`.
</output>
