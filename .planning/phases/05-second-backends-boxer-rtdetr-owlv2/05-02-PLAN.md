---
phase: 05-second-backends-boxer-rtdetr-owlv2
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - LICENSES.md
  - models/.gitkeep
  - models/README.md
autonomous: true
requirements: [DET-MODELS-08, DET-MODELS-07]
must_haves:
  truths:
    - "LICENSES.md exists at repo root"
    - "LICENSES.md contains BoxeR CC-BY-NC-4.0 row"
    - "LICENSES.md contains Ultralytics AGPL-3.0, RT-DETRv2 Apache-2.0, ONNX Runtime MIT rows"
    - "models/.gitkeep + models/README.md document layout for <backend>/<sha>/<artifact>"
  artifacts:
    - path: LICENSES.md
      provides: SPDX + upstream + scope for every runtime dep + BoxeR NC surfacing note
      contains: "CC-BY-NC-4.0"
    - path: models/README.md
      provides: documented checkpoint directory layout (D-11)
      contains: "models/<backend-slug>/<sha>/"
    - path: models/.gitkeep
      provides: models/ directory tracked so .gitignore-skipped contents have a home
      min_lines: 0
  key_links:
    - from: LICENSES.md
      to: BoxeRBackend.CAPABILITIES["license"]
      via: string equality — both say "CC-BY-NC-4.0"
      pattern: "CC-BY-NC-4.0"
---

<objective>
Create LICENSES.md (new repo-root file) with per-dependency SPDX + upstream + scope rows, including the BoxeR CC-BY-NC-4.0 NC-compliance entry. Create models/ directory scaffolding (gitkeep + README documenting layout). This directly satisfies DET-MODELS-08 and lays the groundwork for DET-MODELS-07's pinned checkpoint tree.

Purpose: The NC restriction on BoxeR must be surfaced in the repo (not just in the UI capability badge) so redistributors and auditors have a single source of truth. The models/ README freezes the D-11 layout convention.

Output: LICENSES.md, models/.gitkeep, models/README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-CONTEXT.md
@.planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LICENSES.md at repo root with full SPDX table</name>
  <read_first>
    - .planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-CONTEXT.md D-14 (LICENSES.md + CC-BY-NC surfacing)
    - .planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-RESEARCH.md "LICENSES.md Schema (D-14)" section (lines 857–899)
    - pyproject.toml (to confirm every runtime + optional dep gets a row)
  </read_first>
  <action>
    Create new file `LICENSES.md` at repo root with EXACTLY this content (GitHub-flavored markdown). Key structural point: the main **Runtime Dependencies** table lists ONLY deps argus ships or directly imports. Transitive/indirect deps pulled in by BoxeR's internal pipeline (OWLv2, etc.) are documented in a separate "Indirect / Upstream Dependencies (NOT SHIPPED by argus)" subsection so an auditor reading the file can see at a glance which licenses we take on as a user-facing dep vs. which we merely link through:

    TODO(plan-06): verify BoxeR truly loads OWLv2 at runtime — if BoxeR does NOT actually call into OWLv2, delete the OWLv2 row from LICENSES.md during Plan 06 execution. The entry is inserted under the working assumption that BoxeR's pipeline uses OWLv2 as an internal 2D proposal network, but RESEARCH does not fully confirm this (see Open Risk #1). Plan 06 executes the BoxeR worker script and will make the definitive call.

    ```markdown
    # Third-Party License Inventory

    This document tracks SPDX identifiers and license obligations for every
    runtime dependency in argus. The table is the source of truth — DO NOT add
    a runtime dependency without updating this file.

    ## Runtime Dependencies

    | Dep | SPDX | Scope | Upstream | Note |
    |-----|------|-------|----------|------|
    | Ultralytics YOLOv11 | AGPL-3.0 | Default detector backend (in-process) | https://github.com/ultralytics/ultralytics | **Copyleft** — distributing this codebase as a service triggers AGPL network-use clause. Document in deployment guide. |
    | RT-DETRv2 (PekingU) | Apache-2.0 | Real-time transformer backend (in-process ONNX) | https://huggingface.co/PekingU/rtdetr_v2_r18vd | Permissive |
    | facebook/BoxeR | CC-BY-NC-4.0 | Reference-quality 3D OBB backend (subprocess) | https://github.com/facebookresearch/boxer | **Non-commercial only — research/academic use.** Capability dict surfaces this in the UI per D-14. NOT for production deployments. |
    | DINOv3 (facebook) | DINOv3 License (research-only) | BoxeR feature backbone, downloaded inside subprocess venv | https://huggingface.co/facebook/dinov3-vits16-pretrain-lvd1689m | Coupled with BoxeR's NC restriction. |
    | ONNX Runtime | MIT | RT-DETRv2 inference engine | https://github.com/microsoft/onnxruntime | Permissive |
    | Hugging Face Transformers | Apache-2.0 | Model loading + RT-DETRv2 export | https://github.com/huggingface/transformers | Permissive |
    | huggingface-hub | Apache-2.0 | Snapshot download for pinned checkpoints | https://github.com/huggingface/huggingface_hub | Permissive |
    | optimum (HF) | Apache-2.0 | ONNX export tooling (dev-only, not runtime) | https://github.com/huggingface/optimum | Permissive |
    | msgpack-python | Apache-2.0 | Subprocess bridge wire encoding | https://github.com/msgpack/msgpack-python | Permissive |
    | pyzmq | LGPL-3.0+BSD-3-Clause | Subprocess bridge transport | https://github.com/zeromq/pyzmq | LGPL implications via dynamic linking — accepted (we link against libzmq, do not vendor it). |
    | Open3D | MIT | 3D OBB lifter + geometry | https://github.com/isl-org/Open3D | Permissive |
    | PyTorch | BSD-3-Clause | Tensor runtime for YOLOv11 + (in subprocess) BoxeR | https://github.com/pytorch/pytorch | Permissive |
    | numpy | BSD-3-Clause | Numerical core | https://numpy.org | Permissive |
    | scipy | BSD-3-Clause | Numerical / rotation utilities | https://scipy.org | Permissive |
    | opencv-python-headless | Apache-2.0 | Image preprocessing (letterbox resize) | https://opencv.org | Permissive |
    | scikit-learn | BSD-3-Clause | DBSCAN clustering in PointClusterLifter | https://scikit-learn.org | Permissive |
    | MuJoCo | Apache-2.0 | Simulation engine | https://github.com/google-deepmind/mujoco | Permissive |
    | FastAPI | MIT | Web/coordination layer | https://github.com/tiangolo/fastapi | Permissive |
    | uvicorn | BSD-3-Clause | ASGI server | https://github.com/encode/uvicorn | Permissive |
    | websockets | BSD-3-Clause | WebSocket server | https://github.com/python-websockets/websockets | Permissive |

    ## Indirect / Upstream Dependencies (NOT SHIPPED by argus)

    Below deps are pulled in transitively by BoxeR's internal pipeline when BoxeR is active — argus does NOT expose them as user-selectable backends. Listed here for transparency only.

    | Dep | SPDX | Scope | Upstream | Note |
    |-----|------|-------|----------|------|
    | **[NOT-SHIPPED]** OWLv2 (google) | Apache-2.0 | BoxeR's internal 2D proposal network (loaded inside the BoxeR subprocess venv only) | https://huggingface.co/google/owlv2-base-patch16-ensemble | Permissive. Pulled in transitively by BoxeR; argus does NOT expose OWLv2 as a directly selectable backend (dropped per Phase 5 D-10). If Plan 06 confirms BoxeR does NOT actually load OWLv2 at runtime, this row MUST be deleted. |

    ## NC Compliance Mechanism

    facebook/BoxeR is CC-BY-NC-4.0. argus surfaces this in the UI:
    - `BoxeRBackend.CAPABILITIES["license"] = "CC-BY-NC-4.0"` is rendered as a pill
      in the Detector Dropdown (Phase 3 D-05 capability badge path).
    - Users see the NC restriction inline before selecting BoxeR — no extra modal.

    ## Audit checklist

    - [ ] Every entry in `pyproject.toml [dependencies]` and `[optional-dependencies]` appears here.
    - [ ] Every model checkpoint downloaded by `make download-models` has its license documented.
    - [ ] CC-BY-NC and AGPL deps are surfaced in the UI via capability dict.
    - [ ] Plan 06 has verified whether BoxeR actually loads OWLv2 at runtime; if not, the OWLv2 row in "Indirect / Upstream Dependencies" has been removed.
    ```

    Do NOT omit the NC Compliance Mechanism, Audit checklist, or Indirect/Upstream Dependencies sections — the DET-MODELS-08 test in Wave 0 will grep for "CC-BY-NC-4.0" AND for "NC Compliance Mechanism". The OWLv2 row lives ONLY in the Indirect subsection (not in the main Runtime Dependencies table) to keep the user-facing license surface honest.
  </action>
  <verify>
    <automated>grep -q "CC-BY-NC-4.0" LICENSES.md && grep -q "AGPL-3.0" LICENSES.md && grep -q "Apache-2.0" LICENSES.md && grep -q "NC Compliance Mechanism" LICENSES.md && grep -q "PekingU/rtdetr_v2_r18vd" LICENSES.md && grep -q "facebookresearch/boxer" LICENSES.md && grep -q "Indirect / Upstream Dependencies (NOT SHIPPED by argus)" LICENSES.md && grep -q '\*\*\[NOT-SHIPPED\]\*\* OWLv2' LICENSES.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f LICENSES.md` succeeds
    - `grep -c "CC-BY-NC-4.0" LICENSES.md` returns ≥ 2 (one in table + one in NC compliance section)
    - `grep -c "^| facebook/BoxeR |" LICENSES.md` returns 1
    - `grep -c "^| Ultralytics YOLOv11 |" LICENSES.md` returns 1
    - `grep -c "^| RT-DETRv2 (PekingU) |" LICENSES.md` returns 1
    - `grep -c "^| ONNX Runtime |" LICENSES.md` returns 1
    - `grep -c "AGPL-3.0" LICENSES.md` returns 1 (Ultralytics row)
    - `grep -c "NC Compliance Mechanism" LICENSES.md` returns 1
    - `grep -c "Indirect / Upstream Dependencies (NOT SHIPPED by argus)" LICENSES.md` returns 1
    - `grep -c '\*\*\[NOT-SHIPPED\]\*\* OWLv2' LICENSES.md` returns 1 (OWLv2 row is in the Indirect subsection only, NOT in the main Runtime Dependencies table)
    - OWLv2 does NOT appear in the main Runtime Dependencies table: running `awk '/^## Runtime Dependencies/,/^## Indirect/' LICENSES.md | grep -c "OWLv2"` returns 0
    - File has ≥ 40 lines: `wc -l LICENSES.md` reports ≥ 40
  </acceptance_criteria>
  <done>LICENSES.md ships at repo root with BoxeR NC entry + every runtime dep row + NC compliance mechanism section + separate Indirect/Upstream Dependencies subsection containing the OWLv2 [NOT-SHIPPED] row.</done>
</task>

<task type="auto">
  <name>Task 2: Create models/ directory scaffolding (gitkeep + README)</name>
  <read_first>
    - .planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-CONTEXT.md D-11 (models/ dir layout)
    - .planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-RESEARCH.md "D-11 — Models dir layout" section (tree diagram around lines 384–405)
    - .gitignore (confirm models/orbslam3/ is already excluded specifically, not the whole models/ dir — Phase 5 preserves the v2.0 convention)
  </read_first>
  <action>
    1. Create empty file `models/.gitkeep` (literal zero bytes).

    2. Create `models/README.md` with EXACTLY this content:

    ```markdown
    # Model Checkpoints

    This directory holds pinned-SHA model artifacts fetched by `make download-models`.
    The root `.gitignore` excludes specific large binaries (ORBvoc, weight files) but
    the directory itself is tracked via `.gitkeep` so the layout survives `git clean`.

    ## Layout (Phase 5 D-11)

    ```
    models/<backend-slug>/<sha>/<artifact>
    ```

    Concretely, after `make download-models` completes:

    ```
    models/
    ├── .gitkeep
    ├── README.md                     # this file
    ├── orbslam3/                     # v2.0 — ORB-SLAM3 vocabulary (.gitignored per-file)
    │   ├── ORBvoc.txt
    │   └── ORBvoc.bin
    ├── unitree_go2/                  # v2.0 — robot assets
    ├── rtdetrv2/                     # v3.0 Phase 5 — RT-DETRv2 ONNX checkpoint
    │   └── <RT_DETRV2_SHA>/          # see src/perception/backends/rtdetrv2_backend.py
    │       ├── pt/                   # HF snapshot intermediate (PyTorch .safetensors)
    │       ├── model.onnx            # production runtime artifact
    │       ├── config.json
    │       └── preprocessor_config.json
    └── boxer/                        # v3.0 Phase 5 — BoxeR subprocess checkpoints
        └── <BOXER_SHA>/              # see src/perception/backends/boxer_backend.py
            ├── boxernet/             # BoxerNet weights
            ├── dinov3/               # DINOv3 ViT-S/16 feature backbone
            └── owlv2/                # OWLv2 internal detector (NOT an argus backend)
    ```

    ## Invariants

    - `<backend-slug>` matches the backend `name=` in its `@detector_backend(...)` decorator.
    - `<sha>` is the 40-char hex commit/revision SHA pinned in the backend module's `_SHA` constant.
    - Artifacts inside `<sha>/` are immutable for the lifetime of that SHA. Updating a checkpoint
      requires bumping the SHA constant in the backend file AND updating `EXPECTED_SHA256` in
      `scripts/download_models.py`.
    - `models/` itself is NOT globally gitignored; specific large binaries are excluded per-file
      in `.gitignore` (see ORBvoc entries). Phase 5 does NOT add per-file entries — the
      RT-DETRv2 + BoxeR checkpoints are large but not absurd (<200 MB combined) and their
      SHA-pinned paths make them cache-friendly.

    ## Who writes here?

    - `scripts/download_models.py` (invoked via `make download-models`) — HuggingFace snapshot
      + optimum ONNX export for RT-DETRv2.
    - `scripts/setup_boxer_subprocess.sh` (invoked via `make download-models-boxer`) — git
      clone + BoxeR's own `download_ckpts.sh` for boxer/ subtree.

    Nothing else should write to this tree. Checkpoints are artifacts, not mutable state.
    ```

    Do not deviate from this structure — the Wave 3 integration test at `tests/integration/test_download_models.py` may grep for these path patterns.
  </action>
  <verify>
    <automated>test -f models/.gitkeep && test -f models/README.md && grep -q "models/<backend-slug>/<sha>/<artifact>" models/README.md && grep -q "RT_DETRV2_SHA" models/README.md && grep -q "BOXER_SHA" models/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f models/.gitkeep` succeeds
    - `test -f models/README.md` succeeds
    - `wc -c models/.gitkeep` reports 0 bytes
    - `grep -c "models/<backend-slug>/<sha>/<artifact>" models/README.md` returns ≥ 1
    - `grep -c "rtdetrv2/" models/README.md` returns ≥ 1
    - `grep -c "boxer/" models/README.md` returns ≥ 1
    - `grep -c "<RT_DETRV2_SHA>" models/README.md` returns ≥ 1
    - `grep -c "<BOXER_SHA>" models/README.md` returns ≥ 1
  </acceptance_criteria>
  <done>models/ directory tracked via .gitkeep with a README documenting the D-11 layout.</done>
</task>

</tasks>

<threat_model>
No applicable threats — this plan touches only documentation files (LICENSES.md, README.md) and an empty placeholder. T-5-03 (checkpoint poisoning) is addressed in Plan 11 where sha256 verification lands.
</threat_model>

<verification>
- `git status` shows LICENSES.md, models/.gitkeep, models/README.md as new files.
- `cat LICENSES.md | head -1` prints `# Third-Party License Inventory`.
- `wc -l LICENSES.md` reports ≥ 40 lines.
</verification>

<success_criteria>
LICENSES.md has BoxeR CC-BY-NC-4.0 row + AGPL/Apache/MIT entries + a separate Indirect/Upstream Dependencies subsection for the OWLv2 [NOT-SHIPPED] transitive dep. models/README.md documents the D-11 layout with concrete SHA constant placeholders.
</success_criteria>

<output>
After completion, create `.planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-02-SUMMARY.md`.
</output>
