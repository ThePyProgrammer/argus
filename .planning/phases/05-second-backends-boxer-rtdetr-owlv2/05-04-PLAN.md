---
phase: 05-second-backends-boxer-rtdetr-owlv2
plan: 04
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/perception/test_rtdetrv2_backend.py
  - tests/perception/test_boxer_backend.py
  - tests/perception/test_crash_fallback.py
  - tests/integration/test_download_models.py
  - tests/integration/test_offline_boot.py
  - tests/integration/test_boxer_crash_fallback.py
  - tests/test_licenses_md.py
autonomous: true
requirements: [DET-MODELS-02, DET-MODELS-03, DET-MODELS-06, DET-MODELS-07, DET-MODELS-08]
must_haves:
  truths:
    - "Every test file that Wave 2/3 implementations will fill exists at skeleton stage"
    - "Skeletons use pytest.skip or xfail markers so `pytest --collect-only` sees them but `pytest` does NOT fail"
    - "Every test function name referenced in VALIDATION.md per-task map exists in a test file"
    - "Markers @pytest.mark.slow_boxer / @pytest.mark.network are applied where RESEARCH.md specifies"
  artifacts:
    - path: tests/perception/test_rtdetrv2_backend.py
      provides: DET-MODELS-02 test skeletons (test_construct, test_latency_p95_under_250ms, test_warmup, test_preprocess_letterbox)
    - path: tests/perception/test_boxer_backend.py
      provides: DET-MODELS-03 + DET-MODELS-08 skeletons (test_construct_no_spawn, test_warmup_returns_3d, test_extent_halving, test_capability_license)
    - path: tests/perception/test_crash_fallback.py
      provides: DET-MODELS-06 unit skeletons (test_bridge_hang_raises, test_subprocess_died_raises, test_pool_swaps_to_yolo)
    - path: tests/integration/test_download_models.py
      provides: DET-MODELS-07 network-marked skeletons (test_download_rtdetrv2, test_setup_boxer_idempotent)
    - path: tests/integration/test_offline_boot.py
      provides: DET-MODELS-07 offline gate skeleton (test_all_backends_boot_with_hf_hub_offline)
    - path: tests/integration/test_boxer_crash_fallback.py
      provides: DET-MODELS-06 e2e skeleton (test_kill_nine_triggers_fallback_within_5s)
    - path: tests/test_licenses_md.py
      provides: DET-MODELS-08 doc test (test_boxer_cc_by_nc_present)
  key_links:
    - from: Every test file
      to: Wave 2/3 implementations (backends, pool handler, download script)
      via: pytest collection — implementations must land before skip markers are removed
      pattern: "pytest.skip"
---

<objective>
Create the Nyquist-compliant test scaffolds for every Phase 5 test file. Each skeleton compiles, is collected by pytest, and uses `pytest.skip("Wave N Plan M — not yet implemented")` as the body so Wave 0 test run is green. Wave 2/3 implementations will remove the skip markers one assertion at a time.

Purpose: The validation contract (RESEARCH.md § Validation Architecture, VALIDATION.md Per-Task Verification Map) names specific test functions. Creating them as skip-stubs NOW means:
1. Every later task can reference an exact pytest command (`uv run pytest tests/... -x`)
2. `pytest --collect-only` shows a complete picture of Phase 5 test coverage BEFORE implementations land
3. Removing skip markers on the "green" step of RED→GREEN is a clean, reviewable diff

Output: 7 new test files, each using skip-stubs with the exact function names from VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-CONTEXT.md
@.planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-RESEARCH.md
@.planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-VALIDATION.md
@tests/perception/test_subprocess_bridge_skeleton.py
@tests/perception/test_registry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the 7 test skeleton files with pytest.skip-stubbed functions</name>
  <read_first>
    - .planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-VALIDATION.md § Per-Task Verification Map (table lines 42–58) — source of truth for test function names
    - .planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-RESEARCH.md § Validation Architecture (lines 905–957)
    - tests/perception/test_subprocess_bridge_skeleton.py (canonical example of how existing tests import + mark)
    - tests/perception/test_registry.py (example of how existing tests access DetectorRegistry)
  </read_first>
  <action>
    Create seven new test files. Each file must:
      * Start with a module docstring referencing the requirement(s) it covers
      * Import `pytest`
      * Define each test function with a `pytest.skip("Plan 05-NN (Wave N) fills in: <short description>")` as its first statement — this lets pytest collect the test but skip it as "deferred" not fail
      * Apply markers (`@pytest.mark.slow_boxer`, `@pytest.mark.network`) per VALIDATION.md's "Test Type" column

    --- File 1: `tests/perception/test_rtdetrv2_backend.py` ---
    ```python
    """DET-MODELS-02 — RT-DETRv2 ONNX backend tests (Wave 0 skeleton, Plan 05-04).

    Plan 05-07 (Wave 2) fills in the real assertions. Until then every test is
    skipped via pytest.skip at its first line so `pytest --collect-only` sees the
    full coverage map while `pytest` stays green.
    """
    from __future__ import annotations

    import pytest


    def test_construct_no_onnx_file_raises() -> None:
        pytest.skip("Plan 05-07 (Wave 2) fills in: construction raises when model.onnx missing (D-06 hint).")


    def test_construct_with_onnx_file_succeeds(tmp_path) -> None:
        pytest.skip("Plan 05-07 (Wave 2) fills in: backend constructs cleanly when pinned ONNX artifact exists.")


    def test_preprocess_letterbox_480x640_to_320x320() -> None:
        pytest.skip("Plan 05-07 (Wave 2) fills in: letterbox preserves aspect ratio, pads with black.")


    def test_warmup_runs_one_inference() -> None:
        pytest.skip("Plan 05-07 (Wave 2) fills in: warmup(dummy_frame) returns Detections2D with inference_ms > 0.")


    def test_latency_p95_under_250ms() -> None:
        pytest.skip("Plan 05-07 (Wave 2) fills in: 30-inference P95 < 250 ms on 320x320 (SC#1 proxy).")


    def test_capabilities_include_framework_and_license() -> None:
        pytest.skip("Plan 05-07 (Wave 2) fills in: CAPABILITIES['framework'] == 'onnxruntime', license == 'Apache-2.0'.")


    def test_thread_budget_inherits_from_thread_config() -> None:
        pytest.skip("Plan 05-07 (Wave 2) fills in: intra_op_num_threads reads _thread_config.get_default_budget (D-07).")
    ```

    --- File 2: `tests/perception/test_boxer_backend.py` ---
    ```python
    """DET-MODELS-03 + DET-MODELS-08 — BoxeR composer tests (Wave 0 skeleton, Plan 05-04).

    Plan 05-08 (Wave 2) fills in composer logic and msgpack→OrientedBox3D translation.
    The `test_warmup_returns_3d` integration test is marked `slow_boxer` — it requires
    a live subprocess_venvs/boxer/ venv so is skipped unless explicitly selected.
    """
    from __future__ import annotations

    import pytest


    def test_construct_no_spawn() -> None:
        """D-05 lazy spawn — __init__ must NOT touch filesystem or subprocess."""
        pytest.skip("Plan 05-08 (Wave 2) fills in: BoxeRBackend() does not call Popen, does not open bridge socket.")


    def test_extent_halving() -> None:
        """D-02 gotcha — msgpack sends FULL extents (w,h,d); composer halves for OrientedBox3D."""
        pytest.skip("Plan 05-08 (Wave 2) fills in: composer divides reply['boxes_3d'][i]['w'] by 2 to produce half_extents[0].")


    def test_quaternion_passthrough_no_handcraft() -> None:
        """Phase 1 D-10 invariant — composer passes qx,qy,qz,qw straight into OrientedBox3D(...); no inline construction."""
        pytest.skip("Plan 05-08 (Wave 2) fills in: verify no scipy.spatial.transform import in boxer_backend.py (grep test).")


    def test_capability_license() -> None:
        """DET-MODELS-08 — CAPABILITIES['license'] must equal 'CC-BY-NC-4.0' for Phase 3 D-05 badge render."""
        pytest.skip("Plan 05-08 (Wave 2) fills in: BoxeRBackend.CAPABILITIES['license'] == 'CC-BY-NC-4.0'.")


    def test_capability_outputs_3d_natively() -> None:
        """Phase 3 D-08 — BoxeR bypasses lifter; CAPABILITIES['outputs_3d_natively'] must be True."""
        pytest.skip("Plan 05-08 (Wave 2) fills in: BoxeRBackend.CAPABILITIES['outputs_3d_natively'] is True.")


    def test_available_when_ready_marker_missing() -> None:
        """D-01 — subprocess_venvs/boxer/.ready gates availability."""
        pytest.skip("Plan 05-08 (Wave 2) fills in: BoxeRBackend.available() returns (False, install_hint) when .ready absent.")


    @pytest.mark.slow_boxer
    def test_warmup_returns_3d() -> None:
        """Integration: real bridge spawn + real BoxeR inference returns Detections3D."""
        pytest.skip("Plan 05-08 (Wave 2) fills in: requires subprocess_venvs/boxer/.ready (run make download-models-boxer).")
    ```

    --- File 3: `tests/perception/test_crash_fallback.py` ---
    ```python
    """DET-MODELS-06 — Crash fallback unit tests (Wave 0 skeleton, Plan 05-04).

    Plan 05-05 (Wave 1) lands bridge typed exceptions (BridgeHangError, SubprocessDiedError).
    Plan 05-09 (Wave 2) lands DetectorWorkerPool.on_backend_crash handler.
    """
    from __future__ import annotations

    import pytest


    def test_bridge_hang_raises() -> None:
        """SubprocessDetectorBridge.send_frame raises BridgeHangError on RCVTIMEO (Plan 05-05)."""
        pytest.skip("Plan 05-05 (Wave 1) fills in: bridge.send_frame raises BridgeHangError when worker silent > 5s.")


    def test_subprocess_died_raises() -> None:
        """SubprocessDetectorBridge.send_frame raises SubprocessDiedError when Popen exited (Plan 05-05)."""
        pytest.skip("Plan 05-05 (Wave 1) fills in: bridge.send_frame raises SubprocessDiedError after Popen.poll != None.")


    def test_pool_on_backend_crash_emits_ws_message() -> None:
        """DetectorWorkerPool.on_backend_crash appends crash_fallback to streaming_viz._message_queue."""
        pytest.skip("Plan 05-09 (Wave 2) fills in: message shape per RESEARCH D-03 + SLAM precedent.")


    def test_pool_swaps_to_yolo() -> None:
        """on_backend_crash creates YOLOv11Backend, warms it, atomically swaps under _swap_lock."""
        pytest.skip("Plan 05-09 (Wave 2) fills in: all workers' _detector is YOLOv11Backend instance after call.")


    def test_pool_marks_crashed_backend_unavailable() -> None:
        """D-04 — on_backend_crash calls DetectorRegistry.set_available(name, False, reason=...)."""
        pytest.skip("Plan 05-09 (Wave 2) fills in: DetectorRegistry.list_backends() shows boxer available=False with reason.")
    ```

    --- File 4: `tests/integration/test_download_models.py` ---
    ```python
    """DET-MODELS-07 — Download pipeline integration tests (Wave 0 skeleton, Plan 05-04).

    Plan 05-11 (Wave 3) fills in the real assertions. Tests marked `network` because
    they hit HuggingFace Hub + GitHub; skipped by default per pyproject marker registration.
    """
    from __future__ import annotations

    import pytest


    @pytest.mark.network
    def test_download_rtdetrv2(tmp_path) -> None:
        """snapshot_download + optimum ONNX export produce models/rtdetrv2/<SHA>/model.onnx (Plan 05-11)."""
        pytest.skip("Plan 05-11 (Wave 3) fills in: exercises scripts/download_models.py --backend rtdetrv2.")


    @pytest.mark.network
    def test_setup_boxer_idempotent(tmp_path) -> None:
        """D-01 — .ready marker short-circuits on second run (Plan 05-06 ships script, Plan 05-11 tests)."""
        pytest.skip("Plan 05-11 (Wave 3) fills in: run setup_boxer_subprocess.sh twice, second run is no-op.")


    @pytest.mark.network
    def test_sha256_manifest_verification(tmp_path) -> None:
        """T-5-03 mitigation — sha256 of downloaded model.onnx matches EXPECTED_SHA256."""
        pytest.skip("Plan 05-11 (Wave 3) fills in: after first download, EXPECTED_SHA256 lookup succeeds.")


    @pytest.mark.network
    def test_rollback_on_partial_failure(tmp_path, monkeypatch) -> None:
        """D-13 — partial download + exception leaves no orphan models/<backend>/<sha>/ dir."""
        pytest.skip("Plan 05-11 (Wave 3) fills in: inject optimum export exception; target dir removed.")
    ```

    --- File 5: `tests/integration/test_offline_boot.py` ---
    ```python
    """DET-MODELS-07 — Offline boot gate (Wave 0 skeleton, Plan 05-04).

    Plan 05-11 (Wave 3) fills in; marked `slow_boxer` because it spawns the real
    BoxeR subprocess.
    """
    from __future__ import annotations

    import pytest


    @pytest.mark.slow_boxer
    def test_all_backends_boot_with_hf_hub_offline() -> None:
        """SC#4 — after `make download-models`, all 3 backends boot with HF_HUB_OFFLINE=1."""
        pytest.skip("Plan 05-11 (Wave 3) fills in: spawns coordinator with HF_HUB_OFFLINE=1 + empty HF_HOME.")
    ```

    --- File 6: `tests/integration/test_boxer_crash_fallback.py` ---
    ```python
    """DET-MODELS-06 — End-to-end kill -9 → fallback within 5s (Wave 0 skeleton, Plan 05-04).

    Plan 05-12 (Wave 3) fills in; marked `slow_boxer` because it requires the real
    BoxeR subprocess to be killable.
    """
    from __future__ import annotations

    import pytest


    @pytest.mark.slow_boxer
    def test_kill_nine_triggers_fallback_within_5s() -> None:
        """SC#3 — os.kill(boxer_pid, SIGKILL); assert crash_fallback WS arrives + active backend switches within 5s."""
        pytest.skip("Plan 05-12 (Wave 3) fills in: spawns coordinator + BoxeR, SIGKILLs worker, measures elapsed.")
    ```

    --- File 7: `tests/test_licenses_md.py` ---
    ```python
    """DET-MODELS-08 — LICENSES.md content verification (Wave 0 skeleton, Plan 05-04).

    Unlike the other Wave 0 skeletons, this test ALREADY has an implementation
    after Plan 05-02 lands (LICENSES.md is created in that plan). We still use
    pytest.skip stubs here so Wave 0 validation is clean across all 7 files;
    Plan 05-02 removes these skips in its own commit.
    """
    from __future__ import annotations

    from pathlib import Path

    import pytest


    LICENSES_PATH = Path(__file__).parent.parent / "LICENSES.md"


    def test_licenses_md_exists() -> None:
        pytest.skip("Plan 05-02 fills in: simple file existence check.")


    def test_boxer_cc_by_nc_present() -> None:
        pytest.skip("Plan 05-02 fills in: LICENSES.md contains 'CC-BY-NC-4.0'.")


    def test_agpl_apache_mit_entries_present() -> None:
        pytest.skip("Plan 05-02 fills in: LICENSES.md lists AGPL-3.0, Apache-2.0, MIT rows.")


    def test_nc_compliance_section_present() -> None:
        pytest.skip("Plan 05-02 fills in: 'NC Compliance Mechanism' H2 section exists.")
    ```

    Create the `tests/integration/` directory if it doesn't exist (it currently holds test_pool_end_to_end.py so should already exist — `ls tests/integration/` to confirm; mkdir -p if not). Ensure every new test file has `from __future__ import annotations` at the top.

    Do NOT add any real test logic — every function must be `pytest.skip(...)` one-liner (with optional docstring above). Real implementations land in later plans per the skip reason strings.
  </action>
  <verify>
    <automated>test -f tests/perception/test_rtdetrv2_backend.py && test -f tests/perception/test_boxer_backend.py && test -f tests/perception/test_crash_fallback.py && test -f tests/integration/test_download_models.py && test -f tests/integration/test_offline_boot.py && test -f tests/integration/test_boxer_crash_fallback.py && test -f tests/test_licenses_md.py && uv run pytest tests/perception/test_rtdetrv2_backend.py tests/perception/test_boxer_backend.py tests/perception/test_crash_fallback.py tests/integration/test_download_models.py tests/integration/test_offline_boot.py tests/integration/test_boxer_crash_fallback.py tests/test_licenses_md.py --collect-only -q 2>&1 | grep -v error | head -50 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 7 test files exist
    - `uv run pytest tests/perception/test_rtdetrv2_backend.py --collect-only -q 2>&1 | grep -c "test_construct_no_onnx_file_raises\|test_latency_p95_under_250ms\|test_warmup_runs_one_inference\|test_capabilities_include_framework_and_license\|test_preprocess_letterbox_480x640_to_320x320\|test_thread_budget_inherits_from_thread_config\|test_construct_with_onnx_file_succeeds"` returns ≥ 7
    - `uv run pytest tests/perception/test_boxer_backend.py --collect-only -q 2>&1 | grep -c "test_construct_no_spawn\|test_extent_halving\|test_capability_license\|test_warmup_returns_3d\|test_capability_outputs_3d_natively\|test_quaternion_passthrough_no_handcraft\|test_available_when_ready_marker_missing"` returns ≥ 7
    - `uv run pytest tests/perception/test_crash_fallback.py --collect-only -q 2>&1 | grep -c "test_bridge_hang_raises\|test_subprocess_died_raises\|test_pool_swaps_to_yolo\|test_pool_on_backend_crash_emits_ws_message\|test_pool_marks_crashed_backend_unavailable"` returns ≥ 5
    - `uv run pytest tests/perception/test_rtdetrv2_backend.py tests/perception/test_boxer_backend.py tests/perception/test_crash_fallback.py tests/integration/test_download_models.py tests/integration/test_offline_boot.py tests/integration/test_boxer_crash_fallback.py tests/test_licenses_md.py -x` exits 0 (all tests skip, none fail) — skipping counts as passing
    - No PytestUnknownMarkWarning in output (markers registered by Plan 01)
    - `grep -c "pytest.skip" tests/perception/test_rtdetrv2_backend.py tests/perception/test_boxer_backend.py tests/perception/test_crash_fallback.py tests/integration/test_download_models.py tests/integration/test_offline_boot.py tests/integration/test_boxer_crash_fallback.py tests/test_licenses_md.py` reports ≥ 25 total skips
  </acceptance_criteria>
  <done>7 test files compile, pytest collects ≥ 25 test functions across them, all skip cleanly with no unknown-mark warnings.</done>
</task>

</tasks>

<threat_model>
No applicable threats — test scaffolds do not touch external services, user input, or writable state. T-5-05 (Popen stdout DoS) is addressed in Plan 05 where the bridge gains a drain thread; this plan just creates the skip-stubbed test that will eventually verify it.
</threat_model>

<verification>
- `uv run pytest tests/perception/ tests/integration/ tests/test_licenses_md.py --collect-only -q 2>&1 | tail -5` shows a summary with 0 errors and the total test count including the new skeletons.
- `uv run pytest tests/ -x --timeout=30 -m "not slow_boxer and not network"` stays green (skeletons skip, existing Phase 1–4 tests still pass).
</verification>

<success_criteria>
Every test function named in VALIDATION.md's Per-Task Verification Map exists as a pytest.skip stub. Full `pytest` run is green post-scaffold. Wave 2/3 implementations can remove skip markers one at a time.
</success_criteria>

<output>
After completion, create `.planning/phases/05-second-backends-boxer-rtdetr-owlv2/05-04-SUMMARY.md`.
</output>
