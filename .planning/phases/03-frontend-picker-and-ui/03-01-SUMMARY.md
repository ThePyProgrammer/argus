---
phase: 03
plan: 01
subsystem: frontend
tags:
  - frontend
  - test-infra
  - vitest
  - wave-0
requirements:
  - DET-UI-06
dependency_graph:
  requires: []
  provides:
    - frontend-vitest-runner
    - frontend-jsdom-env
  affects:
    - 03-11 (detectorStore.shape.test.ts will run here)
    - any future frontend Vitest test
tech_stack:
  added:
    - vitest@4.1.4
    - jsdom@29.0.2
  patterns:
    - "Vitest defineConfig with jsdom env + globals=true"
key_files:
  created:
    - frontend/vitest.config.ts
  modified:
    - frontend/package.json
    - frontend/package-lock.json
decisions:
  - "Used jsdom env (not node) per Pitfall #1: zustand transitively pulls DOM types via React"
  - "Kept config minimal: no setupFiles, no coverage (added by later plans if needed)"
  - "Accepted Vitest 4.x exit-1 behavior on no-tests-found per plan's <verify> block (test $? -eq 0 -o $? -eq 1)"
metrics:
  duration: ~3min
  completed_date: 2026-04-14
  tasks: 2
  files: 3
  commits: 2
---

# Phase 03 Plan 01: Wave 0 — Vitest Bootstrap Summary

Bootstrapped Vitest 4.1.4 + jsdom 29.0.2 for the frontend test runner, satisfying the Wave 0 prerequisite for any downstream plan that ships a Vitest test (notably Plan 11's DET-UI-06 detectorStore.shape.test.ts).

## What Shipped

- **`frontend/package.json`**: added `vitest@^4.1.4` and `jsdom@^29.0.2` to devDependencies; added `test` (`vitest run`) and `test:watch` (`vitest`) scripts. Existing `dev`/`build`/`preview` scripts and runtime deps untouched.
- **`frontend/package-lock.json`**: regenerated by npm install (163 transitive packages added; sha512 integrity hashes captured per T-03-01 mitigation).
- **`frontend/vitest.config.ts`** (new, 10 lines): `defineConfig` with `globals: true`, `environment: 'jsdom'`, and an include glob matching both `src/**/__tests__/**/*.test.ts` and `src/**/*.test.ts`.

## Tasks Executed

| Task | Name                                            | Commit  | Files                                                   |
| ---- | ----------------------------------------------- | ------- | ------------------------------------------------------- |
| 1    | Install Vitest + jsdom and add test scripts     | daa1afc | frontend/package.json, frontend/package-lock.json       |
| 2    | Create vitest.config.ts with jsdom env          | 511e7bc | frontend/vitest.config.ts                               |

## Verification (plan-level)

```bash
$ cd frontend && npm ls vitest jsdom --depth=0
argus-frontend@0.1.0 /…/frontend
├── jsdom@29.0.2
└── vitest@4.1.4

$ cd frontend && npx tsc --noEmit
# exit 0 — no type regression

$ cd frontend && npm test
# Vitest 4.1.4: "No test files found, exiting with code 1"
# Plan's <verify> accepts exit 0 OR 1 (test $? -eq 0 -o $? -eq 1) → OK
```

All three plan-level success_criteria verified.

## Version Drift Observed (vs Research's 2026-04-14 snapshot)

None — npm registry resolved exactly:

| Dep    | Research target | npm resolved | Drift |
| ------ | --------------- | ------------ | ----- |
| vitest | ^4.1.4          | 4.1.4        | none  |
| jsdom  | ^29.0.2         | 29.0.2       | none  |

## Deviations from Plan

### Documentation Note (no behavior change)

**1. [Note] Vitest 4.x exits 1 (not 0) when no tests found**
- **Found during:** Task 2 verification
- **Issue:** Plan acceptance bullet states "`npm test` exits 0... per Vitest 4.x default" but Vitest 4.1.4 actually exits 1 on no-tests-found unless `--passWithNoTests` is passed. Plan's `<verify>` block (`test $? -eq 0 -o $? -eq 1`) anticipated this and accepts either code.
- **Fix:** None applied. Kept `"test": "vitest run"` to satisfy Task 1's literal grep acceptance (`grep -E '"test":\s*"vitest run"'` requires the value end at `run"`). The verify-level acceptance (exit 0 or 1) is met. Once Plan 11 lands its first test, exit-on-no-tests becomes a non-issue (tests will exist → exit 0 on green, 1 on failure — desired behavior).
- **Files modified:** none (pure documentation)
- **Commit:** n/a

No auto-fixes (Rules 1–3) and no architectural decisions (Rule 4) needed. No auth gates encountered.

## Threat Surface Scan

No new trust boundaries introduced beyond those already in the plan's `<threat_model>`. T-03-01 (npm integrity) is mitigated by package-lock.json sha512 hashes committed in `daa1afc`. T-03-02 / T-03-03 remain accepted per the plan rationale.

## Wave 0 Status

Plan 11 (DET-UI-06 detectorStore.shape.test.ts) and any later Vitest-using plan can now `npm test` from `frontend/` and have the runner discover their tests via the `src/**/*.test.ts` and `src/**/__tests__/**/*.test.ts` globs. Wave 0 frontend test infrastructure is complete.

## Self-Check: PASSED

Files exist:
- FOUND: frontend/vitest.config.ts
- FOUND: frontend/package.json (modified — `"test": "vitest run"` present)
- FOUND: frontend/package-lock.json (modified)

Commits exist:
- FOUND: daa1afc — chore(03-01): install vitest@^4.1.4 + jsdom@^29.0.2 and add test scripts
- FOUND: 511e7bc — feat(03-01): add vitest.config.ts with jsdom env

Plan-level verify passed (npm ls + tsc + npm test all green per plan acceptance rules).
