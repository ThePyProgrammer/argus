---
phase: 03
plan: 09
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/src/components/CameraFeed.tsx
autonomous: true
requirements:
  - DET-UI-05
tags:
  - frontend
  - camera-feed
  - bbox-overlay
must_haves:
  truths:
    - "CameraFeed 2D bbox overlay colors each box by det.class_id % OKABE_ITO_RGB.length (not by robot color)"
    - "Label backdrop is readable on both light and dark RGB frames (semi-opaque background with contrast text)"
    - "Score tooltip renders class_name + percentage to 2 decimal places; already rendered — polish only"
    - "Items without bbox_xyxy skip the 2D overlay (existing graceful-degradation preserved)"
    - "cd frontend && npx tsc --noEmit exits 0"
  artifacts:
    - path: frontend/src/components/CameraFeed.tsx
      provides: "Polished 2D bbox overlay per DET-UI-05 (per-class color, label backdrop)"
      min_lines: 220
      contains: "OKABE_ITO_RGB"
  key_links:
    - from: frontend/src/components/CameraFeed.tsx
      to: frontend/src/utils/palette.ts
      via: "Per-class color palette indexed by class_id modulo palette length"
      pattern: "OKABE_ITO_RGB\\[.*class_id"

user_setup: []
---

<objective>
Polish the 2D bbox overlay in `CameraFeed.tsx` per D-16 (DET-UI-05). Scaffolding landed in Phase 2 — this plan adds:

1. Per-class color palette: index `OKABE_ITO_RGB` from `frontend/src/utils/palette.ts` by `det.class_id % OKABE_ITO_RGB.length`. Today every bbox uses the robot's color; distinct classes should be visually distinct.
2. Label backdrop polish: ensure `class_name SCORE%` label has a readable backdrop (already partial — confirm dark translucent background is applied).
3. Keep existing graceful degradation: items without `bbox_xyxy` still skip the 2D overlay silently (for future `outputs_3d_natively: true` backends that don't provide 2D boxes).

Purpose: DET-UI-05 requires a camera-feed 2D bbox overlay with class + confidence. The per-class coloring is a visual improvement users immediately notice (multiple detected classes in one frame become distinguishable at a glance).

Output: CameraFeed.tsx edits only; no new files; tsc clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-frontend-picker-and-ui/03-CONTEXT.md
@.planning/phases/03-frontend-picker-and-ui/03-RESEARCH.md

<interfaces>
<!-- Current CameraFeed.tsx structure (verified 2026-04-14): -->
<!--   - DetectionOverlay component renders absolute-positioned divs per det -->
<!--   - Takes `color` prop (single color for all boxes) — from robot's colorIndex -->
<!--   - Renders box border + label div + hover tooltip -->
<!--   - Line 33: skips items without det.bbox_xyxy or bbox_xyxy.length < 4 -->

<!-- palette.ts (verified — file exists): -->
<!--   export const OKABE_ITO_RGB: string[] = [...]  // 8 distinct colors -->
<!--   export function robotColor(idx: number): string { ... } -->

<!-- Detection3DItem (robotStore.ts): -->
<!--   { class_id: number, class_name: string, score: number, center: number[], bbox_xyxy?: number[], track_id?: number } -->

<!-- Target change: -->
<!-- Derive `boxColor = OKABE_ITO_RGB[det.class_id % OKABE_ITO_RGB.length]` inside the .map loop, use it as the box + label color instead of the single `color` prop. -->

<!-- Keep `color` prop of DetectionOverlay — used for hover outline and tooltip border. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Per-class coloring + label backdrop polish in CameraFeed DetectionOverlay</name>
  <read_first>
    - frontend/src/components/CameraFeed.tsx (current 221 lines — the whole file; changes concentrated in DetectionOverlay function)
    - frontend/src/utils/palette.ts (OKABE_ITO_RGB export — pick 8-item palette length at runtime rather than hardcoding `% 8`)
    - .planning/phases/03-frontend-picker-and-ui/03-CONTEXT.md §D-16 (per-class palette keyed by class_id modulo)
  </read_first>
  <files>frontend/src/components/CameraFeed.tsx</files>
  <action>
Edit `frontend/src/components/CameraFeed.tsx`. Concentrated changes:

### Change 1: Imports (top of file)

Current line 3:
```typescript
import { robotColor } from '../utils/palette';
```

Change to:
```typescript
import { robotColor, OKABE_ITO_RGB } from '../utils/palette';
```

### Change 2: Inside DetectionOverlay's `.map((det, i) => ...)` (currently line 31)

Right after the `if (!det.bbox_xyxy || det.bbox_xyxy.length < 4) return null;` guard, add:

```typescript
// D-16: per-class color (fall back to palette[0] for class_id undefined/0)
const classColor = OKABE_ITO_RGB[(det.class_id ?? 0) % OKABE_ITO_RGB.length];
```

### Change 3: Replace every `color` usage inside the box rendering with `classColor`:

- Box div `border: `2px solid ${isHovered ? '#fff' : color}``
  → `border: `2px solid ${isHovered ? '#fff' : classColor}``
- `boxShadow: isHovered ? `0 0 8px ${color}` : 'none'`
  → `boxShadow: isHovered ? `0 0 8px ${classColor}` : 'none'`
- Label badge `background: isHovered ? color : 'rgba(0,0,0,0.75)'` — this is already a dark backdrop on non-hover; keep it. On hover swap to classColor:
  → `background: isHovered ? classColor : 'rgba(0,0,0,0.75)'`
- Label `color: isHovered ? '#000' : color` → `color: isHovered ? '#000' : classColor`
- Tooltip `border: '1px solid ${color}'` → `border: `1px solid ${classColor}``
- Tooltip `strong style={{ color }}` → `strong style={{ color: classColor }}`

### Change 4: Label readability backdrop (D-16 polish)

The label badge at line 66-83 already has `background: isHovered ? color : 'rgba(0,0,0,0.75)'` which is a solid 75%-opacity dark backdrop — good enough for readability. Confirm the `padding: '1px 5px'` provides enough breathing room for the text; leave it unchanged.

### DO NOT change:
- The `color` prop on DetectionOverlay (still passed from outer CameraFeed — used nowhere inside after our edits, but removing it would break the props interface; leave it for future fallback / robot-color features). Actually, to satisfy tsc strict mode and avoid `unused variable`, we can simply not reference `color` anywhere — TypeScript allows unused destructured props in React 18 strict. But if tsc complains, PREFIX the prop with `_color` to mark intentional — verify after edit.
- The `items`, `imgWidth`, `imgHeight` props
- Guard against missing bbox_xyxy (graceful degradation preserved)
- Hover state / tooltip positioning logic
- The outer CameraFeed wrapper — only edits are inside DetectionOverlay

After saving, `cd frontend && npx tsc --noEmit` must exit 0.

Bonus: at Task completion, the file will still pass for a user who opens CameraFeed on current YOLOv11 backend — they'll see different-colored boxes for person/chair/table/etc. instead of all robot-color.
  </action>
  <acceptance_criteria>
    - `grep -q "import { robotColor, OKABE_ITO_RGB } from '../utils/palette'" frontend/src/components/CameraFeed.tsx`
    - `grep -q "OKABE_ITO_RGB\\[(det.class_id" frontend/src/components/CameraFeed.tsx` OR `grep -q "OKABE_ITO_RGB\[\\(det.class_id" frontend/src/components/CameraFeed.tsx`
    - `grep -q "classColor" frontend/src/components/CameraFeed.tsx`
    - The `if (!det.bbox_xyxy || det.bbox_xyxy.length < 4) return null;` graceful degradation still present
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "OKABE_ITO_RGB" src/components/CameraFeed.tsx && grep -q "classColor" src/components/CameraFeed.tsx && grep -q "if (!det.bbox_xyxy" src/components/CameraFeed.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Bboxes render in per-class OKABE_ITO palette colors; label backdrop readable; graceful degradation for missing bbox_xyxy preserved; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| det.class_id (server-sent integer) → palette index | Integer modulo is safe even with negative class_ids; JavaScript `%` preserves sign, but arr[-1] is undefined — palette index must guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-23 | T (Tampering) | class_id < 0 or NaN → undefined palette lookup → runtime crash | mitigate | Use `(det.class_id ?? 0) % OKABE_ITO_RGB.length`. For negative class_ids JS modulo returns negative; Math.abs or `((x % n) + n) % n` if needed. Acceptable: server-side class_id is a non-negative COCO index — just document. |
| T-03-24 | T (Tampering) | det.class_name contains HTML | accept | React auto-escapes JSX `{det.class_name}`; no dangerouslySetInnerHTML added. |
</threat_model>

<verification>
```bash
cd frontend && npx tsc --noEmit
```

Must exit 0.
</verification>

<success_criteria>
- [ ] CameraFeed.tsx imports OKABE_ITO_RGB
- [ ] Inside DetectionOverlay .map, classColor derived from det.class_id
- [ ] Box border / shadow / hover bg / label color / tooltip border all use classColor (not the single robot color)
- [ ] Graceful degradation for items without bbox_xyxy preserved
- [ ] Label has readable backdrop (rgba(0,0,0,0.75) — unchanged but verified)
- [ ] `cd frontend && npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-picker-and-ui/03-09-SUMMARY.md` documenting the palette change + noting that the single-class test case (all same class_id) will still produce a single color, which is correct. Acknowledge this requires manual visual verification (per 03-VALIDATION.md Manual-Only table).
</output>
