---
phase: 07-align-evaluation-action-mode-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.py
  - docs/locomotion-benchmark.md
  - tests/test_main_args.py
  - tests/test_locomotion_benchmark_docs.py
autonomous: true
requirements:
  - LOC-ENV-04
  - LOC-EVAL-03
requirements_addressed:
  - LOC-ENV-04
  - LOC-EVAL-03
must_haves:
  truths:
    - "CLI help tells users velocity_command is the evaluator-runnable mode for argus eval-locomotion."
    - "CLI help and benchmark docs state joint_position and residual_baseline are env-supported seams that fail fast in evaluation until explicit action sources exist."
    - "Documentation still lists all three environment action modes without claiming non-default evaluator runs are supported."
  artifacts:
    - path: "src/main.py"
      provides: "argparse help for eval-locomotion action-mode contract"
      contains: "velocity_command is the evaluator-runnable mode"
    - path: "docs/locomotion-benchmark.md"
      provides: "canonical benchmark guide action-mode contract"
      contains: "argus eval-locomotion currently runs `velocity_command`"
    - path: "tests/test_main_args.py"
      provides: "CLI help contract regression"
      contains: "test_eval_locomotion_help_lists_action_mode_contract"
    - path: "tests/test_locomotion_benchmark_docs.py"
      provides: "docs contract drift guard"
      contains: "test_locomotion_benchmark_guide_states_evaluator_action_mode_contract"
  key_links:
    - from: "src/main.py"
      to: "tests/test_main_args.py"
      via: "python -m src.main eval-locomotion --help contains exact runnable/deferred wording"
      pattern: "velocity_command is the evaluator-runnable mode"
    - from: "docs/locomotion-benchmark.md"
      to: "tests/test_locomotion_benchmark_docs.py"
      via: "content guard asserts docs distinguish env modes from evaluator-runnable mode"
      pattern: "env-supported seams"
---

<objective>
Align user-facing CLI help and benchmark documentation with the implemented evaluation action-mode contract.

Purpose: Phase 7 is not allowed to leave users believing all env action modes are runnable through `argus eval-locomotion`. This plan makes the CLI/docs boundary explicit while keeping env-supported `joint_position` and `residual_baseline` seams visible for future controller work.
Output: Help text, guide text, and deterministic content tests for the action-mode contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-align-evaluation-action-mode-contract/07-RESEARCH.md
@.planning/phases/07-align-evaluation-action-mode-contract/07-VALIDATION.md
@.planning/phases/07-align-evaluation-action-mode-contract/07-PATTERNS.md
@src/main.py
@docs/locomotion-benchmark.md
@tests/test_main_args.py
@tests/test_locomotion_benchmark_docs.py

<interfaces>
Existing CLI flag location in `src/main.py`:
```python
eval_parser.add_argument("--action-mode", default="velocity_command")
```

Chosen public wording fragments for this phase:
```text
velocity_command is the evaluator-runnable mode
joint_position and residual_baseline are env-supported seams that fail fast in eval-locomotion until explicit action sources exist
```

Existing docs must continue to mention all env action modes:
```text
velocity_command
joint_position
residual_baseline
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 CLI and docs wording guards</name>
  <files>tests/test_main_args.py, tests/test_locomotion_benchmark_docs.py</files>
  <read_first>
    - .planning/phases/07-align-evaluation-action-mode-contract/07-VALIDATION.md
    - .planning/phases/07-align-evaluation-action-mode-contract/07-PATTERNS.md
    - tests/test_main_args.py
    - tests/test_locomotion_benchmark_docs.py
    - src/main.py
    - docs/locomotion-benchmark.md
  </read_first>
  <behavior>
    - In `tests/test_main_args.py`, add or extend a help test named `test_eval_locomotion_help_lists_action_mode_contract` using the existing subprocess style: run `.venv/bin/python -m src.main eval-locomotion --help`, assert return code 0, then assert combined stdout/stderr contains `--action-mode`, `velocity_command is the evaluator-runnable mode`, and `joint_position and residual_baseline are env-supported seams`.
    - In `tests/test_locomotion_benchmark_docs.py`, add `test_locomotion_benchmark_guide_states_evaluator_action_mode_contract`; read `docs/locomotion-benchmark.md` and assert it contains `argus eval-locomotion currently runs `velocity_command``, `joint_position` and `residual_baseline`, `env-supported seams`, and `fail fast`.
    - Preserve existing docs guards that require `velocity_command`, `joint_position`, and `residual_baseline` to appear in the guide.
  </behavior>
  <action>Create test-first guards for `07-VALIDATION.md` row 44. The tests must require exact public wording fragments so docs/help cannot drift: `velocity_command is the evaluator-runnable mode`, `joint_position and residual_baseline are env-supported seams`, `argus eval-locomotion currently runs `velocity_command``, and `fail fast`. Do not use subjective assertions. Do not add network calls or import-heavy runtime setup beyond existing test patterns.</action>
  <acceptance_criteria>
    - `grep -R "test_eval_locomotion_help_lists_action_mode_contract" tests/test_main_args.py` finds exactly one test definition.
    - `grep -R "velocity_command is the evaluator-runnable mode" tests/test_main_args.py` finds the CLI help assertion.
    - `grep -R "joint_position and residual_baseline are env-supported seams" tests/test_main_args.py` finds the CLI help assertion.
    - `grep -R "test_locomotion_benchmark_guide_states_evaluator_action_mode_contract" tests/test_locomotion_benchmark_docs.py` finds exactly one test definition.
    - `grep -R "argus eval-locomotion currently runs .*velocity_command" tests/test_locomotion_benchmark_docs.py` finds the docs assertion.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; cli=Path('tests/test_main_args.py').read_text(); docs=Path('tests/test_locomotion_benchmark_docs.py').read_text(); assert 'test_eval_locomotion_help_lists_action_mode_contract' in cli; assert 'velocity_command is the evaluator-runnable mode' in cli; assert 'joint_position and residual_baseline are env-supported seams' in cli; assert 'test_locomotion_benchmark_guide_states_evaluator_action_mode_contract' in docs; assert 'argus eval-locomotion currently runs' in docs and 'velocity_command' in docs and 'fail fast' in docs"</automated>
  </verify>
  <done>CLI and documentation tests fail until the selected action-mode contract is written into help and docs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update eval-locomotion help text</name>
  <files>src/main.py, tests/test_main_args.py</files>
  <read_first>
    - .planning/phases/07-align-evaluation-action-mode-contract/07-RESEARCH.md
    - .planning/phases/07-align-evaluation-action-mode-contract/07-PATTERNS.md
    - src/main.py
    - tests/test_main_args.py
  </read_first>
  <action>In `src/main.py`, update the `eval_parser.add_argument("--action-mode", ...)` call to keep `default="velocity_command"` and set a concrete help string: `Action mode for evaluation. velocity_command is the evaluator-runnable mode; joint_position and residual_baseline are env-supported seams that fail fast in eval-locomotion until explicit action sources exist.` Keep the lazy imports inside `run_eval_locomotion_mode()` unchanged. Do not import `src.locomotion.actions` at module import time. Do not change parser defaults for controller, scenario, seed, matrix config, output root, max episode steps, sim steps per frame, heightfield size, or saved-run regeneration.</action>
  <acceptance_criteria>
    - `grep -n "--action-mode" src/main.py` finds the eval-locomotion argument.
    - `grep -n "velocity_command is the evaluator-runnable mode" src/main.py` finds the help text.
    - `grep -n "joint_position and residual_baseline are env-supported seams" src/main.py` finds the help text.
    - `grep -n "default=\"velocity_command\"" src/main.py` confirms the CLI default is unchanged.
    - `uv run python -m pytest tests/test_main_args.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_main_args.py -q</automated>
  </verify>
  <done>`argus eval-locomotion --help` exposes the implemented evaluator-runnable/deferred action-mode boundary without changing CLI defaults or import behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update benchmark guide action-mode contract</name>
  <files>docs/locomotion-benchmark.md, tests/test_locomotion_benchmark_docs.py</files>
  <read_first>
    - .planning/phases/07-align-evaluation-action-mode-contract/07-RESEARCH.md
    - .planning/phases/07-align-evaluation-action-mode-contract/07-PATTERNS.md
    - docs/locomotion-benchmark.md
    - tests/test_locomotion_benchmark_docs.py
  </read_first>
  <action>In `docs/locomotion-benchmark.md`, update the action-mode section to distinguish environment support from evaluator-runnable support. Include this exact sentence: `argus eval-locomotion currently runs `velocity_command`; `joint_position` and `residual_baseline` are env-supported seams that fail fast in evaluation until explicit action sources exist.` Keep the table entries for all three env modes. For `velocity_command`, state it is the default benchmark/evaluator mode for `analytical_trot`. For `joint_position`, state it is a 12-value direct joint-position env seam and not currently evaluator-runnable without an explicit action source. For `residual_baseline`, state it is a 12-value residual-over-baseline env seam and not currently evaluator-runnable without a trained residual policy/action source. Keep the smoke command and matrix JSON example using `"action_mode": "velocity_command"`. Do not document RL training, MPC/WBC implementation, ROS/hardware, or frontend changes as part of v4.0.</action>
  <acceptance_criteria>
    - `grep -n 'argus eval-locomotion currently runs `velocity_command`' docs/locomotion-benchmark.md` finds the exact sentence.
    - `grep -n "env-supported seams that fail fast" docs/locomotion-benchmark.md` finds the fail-fast docs wording.
    - `grep -n "12-value direct joint-position env seam" docs/locomotion-benchmark.md` finds the joint-position seam wording.
    - `grep -n "12-value residual-over-baseline env seam" docs/locomotion-benchmark.md` finds the residual seam wording.
    - `grep -n '"action_mode": "velocity_command"' docs/locomotion-benchmark.md` confirms the matrix example uses the runnable mode.
    - `uv run python -m pytest tests/test_locomotion_benchmark_docs.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_locomotion_benchmark_docs.py -q</automated>
  </verify>
  <done>The canonical benchmark guide explains all env action modes while accurately saying only `velocity_command` is runnable by current evaluation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI help/docs -> user invocation | User reads help/docs and chooses an action mode for evaluation. |
| Matrix config docs -> artifact expectations | User copies documented matrix examples into reproducibility runs. |
| Documentation -> future controller work | Env seam descriptions guide future RL/direct/residual work without granting current evaluator support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Repudiation | `src/main.py` argparse help | mitigate | Help names `velocity_command` as evaluator-runnable and describes non-default modes as fail-fast seams, reducing misleading unsupported-run claims. |
| T-07-06 | Repudiation | `docs/locomotion-benchmark.md` action-mode section | mitigate | Guide distinguishes env-supported seams from evaluator-runnable mode and keeps matrix example on `velocity_command`. |
| T-07-07 | Tampering | Documented matrix/action-mode examples | mitigate | Content tests require exact runnable/deferred wording; docs do not instruct users to produce unsupported artifacts. |
| T-07-08 | Tampering / Information Integrity | Export path/CSV safety when docs mention artifacts | accept | This plan does not touch artifact writer code; Plan 01 preserves `_prepare_run_dir()` containment and `_csv_safe()` formula safety. |
</threat_model>

<verification>
Quick gate: `uv run python -m pytest tests/locomotion/test_locomotion_evaluation_runner.py tests/locomotion/test_locomotion_evaluation_exports.py tests/locomotion/test_argus_go2_env_action_modes.py tests/test_main_args.py -q`

Docs-specific gate: `uv run python -m pytest tests/test_main_args.py tests/test_locomotion_benchmark_docs.py -q`

Full gate after Wave 1: `uv run python -m pytest tests/locomotion tests/bridge/test_sim_bridge.py tests/bridge/test_multi_bridge.py -q`
</verification>

<success_criteria>
- `argus eval-locomotion --help` includes the action-mode flag and exact evaluator-runnable/deferred wording.
- `docs/locomotion-benchmark.md` states current evaluation runs `velocity_command` and fails fast for `joint_position`/`residual_baseline` until explicit action sources exist.
- Existing documentation remains honest that all three action modes are environment seams.
- CLI/docs tests pass with exact content guards.
</success_criteria>

<output>
After completion, create `.planning/phases/07-align-evaluation-action-mode-contract/07-02-SUMMARY.md`
</output>
