---
phase: 05-harness-docs-and-comparison-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/locomotion-benchmark.md
  - README.md
  - tests/test_locomotion_benchmark_docs.py
autonomous: true
requirements:
  - LOC-REPORT-01
  - LOC-REPORT-02
  - LOC-REPORT-03
must_haves:
  truths:
    - "A new developer can open README.md, find the locomotion benchmark guide link, and copy the explicit flat-ground analytical smoke command."
    - "A new developer can read docs/locomotion-benchmark.md and understand the ArgusGo2Env reset/step surface, observation keys, action modes, reward/metric definitions, scenario catalog, CLI workflow, and artifact outputs."
    - "A reader can see a hard-boundary controller-family matrix that distinguishes supported analytical gait from unavailable/deferred residual RL, direct RL, MPC, WBC, ROS/hardware, and perception-conditioned locomotion."
    - "The guide links outputs/locomotion-rd-systems.md and ADR-0019 while accurately describing current Argus locomotion as analytical trot plus MuJoCo position actuators, not torque-control/RL/MPC/WBC/ROS/hardware."
    - "Markdown content guards verify required guide, README, artifact, matrix, and rationale tokens without executing MuJoCo or the benchmark command."
  artifacts:
    - path: "docs/locomotion-benchmark.md"
      provides: "Canonical locomotion benchmark harness guide and controller-family matrix"
      contains: "uv run argus eval-locomotion --controller analytical_trot --scenario flat_ground --seed 101 --seed 202"
    - path: "README.md"
      provides: "Compact locomotion benchmark command card and guide link"
      contains: "docs/locomotion-benchmark.md"
    - path: "tests/test_locomotion_benchmark_docs.py"
      provides: "Pathlib-only Markdown content guards"
      contains: "Path(__file__).resolve().parents[1]"
  key_links:
    - from: "README.md"
      to: "docs/locomotion-benchmark.md"
      via: "Markdown guide link"
      pattern: "docs/locomotion-benchmark\.md"
    - from: "docs/locomotion-benchmark.md"
      to: "outputs/locomotion-rd-systems.md"
      via: "R&D rationale link"
      pattern: "outputs/locomotion-rd-systems\.md"
    - from: "docs/locomotion-benchmark.md"
      to: "docs/adr/0019-benchmark-locomotion-before-adding-new-controller-families.md"
      via: "ADR-0019 scope rationale link"
      pattern: "docs/adr/0019-benchmark-locomotion-before-adding-new-controller-families\.md"
    - from: "tests/test_locomotion_benchmark_docs.py"
      to: "docs/locomotion-benchmark.md"
      via: "pathlib read_text content assertions"
      pattern: "GUIDE\.read_text"
    - from: "tests/test_locomotion_benchmark_docs.py"
      to: "README.md"
      via: "pathlib read_text content assertions"
      pattern: "README\.read_text"
---

<objective>
Create the Phase 5 locomotion benchmark documentation package: a focused harness guide, a terse README pointer, a hard-boundary controller-family matrix, and fast Markdown content guards.

Purpose: Phase 4 made the benchmark runnable; Phase 5 makes it legible and prevents documentation drift around supported versus deferred controller claims.
Output: `docs/locomotion-benchmark.md`, README command-card updates, and `tests/test_locomotion_benchmark_docs.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-harness-docs-and-comparison-matrix/05-CONTEXT.md
@.planning/phases/05-harness-docs-and-comparison-matrix/05-RESEARCH.md
@.planning/phases/05-harness-docs-and-comparison-matrix/05-VALIDATION.md
@.planning/phases/05-harness-docs-and-comparison-matrix/05-PATTERNS.md
@.planning/phases/04-evaluation-runner-and-regression/04-CONTEXT.md
@.planning/phases/04-evaluation-runner-and-regression/04-01-SUMMARY.md
@.planning/phases/04-evaluation-runner-and-regression/04-02-SUMMARY.md
@.planning/phases/04-evaluation-runner-and-regression/04-03-SUMMARY.md
@.planning/phases/04-evaluation-runner-and-regression/04-04-SUMMARY.md
@README.md
@src/main.py
@src/locomotion/evaluation.py
@src/locomotion/env.py
@src/locomotion/observations.py
@src/locomotion/actions.py
@src/locomotion/scenarios.py
@src/locomotion/metrics.py
@src/locomotion/controllers.py
@outputs/locomotion-rd-systems.md
@docs/adr/0018-use-reproducible-gymnasium-style-locomotion-benchmark-harness.md
@docs/adr/0019-benchmark-locomotion-before-adding-new-controller-families.md

<interfaces>
Concrete values locked by Phase 5 decisions and Phase 4 implementation:

- Blessed smoke command per D-03: `uv run argus eval-locomotion --controller analytical_trot --scenario flat_ground --seed 101 --seed 202`
- Matrix config command per D-04: `uv run argus eval-locomotion --matrix-config path/to/matrix.json`
- Saved-run regeneration command per D-04: `uv run argus eval-locomotion --from-run-dir outputs/locomotion-evals/<timestamp>/`
- Output directory per D-05: `outputs/locomotion-evals/<timestamp>/`
- Artifact filenames per D-05: `manifest.json`, `steps.jsonl`, `episodes.csv`, `summary.json`, `comparison.md`
- Observation keys: `qpos`, `qvel`, `command`, `previous_action`
- Action modes: `velocity_command`, `joint_position`, `residual_baseline`
- Scenario ids: `flat_ground`, `low_friction`, `slope`, `rough_heightfield`, `push_disturbance`
- Controller-family matrix columns per D-07: controller family, status now, Argus hook/seam, why supported/deferred, prerequisite to unlock, R&D rationale link
- Current supported controller id: `analytical_trot`
- Registered unavailable placeholders: `residual_policy`, `direct_policy`, `mpc`, `wbc`
- Deferred/no runnable v4.0 implementation families: `ROS/hardware`, `perception-conditioned locomotion`
- Current-method wording per D-09/T-05-03: `analytical trot plus MuJoCo position actuators`
- Rationale links per D-09/LOC-REPORT-03: `outputs/locomotion-rd-systems.md` and `docs/adr/0019-benchmark-locomotion-before-adding-new-controller-families.md`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical harness guide core</name>
  <files>docs/locomotion-benchmark.md</files>
  <read_first>
    - docs/locomotion-benchmark.md (if it exists)
    - .planning/phases/05-harness-docs-and-comparison-matrix/05-CONTEXT.md
    - .planning/phases/05-harness-docs-and-comparison-matrix/05-RESEARCH.md
    - .planning/phases/05-harness-docs-and-comparison-matrix/05-PATTERNS.md
    - src/main.py
    - src/locomotion/evaluation.py
    - src/locomotion/env.py
    - src/locomotion/observations.py
    - src/locomotion/actions.py
    - src/locomotion/scenarios.py
    - src/locomotion/metrics.py
    - docs/adr/0018-use-reproducible-gymnasium-style-locomotion-benchmark-harness.md
  </read_first>
  <action>Create `docs/locomotion-benchmark.md` per D-01 as the canonical Phase 5 harness guide. Include concise sections covering: (1) what the benchmark harness is, (2) the first runnable smoke workflow using exactly `uv run argus eval-locomotion --controller analytical_trot --scenario flat_ground --seed 101 --seed 202` per D-03, (3) `ArgusGo2Env.reset(seed=...)` returning observation plus info and `ArgusGo2Env.step(action)` returning observation, reward, terminated, truncated, info, (4) observation keys `qpos`, `qvel`, `command`, and `previous_action`, (5) action modes `velocity_command`, `joint_position`, and `residual_baseline`, (6) current reward wording that the environment currently returns reward `0.0` while locomotion quality is inspected through metrics, (7) metric families: command tracking, stability, action quality, and contact/terrain proxies, explicitly using position-servo proxy wording for effort/saturation, (8) scenario catalog ids `flat_ground`, `low_friction`, `slope`, `rough_heightfield`, and `push_disturbance`, (9) CLI examples for `--matrix-config` and `--from-run-dir` per D-04, and (10) artifact glossary under `outputs/locomotion-evals/<timestamp>/` listing `manifest.json`, `steps.jsonl`, `episodes.csv`, `summary.json`, and `comparison.md` per D-05. Include this exact JSON matrix-config example:

```json
{
  "controllers": ["analytical_trot"],
  "scenarios": ["flat_ground"],
  "seeds": [101, 202],
  "action_mode": "velocity_command",
  "max_episode_steps": 500,
  "sim_steps_per_frame": 10,
  "heightfield_size": 16
}
```

Do not document new runtime behavior, do not say reward shaping is implemented, do not call effort/saturation torque metrics, and do not add instructions that require running MuJoCo during documentation tests.</action>
  <verify>
    <automated>test -f docs/locomotion-benchmark.md && grep -F "uv run argus eval-locomotion --controller analytical_trot --scenario flat_ground --seed 101 --seed 202" docs/locomotion-benchmark.md && grep -F "--matrix-config" docs/locomotion-benchmark.md && grep -F "--from-run-dir" docs/locomotion-benchmark.md && grep -F "manifest.json" docs/locomotion-benchmark.md && grep -F "position-servo" docs/locomotion-benchmark.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/locomotion-benchmark.md` exists.
    - `docs/locomotion-benchmark.md` contains `uv run argus eval-locomotion --controller analytical_trot --scenario flat_ground --seed 101 --seed 202`.
    - `docs/locomotion-benchmark.md` contains `--matrix-config` and `--from-run-dir`.
    - `docs/locomotion-benchmark.md` contains all artifact names: `manifest.json`, `steps.jsonl`, `episodes.csv`, `summary.json`, `comparison.md`.
    - `docs/locomotion-benchmark.md` contains all observation keys: `qpos`, `qvel`, `command`, `previous_action`.
    - `docs/locomotion-benchmark.md` contains all action modes: `velocity_command`, `joint_position`, `residual_baseline`.
    - `docs/locomotion-benchmark.md` contains all scenario ids: `flat_ground`, `low_friction`, `slope`, `rough_heightfield`, `push_disturbance`.
    - `docs/locomotion-benchmark.md` contains `reward` and `0.0` in the reward discussion.
    - `docs/locomotion-benchmark.md` contains `position-servo` for effort/saturation proxy wording.
  </acceptance_criteria>
  <done>The canonical guide documents LOC-REPORT-01 plus D-01, D-03, D-04, and D-05 using concrete implemented harness facts.</done>
</task>

<task type="auto">
  <name>Task 2: Add the hard-boundary matrix and README command card</name>
  <files>docs/locomotion-benchmark.md, README.md</files>
  <read_first>
    - docs/locomotion-benchmark.md
    - README.md
    - .planning/phases/05-harness-docs-and-comparison-matrix/05-CONTEXT.md
    - .planning/phases/05-harness-docs-and-comparison-matrix/05-RESEARCH.md
    - src/locomotion/controllers.py
    - outputs/locomotion-rd-systems.md
    - docs/adr/0019-benchmark-locomotion-before-adding-new-controller-families.md
  </read_first>
  <action>Extend `docs/locomotion-benchmark.md` with a controller-family matrix per D-06, D-07, D-08, and D-09. The matrix columns must be exactly: `Controller family`, `Status now`, `Argus hook/seam`, `Why supported/deferred`, `Prerequisite to unlock`, `R&D rationale link`. Include rows for these exact families and statuses: `analytical gait` with status `supported now` and controller id `analytical_trot`; `residual RL` with status `registered placeholder unavailable` and controller id `residual_policy`; `direct RL` with status `registered placeholder unavailable` and controller id `direct_policy`; `MPC` with status `registered placeholder unavailable` and controller id `mpc`; `WBC` with status `registered placeholder unavailable` and controller id `wbc`; `ROS/hardware` with status `deferred/no runnable v4.0 implementation`; and `perception-conditioned locomotion` with status `deferred/no runnable v4.0 implementation`. Include hard-boundary prose saying v4.0 does not ship trained RL policies, MPC, WBC, ROS/hardware behavior, torque-control locomotion, or perception-conditioned locomotion. Include the exact current-method sentence `Current Argus locomotion is analytical trot plus MuJoCo position actuators.` Link `outputs/locomotion-rd-systems.md` and `docs/adr/0019-benchmark-locomotion-before-adding-new-controller-families.md` in the matrix/prose.

Update `README.md` per D-02 only as a compact pointer: keep the existing four-command section and existing locomotion section terse; add a visible link to `docs/locomotion-benchmark.md`, keep the blessed smoke command exactly `uv run argus eval-locomotion --controller analytical_trot --scenario flat_ground --seed 101 --seed 202`, and name the main artifacts `manifest.json`, `steps.jsonl`, `episodes.csv`, `summary.json`, and `comparison.md`. Do not copy the full controller-family matrix into README.</action>
  <verify>
    <automated>grep -F "| Controller family | Status now | Argus hook/seam | Why supported/deferred | Prerequisite to unlock | R&D rationale link |" docs/locomotion-benchmark.md && grep -F "Current Argus locomotion is analytical trot plus MuJoCo position actuators." docs/locomotion-benchmark.md && grep -F "outputs/locomotion-rd-systems.md" docs/locomotion-benchmark.md && grep -F "docs/adr/0019-benchmark-locomotion-before-adding-new-controller-families.md" docs/locomotion-benchmark.md && grep -F "docs/locomotion-benchmark.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/locomotion-benchmark.md` contains the exact matrix header `| Controller family | Status now | Argus hook/seam | Why supported/deferred | Prerequisite to unlock | R&D rationale link |`.
    - `docs/locomotion-benchmark.md` contains `analytical gait`, `supported now`, and `analytical_trot`.
    - `docs/locomotion-benchmark.md` contains `residual RL`, `registered placeholder unavailable`, and `residual_policy`.
    - `docs/locomotion-benchmark.md` contains `direct RL`, `registered placeholder unavailable`, and `direct_policy`.
    - `docs/locomotion-benchmark.md` contains `MPC`, `registered placeholder unavailable`, and `mpc`.
    - `docs/locomotion-benchmark.md` contains `WBC`, `registered placeholder unavailable`, and `wbc`.
    - `docs/locomotion-benchmark.md` contains `ROS/hardware` and `deferred/no runnable v4.0 implementation`.
    - `docs/locomotion-benchmark.md` contains `perception-conditioned locomotion` and `deferred/no runnable v4.0 implementation`.
    - `docs/locomotion-benchmark.md` contains `Current Argus locomotion is analytical trot plus MuJoCo position actuators.`.
    - `docs/locomotion-benchmark.md` contains `outputs/locomotion-rd-systems.md` and `docs/adr/0019-benchmark-locomotion-before-adding-new-controller-families.md`.
    - `README.md` contains `docs/locomotion-benchmark.md`.
    - `README.md` contains the smoke command and all five artifact names.
  </acceptance_criteria>
  <done>LOC-REPORT-02 and LOC-REPORT-03 are documented with hard support/deferred boundaries, rationale links, and README discoverability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add pathlib-only Markdown content guards</name>
  <files>tests/test_locomotion_benchmark_docs.py</files>
  <read_first>
    - tests/test_locomotion_benchmark_docs.py (if it exists)
    - tests/locomotion/test_locomotion_evaluation_exports.py
    - tests/test_main_args.py
    - README.md
    - docs/locomotion-benchmark.md
    - .planning/phases/05-harness-docs-and-comparison-matrix/05-CONTEXT.md
    - .planning/phases/05-harness-docs-and-comparison-matrix/05-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: README content guard reads `README.md` using `Path.read_text(encoding="utf-8")` and asserts it contains `docs/locomotion-benchmark.md`, the blessed smoke command, and all five artifact names.
    - Test 2: Guide content guard reads `docs/locomotion-benchmark.md` and asserts it contains observation keys, action modes, scenario ids, CLI examples, artifact names, and metric/reward wording.
    - Test 3: Matrix content guard asserts all seven controller families are present with supported/unavailable/deferred boundary tokens and known placeholder ids.
    - Test 4: Rationale/current-method guard asserts `outputs/locomotion-rd-systems.md`, ADR-0019 path, and `Current Argus locomotion is analytical trot plus MuJoCo position actuators.` are present.
    - Test 5: Source guard asserts the test file itself does not contain `subprocess`, `uv run`, or `eval-locomotion --controller` outside constants that are only compared as strings; implement this without running any command.
  </behavior>
  <action>Create `tests/test_locomotion_benchmark_docs.py` per D-10, D-11, and D-12 using only stdlib `pathlib` and plain pytest asserts. Define constants:

```python
ROOT = Path(__file__).resolve().parents[1]
README = ROOT / "README.md"
GUIDE = ROOT / "docs" / "locomotion-benchmark.md"
SMOKE_COMMAND = "uv run argus eval-locomotion --controller analytical_trot --scenario flat_ground --seed 101 --seed 202"
ARTIFACTS = ("manifest.json", "steps.jsonl", "episodes.csv", "summary.json", "comparison.md")
FAMILIES = ("analytical gait", "residual RL", "direct RL", "MPC", "WBC", "ROS/hardware", "perception-conditioned locomotion")
```

Do not import `subprocess`, do not call `uv`, do not construct `ArgusGo2Env`, and do not execute the smoke benchmark. Tests must read Markdown via `README.read_text(encoding="utf-8")` and `GUIDE.read_text(encoding="utf-8")`. Use `.venv/bin/python -m pytest`, not `.venv/bin/pytest`, for verification.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_locomotion_benchmark_docs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_locomotion_benchmark_docs.py` exists.
    - `tests/test_locomotion_benchmark_docs.py` contains `from pathlib import Path`.
    - `tests/test_locomotion_benchmark_docs.py` contains `ROOT = Path(__file__).resolve().parents[1]`.
    - `tests/test_locomotion_benchmark_docs.py` contains `README = ROOT / "README.md"`.
    - `tests/test_locomotion_benchmark_docs.py` contains `GUIDE = ROOT / "docs" / "locomotion-benchmark.md"`.
    - `tests/test_locomotion_benchmark_docs.py` contains `SMOKE_COMMAND = "uv run argus eval-locomotion --controller analytical_trot --scenario flat_ground --seed 101 --seed 202"`.
    - `tests/test_locomotion_benchmark_docs.py` does not import `subprocess`.
    - `.venv/bin/python -m pytest tests/test_locomotion_benchmark_docs.py -q` exits 0.
  </acceptance_criteria>
  <done>Fast content guards cover LOC-REPORT-01/02/03 and enforce D-10/D-11/D-12 without runtime benchmark execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation-reader | New developers may treat README/guide statements as operational truth for supported controller families and benchmark workflow. |
| test-runner-filesystem | Pytest reads Markdown files from the repository; Phase 5 tests must stay content-only and must not cross into MuJoCo/runtime execution. |
| rationale-classification | Documentation maps Argus current locomotion to research/control taxonomy; inaccurate wording can make unsupported torque-control/RL/MPC/WBC/ROS/hardware claims look valid. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering / Repudiation | `docs/locomotion-benchmark.md` controller-family matrix | mitigate | Use hard-boundary matrix language with statuses `supported now`, `registered placeholder unavailable`, and `deferred/no runnable v4.0 implementation`; content tests assert required families/statuses so unsupported controller use is not implied. |
| T-05-02 | Denial of Service | `tests/test_locomotion_benchmark_docs.py` | mitigate | Implement pathlib Markdown content checks only; do not import `subprocess`, do not call `uv`, do not construct `ArgusGo2Env`, and do not execute MuJoCo benchmark/runtime smoke commands. |
| T-05-03 | Tampering / Information Disclosure | `docs/locomotion-benchmark.md` rationale/current-method wording | mitigate | Require links to `outputs/locomotion-rd-systems.md` and `docs/adr/0019-benchmark-locomotion-before-adding-new-controller-families.md`; assert exact current-method wording `Current Argus locomotion is analytical trot plus MuJoCo position actuators.` so Argus is not misclassified as torque-control/RL/MPC/WBC/ROS/hardware. |
</threat_model>

<verification>
Run these checks after all tasks:

```bash
.venv/bin/python -m pytest tests/test_locomotion_benchmark_docs.py -q
.venv/bin/python -m pytest tests/test_locomotion_benchmark_docs.py tests/test_main_args.py -q
```

Before `/gsd-verify-work`, run the Phase 5 gate from validation strategy:

```bash
.venv/bin/python -m pytest tests/locomotion tests/bridge/test_sim_bridge.py tests/bridge/test_multi_bridge.py tests/test_main_args.py tests/test_locomotion_benchmark_docs.py -q
```
</verification>

<success_criteria>
- `docs/locomotion-benchmark.md` exists and covers LOC-REPORT-01, LOC-REPORT-02, and LOC-REPORT-03.
- README links `docs/locomotion-benchmark.md`, keeps the locomotion benchmark pointer terse, and includes the blessed smoke command plus artifact names.
- The guide contains the hard-boundary controller-family matrix with all required families and statuses.
- The guide links `outputs/locomotion-rd-systems.md` and ADR-0019 and states the current method as analytical trot plus MuJoCo position actuators.
- `tests/test_locomotion_benchmark_docs.py` uses pathlib-only file reads and passes under `.venv/bin/python -m pytest tests/test_locomotion_benchmark_docs.py -q`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-harness-docs-and-comparison-matrix/05-01-SUMMARY.md`.
</output>
