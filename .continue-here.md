# Continue Here: AGIBOT X2 Adaptation

Created: 2026-04-30
Worktree: `/home/prannayag/pragnition/robotics/argus/.worktrees/agibot-x2-adaptation`
Branch: `agibot-x2-adaptation`
Latest approved commit: `546b156 fix: wire platform metadata into web app`

## Resume instruction

Continue the AGIBOT X2 adaptation in the existing worktree. Do not create a new worktree. Use `superpowers:subagent-driven-development` and resume at Task 11 unless the recovered plan says otherwise.

## Current state

Tasks 1-10 are complete and reviewed. Task 10 was completed after a quality-review fix that wired bridge platform metadata into the production web app startup path.

Recent completed commits include:

- `546b156 fix: wire platform metadata into web app`
- `6480611 feat: stream robot platform runtime state`
- `89fcb7c feat: isolate disabled robots in coordinator`
- `bcbe68b fix: harden platform bridge runtime status`
- `6052d86 refactor: route multi-robot bridge through platforms`
- `94975b8 fix: load generated scenes with assets`
- `c54c51b fix: preserve scene builder runtime contracts`
- `79cb518 feat: build multi-robot scenes through platform metadata`

The original referenced plan path was `/home/prannayag/pragnition/robotics/argus/docs/superpowers/plans/2026-04-30-agibot-x2-adaptation-design.md`, but that file was not present in this worktree during pause. Use this handoff plus git history if the plan is unavailable.

## Completed work

- Task 4: X2 walking-controller boundary.
  - Added `src/locomotion/x2_controller.py`.
  - Added controller tests under `tests/bridge/platforms/test_x2_controller.py`.
  - Hardened policy loading, bounds validation, clamp counts, and non-finite output fallback.

- Task 5: AGIBOT X2 platform.
  - Added `src/bridge/platforms/agibot_x2.py`.
  - Registered AGIBOT X2 in `src/bridge/platforms/__init__.py`.
  - Added AGIBOT X2 platform tests.
  - Fixed qvel indexing for freejoint/multi-robot qpos offsets and cached XML-derived metadata.

- Task 6: Generic collision and near-miss state.
  - Added `src/bridge/collision_state.py`.
  - Added collision-state tests.

- Task 7: Generic scene builder and config platform selection.
  - Added `platform` and `platform_config` fields to `MultiRobotConfig`.
  - Added generic `build_multi_robot_scene(...)`.
  - Preserved Go2 position-actuator patching and generated asset loading.
  - Updated spawn height support.

- Task 8: Refactor `MultiRobotBridge` through `RobotPlatform`.
  - Routed platform creation, root body discovery, actuator discovery, controller compute, runtime statuses, collision summaries, and generic robot commands through platform abstractions.
  - Added platform-selection tests.
  - Fixed disabled robots so runtime status records the actual applied stop command.

- Task 9: Coordinator generic commands and disabled-robot isolation.
  - Coordinator now forwards velocity/stand/stop/recover commands through `RobotCommand` APIs.
  - Disabled robots are stopped and skipped in exploration.
  - Viz data includes platform and runtime status payloads.

- Task 10: Backend platform metadata and runtime status stream.
  - `backend/web/server.py` accepts and sends platform metadata in the `robot_list` WebSocket handshake.
  - `backend/web/streaming_viz.py` includes platform and runtime status in per-robot stats.
  - `src/main.py` now passes bridge platform metadata into `create_app(...)` in real web mode.
  - Added focused tests in `tests/web/test_web_server.py`, `tests/web/test_platform_metadata_stream.py`, and `tests/test_main_args.py`.

## Known working tests from the session

The relevant task tests were run through `uv run pytest ...` during implementation/review loops. Before resuming feature work, run a targeted smoke test for the areas touched by Tasks 4-10, then continue with Task 11.

Suggested smoke command:

```bash
uv run pytest tests/bridge/platforms/test_x2_controller.py tests/bridge/platforms/test_agibot_x2_platform.py tests/bridge/test_collision_state.py tests/bridge/test_multi_bridge.py tests/bridge/test_multi_bridge_platform_selection.py tests/coordination/test_coordinator.py tests/web/test_web_server.py tests/web/test_platform_metadata_stream.py tests/test_main_args.py
```

## Pending work

- Task 11: Frontend store and message contracts.
- Task 12 onward: remaining AGIBOT X2 adaptation plan tasks, if present in the recovered plan.

## Important constraints

- Stay in `/home/prannayag/pragnition/robotics/argus/.worktrees/agibot-x2-adaptation`.
- Do not create a new worktree.
- Keep scope tight.
- Do not skip spec-compliance or code-quality reviews.
- Commit each task/fix atomically.
- Use `uv run pytest ...` for backend tests.
- For UI/frontend changes, start the dev server and verify the feature in a browser before reporting completion.

## Current uncommitted context

At pause time, `.planning/STATE.md` was already modified. This appears to be GSD workflow state and should be reviewed before including it in any implementation commit.
